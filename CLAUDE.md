# CLAUDE.md — TEN 작업 지침

이 파일은 AI 협업 세션이 저장소에서 작업을 시작할 때 자동으로 읽는 지침서다.
Claude는 매 세션이 시작될 때 이 파일을 기준으로 프로젝트 맥락을 복원한다.
(성권님 계정은 지속 메모리가 꺼져 있어, 이 저장소가 세션 간 기억을 대신한다.)

---

## 프로젝트 한 줄 요약

**TEN** — 슬롯 10개 카드 배틀러. 크리처끼리 전투하지 않고, 각자 자기 턴 종료 시 상대 플레이어를 자동으로 때린다. 개입 수단은 **스펠**과 **수호**뿐. 자세한 규칙은 `README.md`, 설계 배경은 `docs/design_deck.html` 참조.

## 담당자

- 성권 (sepi-toolbox) — 게임 시스템 디자이너. 규칙 설계와 데이터 테이블 작업이 중심.
- 설명은 데이터 테이블·수치 중심으로. 기초 프로그래밍 지식 있음(깊은 내부 구현보다 규칙·밸런스 관점 우선).

### 새 카드를 만드는 법 (매수 0 = 덱 미수록)

골격(20종 · 23장 · 커브)이 꽉 차 있어서 카드를 그냥 더하면 검산이 깨진다.
그래서 `DECKS` 의 **매수(copies)를 0 으로 두면 카드 풀에만 실리고 덱에는 안 들어간다.**

- 예산 검산은 똑같이 받는다(초과하면 잡힌다). 종수·장수·커브 검산에서는 빠진다.
- `promote_decks` 가 덱 목록(`entries`)에 안 넣고 POOL 에만 넣는다.
- 뷰어는 그 속성 칸 **맨 뒤에** 붙여 보여 주고 카드 밑에 '덱 미수록' 이라고 적는다
  (`poolOnlyOf(el)`). ⚠ 이걸 안 하면 만든 카드가 **어디에도 안 보인다.**
- 실제로 덱에 넣을 때는 매수를 1~2 로 올리고 **같은 코스트에서 한 종을 빼야** 커브가 맞는다.
- ⚠ `test_cards.js` 의 '덱 7종으로 묶임' 은 셀 개수가 아니라 **헤더의 '21종 · 40장'** 으로 본다 —
  덱 미수록 카드가 셀 수를 늘리기 때문이다.


## 카드 데이터 파이프라인 (2026-07 개편 — 정본이 xlsx에서 코드로 옮겨졌다)

**카드 정본은 `tools/gen_decks.py`의 `DECKS` 딕셔너리 하나뿐이다.** 140종 전부 여기 있다.
`data/`의 CSV·JSON은 전부 **생성되는 산출물**이므로 직접 손대지 않는다.

```bash
# 1) 카드를 고친다 → tools/gen_decks.py 의 DECKS
python3 tools/gen_decks.py --check      # 예산 검산만 (빠름)

# 2) 데이터 생성
python3 tools/gen_grown.py --write      # 성장·진형 상위 몸 → data/grown.json
python3 tools/promote_decks.py          # DECKS → creatures/spells/enchants.csv · cards.json · decks.json
python3 tools/build_proto_data.py       # cards.json·decks.json → 프로토타입 POOL·DECKS 주입
python3 tools/build_prototype.py        # 아트 data URI 주입 (공유 키로 중복 제거)
python3 tools/build_editor.py           # CSV → card_editor.html · card_gallery.html
python3 tools/build_deck_doc.py         # DECKS → docs/sample_decks.html

# 3) 검산 — 통과(exit 0)해야 커밋한다
python3 tools/validate_budget.py        # 예산 + CSV/POOL 동기화 + 7덱 40장

# 4) 게시 — ⚠⚠ **이걸 안 하면 사이트는 안 바뀐다.** git push 는 배포가 아니다.
python3 tools/build_pages.py --push
python3 tools/check_published.py        # 게시본 == 작업본 인지 확인 (exit 1 이면 아직 옛 판)
```

> ### ⚠⚠ 푸시는 **헤더로** 인증한다 (2026-08)
> 이 환경의 git 프록시가 세션 중간부터 URL 에 박은 토큰(`https://x-access-token:…@`)을
> 걷어내고 403 을 준다("이 저장소는 이 세션의 허가 목록에 없다"). 헤더로 주면 통과한다:
> ```bash
> T=$(cat ~/.config/ten/token); B=$(printf 'x-access-token:%s' "$T" | base64 -w0)
> git -c "http.extraHeader=Authorization: Basic $B" push https://github.com/sepi-toolbox/ten.git HEAD:main
> ```
> `tools/build_pages.py` 의 `push()` 도 같은 방식으로 고쳤다.

> ### ⚠⚠⚠ `git push` 는 배포가 아니다
> GitHub Pages 는 `main` 이 아니라 **`gh-pages` 브랜치**를 게시한다.
> main 에 커밋·푸시하고 "올렸다" 고 말하면 **거짓말이 된다.**
>
> 실제로 이걸 밟았다 — 판 넷(0.24 → 0.27)을 올리는 동안 사이트는 **0.23.0 에 멈춰** 있었다.
> 성권이 "지운 카드가 아직 보인다" 며 스크린샷을 보내고, 캐시인 줄 알고 앱을 **다시 깔기까지** 했다.
> 나는 저장소만 확인하고 "커밋된 빌드는 정상이니 기기 캐시" 라고 답했다. 저장소는 정상이었지만
> **게시본은 정상이 아니었다.** 확인해야 할 것은 저장소가 아니라 `gh-pages` 였다.
>
> **판을 올렸다고 말하기 전에 반드시 `check_published.py` 를 돌린다.**

- `tools/extract_cards.py`(xlsx → CSV)는 **보류 상태**다. 그냥 돌리면 승격된 140종을 옛 20종으로 덮어쓰므로
  `--force-legacy` 없이는 exit 2로 멈춘다. `data/ten_balance.xlsx`는 보관용이다.
- `tools/card_editor.html`로 편집한 결과를 되돌리려면 `build_from_data.py`가 아니라 **DECKS를 직접 고친다.**
  에디터는 이제 시각 확인·검토용이다(140종 전부 표시).
- `validate_budget.py`는 CSV가 DECKS와 어긋나면 실패한다 → 승격을 빠뜨리면 바로 잡힌다.

## 커밋 규칙

- 제목은 한국어, 무엇을 왜 바꿨는지 한 줄로.
- 데이터 변경이면 검산 통과 여부를 본문에 남긴다.
- push 대상: `origin` = https://github.com/sepi-toolbox/ten (main 브랜치).
- 클라우드 세션에서 push하려면 성권님의 GitHub 토큰이 필요하다(세션 한정, 저장 안 함).

## 예산 공식 (검산 기준)

```
크리처 예산   P(n) = round(a·n + b + (n−1)²·k) + 태그 보정      a=3, b=1, k=0.5
스탯 소모      일반 2A+H · 수호 2A+2H · 비행 4A+H · 비행수호 4A+2H
인챈트 총산출  T = 효과값 E × 충전 C
수호 ATK 캡    코스트−1 이하 (막으면서 때리는 만능 카드 방지)
```

계수를 바꾸려면 `data/ten_balance.xlsx`의 규칙상수 시트에서 수정한다.

## 확정 규칙 (규칙 결정은 여기 + README에 기록. xlsx는 수식 캐시 보존 위해 건드리지 않는다)

- **덱 소진 = 즉시 패배.** 덱이 빈 상태에서 드로우해야 하면 그 플레이어 패배. (누적 피해 방식 폐기)
- **슬롯 만석 = 소환 불가.** 10칸이 다 차면 새 카드 배치 불가. 밀어내기 없음.
- **동명 중첩 규칙 폐기(2026-08).** 같은 이름이라도 겹쳐 놓을 수 없다 — **크리처 1종 = 슬롯 1칸**.
  `STACKMAX`·`findStackable`·`leftStack`·`×N` 뱃지(`.tstk`/`.fstk`/`.tpips`) 전부 제거했다.
  ⚠ 유닛의 `insts` 배열은 **구조만 남았다**(항상 길이 1). 가호·개체 체력이 이 배열에 걸려 있어
  통째로 걷어내지 않았다. 새 코드는 `u.insts[0]`을 그냥 쓰면 되고, 길이가 1보다 커지는 경로를 만들지 말 것.

## 현재 상태 (2026-07-31)

- 초기 커밋 + 규칙 결정 반영 완료. 카드 22종(크리처12·스펠5·인챈트1 채용 + 잠정2) 전부 예산 범위 내, 덱 30장 정합.
- 프로토타입(`prototype/index.html`)에 위 확정 규칙 2건 반영됨(덱 소진 패배, 만석 소환 불가). 턴 종료 처리는 `크리처 공격 → 인챈트 발동(마지막)` 순.
- 프로토타입은 아직 카드 풀(POOL)을 하드코딩 중 — `data/cards.json`을 읽도록 리팩터하는 것이 다음 후보.
- **카드 규격 — 폭 하나로 결정된다**: `.tcard`는 `--cw`(폭)와 `aspect-ratio:5/7`만 받고, 내부 치수(글자·동그라미·아트·뱃지·여백)는
  전부 `calc(var(--cw) * 비율)`이다. 따라서 **보드·손패·확대 카드가 완전히 같은 규격**이고, **효과문이 길어져도 카드 크기가 변하지 않는다.**
  - 보드 `--cw:100cqw` (슬롯이 `container-type:inline-size`) · 손패 `86px` · 확대 `min(72vw,260px)`. 슬롯 비율도 5:7로 통일.
  - **모든 줄이 고정 높이다**: 머리(.thead) · 효과문(.tbody, 정확히 3줄) · 스탯(.tstat)까지 전부 고정이고, 남는 높이는 아트가 받는다.
    ⚠ 어느 한 줄이라도 내용에 따라 높이가 변하면 **그 차이를 아트가 흡수해 카드마다 일러스트 크기가 달라진다.**
    실제로 그렇게 깨졌던 원인 둘: (1) 코스트 동그라미 줄바꿈으로 머리가 높아짐 → `flex-wrap:nowrap` + 높이 고정,
    (2) 스탯 뱃지가 없는 스펠·지형의 `.tstat`이 얇아짐 → 높이 고정 + 스펠도 빈 `.tstat`을 항상 렌더.
  - 갤러리·에디터 템플릿도 같은 규칙을 쓴다: `.card{aspect-ratio:5/7}` + `.art{height:47%}` + 머리·태그·효과·하단 전부 고정 높이.
  - 크기를 바꾸려면 `--cw` 하나만 건드린다. 개별 요소 크기를 따로 잡지 말 것(규격이 다시 어긋난다).
- **카드 확대**: 보드·손패 카드를 길게 누르면(≈0.42초) 확대. 손패는 `.hcw`, 보드는 `.slot.occ`가 대상이고
  지형 카드와 소환 토큰도 확대된다(POOL에 없으므로 `zoomHTML`이 따로 처리). 손가락이 12px 넘게 움직여야 취소된다.
- **태그·키워드 설명은 카드 본문에 넣지 않는다.** 카드에는 **이름만** 찍는다(예: `수호 · 대가 8`).
  설명은 확대했을 때 **카드 바깥 옆 패널**(`.zside`)에 붙는다. 카드 레이아웃을 건드리지 않으므로 설명이 길어도 규격이 안 변한다.
  - 용어집은 `tools/build_proto_data.py`의 `GLOSSARY`에 한 곳으로 정의 → `POOL_START` 블록에 주입.
    키는 카드에 찍히는 이름의 첫 낱말(`"연소 3"` → `연소`). 키워드를 추가하면 여기에도 항목을 넣는다.
  - `glossaryFor(name,u)`가 태그(g/f/p)와 `kw` 문자열에서 용어를 뽑아 패널을 만든다. 좁은 화면에서는 카드 아래로 내려간다.
- **스탯 표기 원칙**: 카드에 인쇄되는 ATK/HP는 **기본값**, 전투 중 현재값은 보드 위 **오버레이 뱃지**로 표시한다(피해=빨강, 버프=초록, 스택은 우측 pip). 인챈트는 충전을 보라 뱃지로. `python3 tools/build_prototype.py`가 아트를 프로토타입에 주입(ART 블록, 멱등).
- `tools/card_editor.html`: **비주얼 카드 디자이너**. 카드 그리드 → 탭하면 편집 모달(이름·코스트·프레임/태그·능력치·매수·효과설명·아트 선택 + 실시간 미리보기·예산 검산). ten_data.json 내보내기. 라운드트립 무손실.
- `tools/card_gallery.html`: 카드 갤러리(보기 전용, 필터). 에디터와 동일 렌더.
- 아트 라이브러리(20종 SVG 모티프)는 `build_editor.py`의 MOTIFS에서 한 번 정의 → 두 뷰어에 `__MOTIFS__`로 주입. 아트는 SVG 라인아트(래스터 일러스트 아님).
- **속성 체계(7종)**: fire·water·nature·steel·earth·dark·light. **카드 프레임 색 = 속성**, **카드 타입(크리처/스펠/인챈트) = 이름 왼쪽 아이콘**, 태그(수호·비행·관통) = 효과문 첫머리 굵은 키워드. 팔레트·아이콘은 `build_editor.py`의 ELEMENTS/TYPEICONS에서 정의 → 템플릿에 주입.
  - 프로토타입 **보드 슬롯은 태그 색 유지**(수호=에메랄드 광채/비행=호박 점선) — 전투 중에는 규칙 정보가 우선. 속성 색은 확대 카드·손패 좌측 띠에 적용.
  - 기존 20종 속성은 아트·테마 기준 **잠정 배정**(강철5·대지4·어둠4·물2·자연2·불2·빛1) — 불균형하므로 재배정 검토 필요.
- 설계 문서: `docs/meta_design.html`(아키타입·상성·위협↔해답), `docs/element_design.html`(7속성 메인+서브2·가격 규칙), `docs/keywords.html`(⚠ **2026-08 키워드 전면 개정 이전 문서라 낡았다** — 규칙은 위 '크리처가 지는 효과 전부' 표를 볼 것), `docs/spells.html`(속성별 시그니처 스펠 14종·티어 확장), `docs/effects_table.html`+`.csv`(효과 49종 종합표 — 속성/구분 필터), `docs/land_system.html`(지형 시스템 — 매수 확률 계산·코스트 개편), `docs/sample_decks.html`(속성별 견본 덱 140종 — 메커니즘·20종·커브). 관통 채택 확정.
  - 드로우 환산 신설: 1마나 ≈ 크리처 예산 4 · HP 8 지불 = 1마나 · 드로우 N장 = 1.5N 마나 · 크리처 희생 = 1.5마나 · 파괴 = 6코.
  - **요격(제거) 티어 개정 — 오목 곡선**: 단일 제거 1코 2 · 2코 4 · 3코 5 · 4코 6 · 5코 7 · 6코 파괴.
    크리처는 마나당 예산이 4.0↗5.3으로 오르고 제거는 2.0↘1.4로 내려가 **고코스트 크리처가 유리**해진다.
    요격은 속성마다 **저코스트(1~2코, 소형만) + 고코스트(5~6코, 확실하되 마나 1:1)** 2종씩.
    ⚠ 기존 카드 재조정 필요: **소멸**(5코 12피해 → 티어 7), **처형**(3코 6 → 티어 5).
  - 스펠 티어 신설 5종: 약화(단일×0.75)·강화(×1.2)·회복(×2.0)·파쇄·바운스. 소환은 같은 코스트 크리처 예산으로 환산.
  - 키워드 겹침 규칙: **배수(연소·증식)를 먼저 적용하고 가감을 나중에 더한다.** 태그(수호/비행/관통)는 스탯 가중치라 배타적, 신규 키워드는 예산 가감이라 중첩 가능.
- 데이터에 `art`(모티프 키)·`desc`(커스텀 효과문)·`image`(생성 아트 경로) 컬럼 추가됨. extract/build_from_data 모두 보존.
- **생성 아트 연동**: `assets/art/<id>.png`에 카드별 일러스트. 카드에 `image` 있으면 그림, 없으면 SVG 모티프로 자동 폴백. `build_editor.py`가 이미지를 리사이즈·data URI로 임베드(자립형 HTML). 아트 소스 = ChatGPT 4×5 시트를 `tools/`(임시)에서 20분할 → assets/art. 새 아트 넣는 법: PNG를 assets/art/<id>.png로 두고 `build_editor.py` 재실행.

## 판(보드·지형존) — 10칸 격자를 버리고 '가운데로 모인 목록'으로 (2026-08)

모바일에서 10칸 격자로 나누니 카드가 35px까지 줄어 읽을 수 없었다. 격자를 걷어냈다.

- **보드는 구멍 없는 목록이다.** `S[p].board`는 packed 배열이고 **몇 번째냐가 곧 위치**다.
  `placeCreature(p,name,at)`은 그 자리에 **끼워 넣는다**(splice). `cleanup()`은 죽은 것을 빼고 다시 채운다.
  ⚠ 예전 코드처럼 `if(board[idx])return`으로 막으면 **끼워 넣기가 통째로 죽는다**(실제로 밟았다).
- ~~진형은 자리가 바뀌면 다시 계산한다(`reform`)~~ — **폐기.** 진형은 자리와 무관하고,
  **내 크리처가 그 하나뿐이 되면** `endStep` 에서 `각성한 X` 로 갈아입는다(아래 '성장·진형' 참고).
- **렌더는 `packRow(el)`** — 놓인 카드만, 가운데로 모으고, 폭이 모자라면 오른쪽이 왼쪽을 덮으며 겹친다.
  ⚠ **카드는 `left`로 절대 배치한다.** 플렉스 + 음수 `margin`으로 하려다 실패했다.
  ⚠⚠ **`.slot`의 기본 `transition`은 `all`이라 `left`까지 물린다.** 방금 넣은 값을 바로 재면 아직 0이 나와
    "스타일이 안 먹는다"고 착각하게 된다(margin·left 모두 이걸로 두 번 헤맸다).
    판에서는 `transition`을 색·그림자로 좁혀 위치를 즉시 반영한다. **테스트도 배치 직후가 아니라 몇 프레임 뒤에 잴 것.**
- **카드 규격은 하나다 — `--cardw`.** 손패·크리처 판·지형존이 **전부 같은 폭**을 쓴다.
  (한때 판·지형존을 따로 줄였다가 되돌렸다 — 규격이 갈리면 안 된다.)
  모바일 세로 예산이 빠듯해서 이 값을 키우면 4개 판 + 손패가 함께 커진다(1px당 약 8.4px).
  `tools/test_mobile.js`의 '바닥초과'가 곧 예산 초과 경보다.
- **지형존은 자리가 남아도 항상 겹쳐 쌓는다**(`packRow(el, stack=true)`, 노출 52%).
  넘칠 때만 겹치면 지형이 적을 때 그냥 나열돼 보인다 — 요청한 형식은 '손패처럼 쌓이는 것'이다.
- **끼워 넣을 자리 미리보기**: 드래그 중 손끝 x로 `insertAt(x)`를 구해 `#dropbar`(세로 막대)를 그린다.
  카드 중심을 지나면 다음 자리. 판이 비면 한가운데. 드롭하면 `playFromHand(i,hit,at)`이 그 자리에 넣는다.
  `dropbar`의 z-index는 드래그 고스트(88)보다 높아야 손가락에 가려지지 않는다.

## 앱 흐름 — 단계마다 '페이지' (2026-08)

```
시작 → 모드 선택 → 덱 선택 → (배틀: 상대 덱 선택 | 원정: 지도) → 게임
패배/승리 → 다시 도전하기(덱 선택으로) · 초기 화면(모드 선택으로)
```

- 각 단계는 팝업이 아니라 **화면 전체를 쓰는 페이지**(`#page`, `pgMode/pgDeck/pgFoe`).
  상태는 `FLOW={mode,my,foe}`, 현재 단계는 `PAGE`.
- 배틀은 `PDECK`/`EDECK`에 고른 두 덱을 넣고 `newGame()`(→ `startBattle()`).
  ⚠ `setDeck(el)`은 **`PDECK=EDECK=null`로 지운 뒤** 새 덱을 물린다 — 안 지우면 흐름에서 고른
  옛 덱이 계속 나온다(테스트가 이걸로 무더기로 깨졌다).
- 원정은 덱 선택이 곧 속성 선택이다(`rgStart(el)` → 지도). `rgPickEl()`은 호환용 껍데기.
- **`?dev=1`로 열면 페이지를 건너뛰고 바로 배틀로 들어간다.** 모든 자동 테스트가 이걸 쓴다
  (`tools/test_flow.js`만 실제 흐름을 밟는다).
- 겹침 순서: 페이지 120 < 톱니 130 < 전환 가리개 150 < 설정 딤 155 < 설정 팝업 160 < 스플래시 200.
  톱니는 페이지 위에서도 눌려야 한다(버전·설치 확인).
- **설정은 가운데 사각 팝업**(예전엔 서랍). 안에 버전(`#verTag`, `VERSION`/`BUILD` 상수) ·
  덱 다시 고르기 · 모드 선택 · 덱 메커니즘 · 로그 · 속도 · 새 게임 · 앱 설치가 들어 있다.

## 로딩 표시 3종 (2026-08)

- **`boot`** — 앱 시작 스플래시. 글꼴이 늦게 오면 카드 글자가 튀므로 `document.fonts.ready`까지 기다린다.
  **최대 2.5초 뒤에는 무조건 걷는다**(멈춘 것처럼 보이는 게 최악이다).
- **`veilOn/veilOff/veilRun(text, work, ms)`** — 화면을 **가리는** 전환 로딩.
  모드 전환·새 게임·덱 교체·원정 전투 진입에 쓴다. 1.6초 안전장치로 절대 갇히지 않는다.
- **`busyOn/busyOff`** — **가리지 않고 조작만 막는** 로딩. 위쪽 얇은 진행 막대 + 투명 입력 차단막.
  `render()`가 `S.busy`를 보고 자동으로 켠다(전투 처리·상대 턴).
- ⚠ 지속 시간은 **`SPEED`로 나눈다**(`veilRun`). 자동 테스트가 `SPEED`를 올려 두면 즉시 사라져 방해하지 않는다.

## 서랍이 터치를 먹던 문제 (2026-08 · 꼭 기억할 것)

`.wrap{position:relative;z-index:1}`이 **쌓임 맥락**을 만든다. 그 안에 있던 `.side`(z-index 70)는
`body.sideon::after` 딤(z-index 69)보다 **아래**로 깔린다 — z 숫자만 보면 이길 것 같지만 맥락이 다르다.
그래서 서랍을 누르면 딤이 먹어 **스크롤도 안 되고 바로 닫혔다.**
→ **톱니와 서랍은 `.wrap` 밖(body 직계)에 둔다.** 오버레이와 그 위에 뜨는 패널은 같은 맥락에 있어야 한다.

## 앱으로 설치 (PWA · 2026-08)

- `prototype/manifest.webmanifest` + `sw.js` + 아이콘 4종(192·512·maskable·apple-touch).
  `build_pages.py`의 `FILES`에 들어 있어야 게시된다 — 새 정적 파일을 추가하면 여기도 넣을 것.
- HTML `<head>`에 `rel="manifest"` · `apple-mobile-web-app-capable` · `viewport-fit=cover`.
  노치·홈 인디케이터는 `env(safe-area-inset-*)`로 피한다.
- 설정 서랍 맨 아래에 **설치 버튼/안내**. 안드로이드·크롬은 `beforeinstallprompt`를 잡아 버튼을 띄우고,
  iOS 사파리는 그 이벤트가 없으므로 "공유 → 홈 화면에 추가" 안내 문구를 대신 보여 준다.
- `sw.js`는 **네트워크 우선, 실패 시 캐시**. `install` 에서 `skipWaiting`, `activate` 에서 `clients.claim`.

### 새 판 자동 적용 (2026-08)

앱으로 깔면 페이지 이동이 없어서 **가만두면 몇 날이고 옛 판이 뜬다.** 그래서 스스로 감지해 새로고침한다.

- **감지** — 브라우저는 알아서 확인하지 않는다. `reg.update()` 를 **직접 찔러야** 한다:
  10분 간격 · 앱이 다시 보일 때(`visibilitychange`) · 온라인 복귀(`online`) · 로드 직후 한 번.
- ⚠⚠ **`register('sw.js', {updateViaCache:'none'})` 가 없으면 통째로 안 돈다.**
  이게 빠지면 브라우저가 **HTTP 캐시에 있는 옛 sw.js** 와 비교해서 바뀐 걸 못 알아챈다
  (실제로 이것 때문에 갱신이 한 번도 안 잡혔다).
- ⚠ **첫 설치와 갱신을 구분해야 한다.** 둘 다 새 워커가 `activated` 로 간다 —
  첫 설치에서 새로고침하면 앱을 켤 때마다 한 번씩 깜빡인다.
  구분은 `updatefound` **시점에 이미 컨트롤러가 있었나** 하나로 한다(`hadCtrl`).
- ⚠⚠ **게임은 전투 중에 새로고침하면 판이 통째로 날아간다.**
  `updateSafe()` 가 안전한 순간(흐름 페이지 위 · 아직 판이 없음 · `S.over`)만 허락하고,
  전투 중이면 위쪽에 알림 칩(`#newver`, 탭하면 즉시 적용)만 띄운 뒤 5초마다 다시 살펴 자동 적용한다.
  카드 뷰어는 잃을 상태가 없으므로 **묻지 않고 바로** 새로고침한다.
- ⚠ **`CACHE` 이름은 배포 때 자동으로 찍힌다.** `build_pages.py` 의 `stamp_sw()` 가 배포본 sw.js 의
  `CACHE` 뒤에 index.html 의 **내용 해시**를 붙인다(`ten-v27-h27e21087`).
  자동 갱신은 **sw.js 가 바이트 단위로 달라져야만** 도는데, 손으로 올리는 걸 한 번이라도 빠뜨리면
  앱이 옛 화면에 영영 갇힌다. 저장소의 sw.js 는 읽기 좋은 이름 그대로 둔다.
- 검사: `tools/test_update.js`(4건). 진짜 서비스워커가 필요해 **http 로 띄워** 검사한다.
- **카드 크기는 `dvh`에도 묶여 있다**: `--cardw: min(clamp(46px,19vw,86px), 8.2dvh)`.
  판 4줄 + 손패 2줄이 전부 이 값을 따라 커지므로, 주소창이 있는 브라우저(세로가 짧음)에서는
  자동으로 줄고 **앱으로 설치하면 주소창이 사라진 만큼 카드가 커진다.**
  `tools/test_mobile.js`가 앱(844)·브라우저(745)·작은 폰(640) 높이를 모두 검사한다.

## 모바일 세로 UI (2026-08) — 하스스톤/MTGA 배치

- **한 화면에서 끝난다. 이 레이아웃은 미디어 쿼리 없이 *모든 해상도*에 적용된다.**
  `.wrap`이 세로 플렉스가 되고 `body{overflow:hidden}`. **`vh`가 아니라 `dvh`** — 모바일 주소창이 접히면 `vh`는 어긋난다.
  ⚠ 예전에는 `@media (max-width:900px)` 안에 있었다. 그 바람에 **아이패드 가로(1194px)나 데스크톱에서는
  옛 세로 나열 레이아웃**이 나와 턴 종료 줄이 화면 밖 맨 아래로 밀리고 페이지가 스크롤됐다.
  카드 크기가 `vw`·`dvh`에 묶여 있으므로 넓은 화면에서도 그대로 통한다.
  - `@media (max-width:900px)` 블록에는 이제 **레이아웃이 아니라 글자 크기만** 남겼다(서랍·원정 화면).
  - `.wrap{max-width:min(100%,1180px);margin:0 auto}` — 넓은 화면에서 판이 한없이 늘어나지 않게 묶는다.
  - `@media (max-height:520px)`(가로로 눕힌 휴대폰): 고정 크롬(바 2줄 + 조작 줄 ≈107px)이 화면의 1/4을
    넘어 10dvh 를 쓰면 손패가 잘린다(390px 높이에서 24%만 보였다) → 크롬을 더 줄이고 카드는 8.4dvh 로.
    예전 `--cardw:56px` 고정값은 지웠다 — 세로 390px 에서 56px 은 오히려 넘친다.
  - `test_mobile.js`가 **세로 5종 + 가로/태블릿/데스크톱 4종**을 돌며 '조작 줄이 두 판 사이에 있는가'와
    '스크롤이 없는가'를 검사한다. 레이아웃을 건드렸으면 반드시 이걸 돌릴 것.
- **양쪽 손패는 화면 가장자리에 70%만 걸친다.** 하스스톤처럼 손패 일부를 화면 밖으로 밀어
  가운데 판에 세로를 몰아준다. `.hand{margin-bottom:calc(var(--cardw)*-0.42)}` /
  `.hand.foe{margin-top:calc(var(--cardw)*-0.42)}` — 카드 높이가 `1.4W`라 `0.42W`가 정확히 30%다.
  따라서 **손패가 화면 밑/위로 넘치는 것은 정상**이다. 회귀 검사는 "바닥 초과 여부"가 아니라
  **노출률(62~85%)** 을 본다(`test_mobile.js`).
- **손패 좌우에 여백을 준다**(`.hand{padding:4px 12px 0}`). ⚠ `fanHand`의 `avail` 계산에서
  **`clientWidth`는 padding을 포함**하므로 그냥 쓰면 이 여백이 무시되고 카드가 화면 끝까지 붙는다
  (실제로 "갑갑하다"는 지적을 받았다). `getComputedStyle`로 좌우 padding을 빼고 재야 한다.
- 배치는 위→아래: 상대 바 · 상대 지형 · 상대 보드 · **가운데 조작 줄(안내 + 턴 종료)** ·
  내 보드 · 내 지형 · 내 바 · 손패. DOM은 그대로 두고 **CSS `order`로 재배치**한다
  (그래서 라벨마다 id가 붙어 있다 — `#lbFoeLz`·`#lbFoeB`·`#lbMyB`·`#lbMyLz`·`#lbHand`).
- **남는 세로 여백은 라벨의 `margin-top:auto`로 고르게 나눈다.** 손패에만 몰아주면
  판과 손패 사이에 거대한 빈칸이 생긴다(실제로 그렇게 만들었다가 되돌림).
- **손패는 좌우로 곧게 나열하고, 넘치면 겹친다**(`fanHand(id, invert)`).
  기울이는 부채꼴로 만들었다가 되돌렸다 — 회전이 들어가면 폭 계산과 터치 판정이 전부 까다로워진다.
  - **겹침**: `.hand{gap:0}` + 카드마다 `margin-left:var(--hgap)`를 음수로. 가로 스크롤로 두면
    세로 화면에서 카드가 화면 밖에 숨는다. 한 장은 최소 26px(또는 폭의 34%)은 보이게 한다.
  - **넘침 보정**: 그래도 넘치면 `#hand`에 `scale()`을 건다.
  ⚠ 함정 둘, 둘 다 실제로 밟았다.
    · **측정 전에 지난 `scale`을 반드시 푼다.** 안 풀면 줄어든 폭 기준으로 또 줄여 손패가 계속 작아진다.
      카드 폭은 `offsetWidth`로 잰다(조상 transform의 영향을 안 받는다).
    · **측정 중에는 전환을 끈다**(`.hand.measuring .hcw{transition:none}`).
  ⚠ `transform`을 여러 CSS 규칙에서 덮어쓰지 말 것. `--rot/--dy/--sc` 변수로 두고 transform 선언은 한 줄뿐이다.
  ⚠ **겹친 손패에서 가운데 카드를 '가운데'로 집으면 오른쪽 이웃이 잡힌다.**
    테스트는 온전히 드러난 **맨 오른쪽 카드**로 하거나 왼쪽 노출부를 눌러야 한다(실제로 두 번 헛짚었다).
- **상대 손패도 내 카드와 완전히 같은 규격이다**(`--foecw: var(--cardw)`, 뒷면만 다르다).
  한 번 0.48배로 줄였다가 되돌렸다 — **화면을 덜 먹게 하는 건 크기가 아니라 노출 비율**이다.
  - 밀어 넣는 양은 변수 둘로 관리한다: `--myhide: -0.42×cardw`(내 손패 70% 노출),
    `--foehide: -0.76×cardw`(상대 손패 **50%** 노출). 카드 높이가 `1.4W`라 `0.7W`가 정확히 절반이고,
    `.main`의 위쪽 패딩 4px을 감안해 `0.76W`로 맞췄다.
  - ⚠ `.hand.foe`는 `.hand` 규칙도 함께 받는다. 내 손패용 `margin-bottom`이 새어 들어와
    **뒷면 카드가 상대 HP 바를 덮었다** — `.hand.foe`에서 `margin-bottom:0`으로 되돌릴 것.
- **판 라벨('내 보드'·'상대 지형' …)은 없다.** 위아래 배치만 봐도 자명하고 네 줄이 세로 예산을 먹었다.
  지형 정보는 지형존 **왼쪽 위 배지**(`.lzb`)로 옮겼다 — `남은 마나 / 배치한 지형 수`.
  - ⚠ 배지는 `packRow` **뒤에** 붙인다. `packRow`는 `:scope > .slot`만 건드리므로 서로 간섭하지 않는다.
  - ⚠ `.lbl`을 통째로 숨기지 말 것 — 설정 서랍('덱 메커니즘'·'전투 로그')도 같은 클래스다. `.main .lbl`로 한정한다.
- **게임 화면은 스크롤하지 않는다**(`.main{overflow:hidden}`). 해상도에 맞춰 `--cardw`가 줄어 한 화면에 들어간다.
  세로 예산 ≈ 판 4줄(5.6W) + 내 손패 노출분(0.98W) + 상대 손패 노출분(0.64W) ≈ **7.2W** + 바·조작줄 ~107px.
  라벨 4줄을 없애고 상대 손패를 절반까지 밀어 넣어 `8.2dvh → 10dvh`로 올렸다(카드 69 → 82px).
  ⚠ 더 올리면 320×568에서 판 사이 여백이 0이 되고 손패가 잘린다(11dvh에서 확인).
  ⚠ `overflow-y:auto`로 되돌리지 말 것 — iOS가 가로 팬까지 잡아채 손패 드래그를 취소시킨다.
- **글자는 전부 선택 불가, 화면 확대는 잠근다.** 게임 화면은 문서가 아니라 조작판이다.
  길게 눌러 카드를 확대하려다 글자가 파랗게 잡히거나 돋보기가 뜨던 문제.
  - `*{user-select:none}` — 요소를 하나씩 지정하면 새 UI에서 또 샌다(슬롯·손패만 막아 뒀다가 계속 샜다).
    `input/textarea/[contenteditable]`만 예외로 되살린다.
  - 뷰포트 메타에 **`maximum-scale=1`까지** 박는다. `user-scalable=no`만으로는 iOS 사파리가 무시한다.
  - `html,body{touch-action:pan-x pan-y}` — 스크롤은 남기고 핀치·더블탭 확대만 뺀다.
    `manipulation`은 더블탭만 막고 핀치는 남으므로 부족하다.
  - iOS용 `gesturestart/change/end` preventDefault + 두 손가락 `touchmove` 차단, `contextmenu` 차단.
  - ⚠ `touchend`로 더블탭을 막는 흔한 수법은 **쓰지 않는다.** 카드 탭 → 슬롯 탭처럼 빠른 연타가
    정상 조작이라 그 클릭까지 삼킨다.
- **상대가 낸 카드 공개(`S.reveal`)도 평소 카드를 그대로 키운 것**(`tcardHTML(name,{size:'lg'})`).
  전용 마크업 `.rcard`가 따로 있었는데 같은 카드가 두 모양으로 보여 혼란스러웠다 — 삭제했다.
  확대 UI와 폭이 정확히 같은지 `test_pwa.js`가 검사한다.
  ⚠ 이 연출은 `rotateY`로 뒤집히며 들어오므로 **`getBoundingClientRect`로 재면 폭이 0에 가깝게 나온다.**
  `offsetWidth`로 잴 것.
  ⚠⚠ **판 위 레이아웃 검사도 전부 `offsetWidth`/`offsetLeft` 로 잰다.** 갓 놓인 슬롯에는
  `.spawned` 등장 애니메이션(scale)이, 변신(성장·진형)한 슬롯에는 flip 이 걸려 있어서
  `getBoundingClientRect` 는 **변형 중인 폭**을 돌려준다. `test_board.js` 가 82px 여야 할 카드를
  57px 로 재고 터졌다 — 순서만 바꿔도 통과했다 사라졌다 하는, 제일 헷갈리는 종류의 실패다.
  덧붙여 `render()` 는 슬롯 노드를 **재사용**하므로, 검사에서 판을 비울 땐 `S.*.board=[]` 만으로
  부족하고 `#myBoard`/`#aiBoard`/`#myLands`/`#aiLands` 의 `innerHTML` 도 비워야 애니메이션이
  다음 검사로 새지 않는다.
- **손패 카드에 `opacity`를 쓰지 않는다.** 겹쳐 있어서 반투명이면 **밑장이 비쳐 보인다**.
  낼 수 없는 카드는 `filter:grayscale+brightness`로 어둡게, 끌고 있는 원본은 `opacity:0`으로 아예 감춘다.
- 오른쪽 카드가 왼쪽을 덮는다(`z-index=10+i`). **손패 카드는 눌러도 제자리에 그대로 있어야 한다.**
  z-index를 올리는 것은 물론이고 **들어올리거나(`--dy`) 키우는 것(`--sc`)도 안 된다** —
  옆 카드에 가려져 있던 부분이 위로 삐져나와 "갑자기 앞으로 튀어나온" 것처럼 보인다.
  강조는 **크기를 바꾸지 않는 테두리 광채**(box-shadow)로만 한다. hover·선택·확대 전부 같은 규칙.
  ⚠ 데스크톱 브라우저에서는 `:hover`가 함께 걸려 이 문제가 가려진다. **터치 전용 컨텍스트로 확인할 것**
    (`tools/test_zoom.js`의 '눌러도 제자리' 항목).
- **확대는 손패·보드 모두 롱프레스(420ms)로 열리고, 그 다음엔 팝업으로 남는다**(`showZoom(...,hold=false)`).
  화면 아무 곳이나 누르면 닫힌다. 예전에는 손패만 "누르고 있는 동안"이었는데(`hold=true`),
  손을 떼면 사라져 카드를 읽을 시간이 없었다 — 요청으로 팝업으로 바꿨다.
  - ⚠ **끌기 탈출구를 반드시 남길 것.** 팝업으로 두면 "카드 끌기가 갑자기 안 된다"가 재발한다.
    `pointermove`에서 `lpDown && 12px 이상 이동 && 확대 열림` 이면 확대를 접고 드래그로 넘긴다.
    (`lpDown`은 pointerdown에서 켜고 pointerup/cancel에서 끈다. 예전 `lpHold` 대신 이걸 본다.)
  - ⚠ 손을 뗄 때 따라오는 `click`을 **직접 막아야 한다.** 예전에는 `hideZoom(swallow)`가
    해 줬는데 이제 뗄 때 닫지 않으므로, pointerup에서 `if(lpDown && 확대 열림) lpFired=true`.
    안 하면 길게 눌러 확대만 하려던 것이 카드 선택으로 새어 나간다.
  - `.zoom.hold{pointer-events:none}` 규칙은 남겨 뒀지만 이제 붙는 곳이 없다.
- **용어(태그) 설명은 모바일에서 카드 *아래*에 쌓는다.** 예전에는 카드를 화면 정중앙에 절대 배치하고
  설명을 화면 하단에 `position:fixed`로 붙였다 — 둘이 서로 모르는 사이라 **설명이 카드를 덮었다.**
  이제 `.zwrap`을 세로 플렉스 한 덩어리로 두고 카드 위·설명 아래로 쌓는다.
  - **태그는 3개까지를 전제**로 크기를 잡았다(`.zoom .tcard.lg{--cw:min(66vw,250px,34dvh)}`).
    그 이상은 카드 설계 단계에서 지양할 것. 안전망으로 `.zoom{overflow-y:auto}` + `.zwrap{margin-block:auto}` —
    들어가면 가운데, 안 들어가면 위부터 스크롤된다(잘려 안 보이는 것보다 낫다).
  - ⚠ **뒤에 오는 규칙에 진다.** 기본 `.tcard.lg{--cw:min(72vw,260px)}` 와
    `@media(max-width:640px){.zside{max-height:32vh}}` 가 모바일 블록보다 **아래**에 있어
    확대 크기가 안 먹고 좁은 폰에서 세 번째 태그가 통째로 잘렸다.
    확대 레이아웃은 `@media(max-width:900px)` **한 곳에서만** 정하고, 선택자를
    `.zoom .tcard.lg,.reveal .tcard.lg` 로 한 단계 올려 이기게 했다.
- 드래그 `pointerdown` 핸들러에서 **확대 여부로 막지 않는다**. 대신 `dragStart`가 `hideZoom(false)`를 부른다.
- **드래그 중에는 손패 DOM을 다시 만들지 않는다**(`HANDLOCK`). 터치에서는 `pointerdown` 대상에
  **암묵적 포인터 캡처**가 걸리는데, 그 노드를 지우면 브라우저가 `pointercancel`을 쏘고
  이후 `pointermove`가 끊긴다 → 드래그가 통째로 죽는다.
  **마우스에는 암묵적 캡처가 없어서 데스크톱 테스트로는 절대 안 잡힌다.**
  보루로 드래그 시작 시 `document.body.setPointerCapture(pointerId)`도 건다(끝나면 release).
- **낼 수 없는 카드도 끌린다.** 막아 두면 아무 반응이 없어 "드래그가 안 된다"로 읽힌다.
  끌 때 안내가 붉게 바뀌고(`.dztip.bad`), 놓으면 이유를 토스트로 알려 준다.
- ⚠⚠ **가로 스크롤이 생기면 iOS가 드래그를 통째로 가져간다.** 증상은
  "끌려고 하면 화면이 오른쪽 끝으로 밀리고 드래그가 취소됨". 원인 두 개가 겹친다.
  1. `.hcw{touch-action:pan-x}` — "가로 팬은 브라우저가 가져가도 좋다"는 선언이다. **`none`으로 둘 것.**
     손패는 부채꼴로 화면에 맞추므로 가로 스크롤이 필요 없다.
  2. `.main{overflow-y:auto}`만 쓰면 **CSS 규칙상 `overflow-x`가 `visible`→`auto`로 계산된다.**
     부채꼴 카드가 1px만 넘쳐도 가로로 스크롤 가능해진다. **`overflow-x:hidden`을 명시할 것.**
- 드래그 중에는 `resize`에도 `fanHand()`를 돌리지 않는다 — iOS는 주소창이 접힐 때 resize를 쏘는데,
  그때 손패를 다시 배치하면 잡고 있던 카드가 어긋난다.
- 주소 끝에 **`?dbg=1`** 을 붙이면 포인터 동작(어느 카드를 잡았는지·어디서 끊겼는지)이 전투 로그에 남는다.
- **상대 손패는 맨 위에 뒷면(`.cback`)으로 보여 준다.** 내 손패와 **같은 규격**(`--cardw`)이고
  매수만 드러내며 `pointer-events:none`.
  같은 `fanHand(id, invert)`를 쓴다(`invert`는 축소 기준점만 위쪽으로 바꾼다).
  ⚠ 상대 손패도 `.hcw`를 쓰므로 **선택자는 `#hand .hcw`로 한정할 것.**
  `.hcw:last-child` 같은 전역 선택자는 DOM 순서상 **상대 카드부터** 잡는다(테스트가 통째로 깨졌다).
- 확대 카드는 모바일에서 **화면 정중앙 고정** — 용어집 패널(`.zside`)을 `position:fixed`로 빼서
  카드를 밀어 올리지 않게 했다.
- 손패 카드 폭은 `--handcw`(`clamp(78px,24vw,112px)`) 하나로 조절한다.
- **헤더는 없다(2026-08).** 화면 위에 **톱니(⚙) 하나만 떠 있고**(`position:fixed`, 자리를 차지하지 않는다),
  제목·턴 표시·모드 전환(배틀/원정)·덱 선택·덱 메커니즘·전투 로그·속도·새 게임이 **전부 설정 서랍**에 들어 있다.
  서랍은 모든 화면 크기에서 서랍이다(데스크톱은 오른쪽, 모바일은 아래에서). 여는 것은 톱니뿐.
  헤더를 걷어내 확보한 세로 공간은 `--cardw`에 돌려서 카드가 커졌다(390px 기준 72 → 82).
  ⚠ **닫힌 서랍은 화면 밖으로 밀어 둔 `position:fixed` 요소라 문서 크기를 늘린다.**
    `body{overflow:hidden}`은 fixed 요소를 자르지 못한다 — **`html`에서 잘라야** 한다.
  ⚠ 옛 규칙 `@media(max-width:900px){.side{position:static}}`이 남아 서랍이 모바일에서 본문에 끼어들었고,
    그 바람에 `.main`이 절반 높이로 줄어 **손패가 잘려 눌리지 않았다.** 레이아웃을 바꿀 땐 옛 미디어 규칙을 먼저 훑을 것.
- 마나 pip은 모바일에서 감춘다(지형존이 같은 정보를 더 잘 보여준다).
- 지도 노드는 적 이름이 길어 넘치므로 **높이를 내용에 맞춘다**(`height:auto;min-height`).
- **검증**: `tools/test_mobile.js`(4해상도 레이아웃) · `tools/test_zoom.js`(확대 8건) ·
  **`tools/test_touch.js`(진짜 터치 입력, CDP `Input.dispatchTouchEvent`)**.
  ⚠ Playwright의 `mouse` API는 `pointerType=mouse`라 `:hover`가 걸린다 —
  **휴대폰에서만 나는 증상은 `test_touch.js`에서만 잡힌다.** 손패 조작을 고쳤으면 이걸 꼭 돌릴 것.

## 원정(로그라이크) — 적 명단과 적별 덱 (2026-08 신설)

**"적이 다짜고짜 나온다"는 문제**를 구조로 풀었다. 이제 적은 이름·성향·체력을 가진 개체다.

- **명단**: `tools/gen_enemies.py` → `data/enemies.json` → `build_proto_data.py`가 `ENEMY`로 주입.
  속성 7종 × (**일반 3 · 정예 1 · 보스 1**) = **35명**. 내 속성의 적은 후보에서 빠진다(속성 불혼합 규칙).
- **3축 구조**: `등급(일반/정예/보스) × 아키타입 × 난이도 단계`.
  - 아키타입 6종(속공·군세·주문·방벽·거물·보스)이 **코스트 곡선 + 카드 선호도**를 정한다.
  - **난이도 단계 3개**(층 0~2 / 3~6 / 7~)마다 덱이 따로 있다. 단계가 오르면 곡선이 무거워지고
    강화 카드가 늘어난다(일반 0/2/4 · 정예 4/6/8 · 보스 8/9/10장).
  - ⚠ 곡선을 무겁게 만들 때 **1코부터 빼서 6코에 몰아주면 아키타입이 뭉개진다**
    (속공 덱이 1코 0장·6코 4장이 되는 식). `shift_curve`는 원래 비중에 비례해 옮긴다.
- **적 체력이 난이도를 따라 오른다**: 기본 일반 **20** · 정예 **30** · 보스 **60**, 층당 가산(1.5/2/2).
  플레이어 HP는 원정 내내 이어지고(`RG.hp`) 적만 매판 새로 채워진다 — 슬레이 더 스파이어 방식이다.
  이 때문에 `S[p].maxhp`가 진영별로 따로 있어야 한다(체력 바·회복 상한이 전부 이걸 본다).
- **지도에 적이 보인다**: 노드마다 이름·성향·체력을 찍는다. 경로 선택이 정보 있는 선택이 됐다.
- **조우 화면**(`rgEncounter`): 초상화(덱의 최고 코스트 **크리처** 아트) · 이름 · 속성·성향 ·
  체력 · 난이도 별 · 강화 매수 · 한 줄 소개 · 주력 카드 4장. `전투 시작`을 눌러야 그 칸에 진입한다
  (누르기 전에 지도로 돌아가면 이동하지 않은 것 — `rgEnter`는 층을 확정하지 않는다).
- 같은 얼굴이 연달아 나오지 않게 최근 사용 적을 잠시 후보에서 뺀다(`FOEUSED`).
- **검증**: `tools/test_enemies.js`. 전투 길이 실측은 `tools/sim_expedition.js`(느림 — 케이스를 좁혀 돌릴 것).

## 크리처 키워드 엔진 (전수검사 후 구현 완료 · 2026-08)

**전수검사 결과**: 이전에는 13종 중 **대가·축복 2종만** 실제로 작동했다. 나머지 11종과 고유 문구 4종,
그리고 보드 위 토큰 표시는 전부 죽은 데이터였다(카드에 글자만 찍히고 아무 일도 안 일어남).

- **유일한 출처는 `POOL[n].kw` 문자열**이고, 프로토타입의 파서가 곧 규칙 구현이다.
  원본 표는 `tools/gen_decks.py`의 `KW`. **문자열 형식을 바꾸면 파서도 같이 고쳐야 한다** —
  실제로 `진형 +4`의 `+` 때문에 `kwNum`이 조용히 0을 반환하고 있었다. 숫자 앞 부호를 항상 허용할 것.
- 유닛 생성 시 키워드를 상태로 굳힌다(`mkUnit`): `burn·burst·hard·drain·form·echo·breed·hone·grow·ward`.
- **개체(inst)는 `{hp, mh, ward}`** — `mh`(최대 체력)가 있어야 가호 부활과 성장/연마 누적이 맞는다.
  체력을 영구히 올리는 곳은 **`mh`도 같이 올려야 한다**(성장·연마·버프·숲의 여왕).
- **모든 피해는 `hurt()`/`hurtAll()`을 지난다** — 경화를 한 곳에서만 적용하기 위해서다.
  새 피해원을 추가하면서 `inst.hp -= v`를 직접 쓰면 경화가 조용히 무시된다.
- **사망 판정은 `cleanup()` 한 곳** — 가호(파괴 무효 1회) → 환류(손으로) → 폭산(적 전체) → 불사조(잿불 토큰)
  순으로 걸려 있다. 폭산이 서로 물릴 수 있어 `DEATHD` 깊이 제한(4)을 둔다.
- **턴 종료 훅 `endStep(p)`** — 성장 → 연마 → 증식 → **연소 감소는 맨 마지막**.
  연소는 가호로 못 막는다(스스로 타 없어지는 것이라 `ward`를 꺼 준다).
- **증식은 중첩이 아니라 왼쪽 빈 슬롯에 새 개체를 세운다**(2026-08 변경).
  **복제본은 다시 증식하지 않는다**(`breed=false, bred=true`) — 안 막으면 1→2→4→8로 기하급수가 되어
  ×0.5 배수로는 감당이 안 된다. 지금은 턴당 1개체씩 선형(2→3→4→5)이라 중첩 시절 템포와 같고,
  대신 **슬롯을 먹으므로 보드 만석이 자연스러운 상한**이 된다(만석이면 그 턴은 건너뛴다).
- ~~진형은 소환 시점의 슬롯으로 결정된다(0-based `idx<3`)~~ — **폐기.** 지금은 자리와 무관하게
  **혼자 남으면 각성**한다. ⚠ 이 낡은 설명을 보고 쓴 `test_board.js` 의 '진형 재계산' 이
  오래 빨간불이었다 — 규칙을 바꿀 땐 문서의 옛 서술도 같이 지울 것.
- 고유 문구 4종은 `ONSUMMON` 표 + `onDeath`의 이름 분기: 조수술사·숲의 여왕·지진술사·불사조.
- **토큰은 POOL에 없어서 보드에서 빈 칸으로 그려지고 있었다** → `tcardHTML`에 토큰 분기 추가.
- 진행 상태는 슬롯 위 뱃지(`kwBadges`)로 보여준다. 일러스트 오른쪽 세로 정렬 —
  아래쪽에 두면 공/체 뱃지와 겹친다.
- **검증**: `tools/test_keywords.js`(키워드 19건) · `test_nostack.js`(중첩 폐기 8건) ·
  `test_drag.js`(드래그) · `test_targeting.js`(타게팅 7건) · `test_battle.js`(7덱 전투 회귀) ·
  `test_ench.js`(인챈트 17건) · `test_goblin.js`(14건) · `test_burn.js`(24건) ·
  `test_arson.js`(방화범·화염술사 2·3단계 22건) · `test_swipe.js`(확대창 겹쳐 보기 10건) ·
  `test_grok.js`(그록 램프·깎이는 비용·개전 20건) · `test_ignis.js`(보스 2막 13건).
  전부 Playwright headless — `node tools/test_*.js`.

## 조작 — 드래그 앤 드롭 (구현 완료)

- **손패 밖으로 끌어내면 발동한다.** 드롭 지점을 정확히 맞출 필요가 없다 —
  판정은 "손패 사각형 밖인가" 하나뿐이고, 자리는 코드가 정한다.
- **소환·배치는 슬롯을 고르지 않는다** — `leftFree()`로 **왼쪽 빈 칸부터** 채운다.
  보드 오른쪽 끝에 떨궈도 0번에 들어간다. 유효 슬롯 위에 떨궈도 무시한다(의도적).
  자리를 직접 지정하고 싶으면 **기존 탭 흐름**(카드 탭 → 슬롯 탭)이 그대로 남아 있다.
### 딤 규칙 — 뒤 조작을 막는 오버레이는 전부 같은 배경을 쓴다

- **토큰은 `--dim: rgba(6,4,2,.66)` / `--dim-blur: 3px` 둘뿐이다.** 개별 규칙에 `rgba`를 직접 적지 않는다.
  공용 선택자 `.dim,.tgtdim,.zoom,.mull,.reveal`이 배경과 `backdrop-filter`를 함께 건다.
  `body.sideon::after`(설정 뒤)는 의사요소라 클래스를 못 붙이니 변수를 직접 쓴다.
  적용 대상: 대상 지정 딤 · 카드 확대 · 시작 손패(멀리건) · 원정 지도 · 상대 카드 공개 · 설정 뒤.
  **새 팝업을 만들면 여기에 붙일 것.** `test_ui.js`의 '딤 규칙 공통'이 값이 어긋나면 잡는다.
- **예외 둘.** `.veil`(페이지 전환 커튼)은 뒤를 흐리게 보여 주는 게 아니라 **아예 가리는** 장치라
  더 진하다(`rgba(9,6,3,.96)`/blur 6px). `.busy`(안 가리는 로딩)는 **배경이 없어야** 한다 —
  화면은 그대로 보이고 입력만 막는 게 존재 이유다. 여기에 딤을 붙이면 그 장치가 죽는다.
- 딤을 덮으면 **지금 무엇을 해야 하는지 읽을 수 없게 된다.** 대상 지정 중에는 가운데 안내 줄
  (`body.tgtmode .ctl{z-index:41}`)을 딤 위로 올린다. 조작은 어차피 capture 단계에서 가로채므로 안전하다.

- 대상 지정 중에는 **화면 전체를 딤(`#tgtdim`)으로 덮고 고를 수 있는 슬롯만 그 위로 올린다**(`body.tgtmode`).
  대상 카드는 판 한가운데에 '나간 것처럼' 세운다. 빈 곳을 누르면 손으로 돌아온다.
  함정이 셋이고 전부 실제로 밟았다.
  - ⚠ **딤은 `.wrap` 안에 둔다.** `.wrap{z-index:1}`이 쌓임 맥락을 만들기 때문에 밖에 두면
    안쪽의 `.slot.pick`을 아무리 올려도 딤 위로 못 올라온다(`position:fixed`라도 조상 맥락에 갇힌다).
  - ⚠ **`z-index:41 !important`가 필요하다.** `packRow`가 겹침 순서를 위해 슬롯마다 z-index를
    **인라인**으로 박는다(10+i). 스타일시트 선언은 인라인에 지므로 `!important` 없이는 무시된다.
  - ⚠ 예전처럼 슬롯마다 `filter`를 걸지 않는다 — `filter`는 그 자체로 쌓임 맥락을 만들어 딤 계산을 망친다.
  - 검사는 `test_pwa.js`의 '딤이 화면을 덮는다'. **`elementFromPoint`로 실제 픽셀을 짚는다** —
    z-index 값만 봐서는 맥락에 갇혔는지 알 수 없다. 딤은 `pointer-events:none`이라 **재는 동안만 켜야** 잡힌다.
  - 관련: `.dropbar`는 순수 장식인데 `opacity:0`이어도 히트 테스트에 걸려 딤 검사를 흐렸다 →
    `pointer-events:none`을 붙였다. 보이지 않는 장식은 전부 이걸 붙일 것.
- **대상이 필요한 스펠은 하스스톤 방식 타게팅으로 간다**(`mode==='target'||'mine'`).
  대상 위에 바로 떨구면 그대로 발동하고, 빈 곳에 떨구면 `startTargeting()` —
  카드가 손패 위에 서고 손끝까지 곡선 화살표가 이어진다. 유효 대상을 탭하면 발동, 딴 곳을 탭하면 취소(카드는 손에 남는다).
  `autoFoe()`/`autoMine()`은 이제 **대상이 하나라도 있는지 판정**하는 데만 쓴다(없으면 진입조차 안 함).
- 타게팅 중에는 `pointerdown`을 **capture 단계에서 가로챈다** — 슬롯 기본 클릭과 이중 발동하지 않도록.
  `취소` 버튼·새 게임·다음 드래그 시작에서 `endTargeting()`을 반드시 호출할 것(안 하면 SVG가 화면에 남는다).
- **화살표 SVG는 대체 요소라 `inset:0`만으로는 300×150 고유 크기로 남는다** — `width:100vw;height:100vh`를 명시할 것.
  이것 때문에 화살표가 화면 왼쪽 위 구석에서 잘려 안 보였다.
- 제스처 분기: **가로 우세 = 손패 스크롤 · 세로(위) 14px = 드래그**. `.hcw{touch-action:pan-x}`가 있어야
  세로 스와이프가 페이지 스크롤로 새지 않는다. 롱프레스 확대(420ms)는 드래그 시작 시 취소된다.
- 드래그 중에는 `S.sel`/`S.mode`를 미리 세워 두어 **기존 `.pick` 슬롯 하이라이트를 그대로 재사용**한다.
  놓으면 `playFromHand()` → `clickSlot()`으로 합류하므로 지불·로그·연출 경로가 하나다.
- **`.ghost`는 이미 취소/속도/새 게임 버튼이 쓰는 클래스다.** 드래그 고스트는 `.dgh`.
  여기서 이름이 겹치면 버튼이 `position:fixed`가 되어 화면이 무너진다 — 실제로 한 번 겪었다.

### 물 덱 개편 1단계 — 새 지형 장치 (2026-08)

| 지형 | 종류 | 하는 일 |
|---|---|---|
| 파도 지대 · 폭포 · 설원 지대 | 기본 | 물 자원 1. 심해와 같은 규격(이름만 다르다) |
| 호수 유적 | 특수 · 레어 | 자원 0 · **내 턴이 끝날 때** 무작위 아군 HP +1 |
| 바닷속 풍경 | 특수 · 레어 | 자원 0 · **내 턴이 끝날 때** 카드 1장 |
| 인어 기둥 | **추가** · 레어 | 자원 2를 내고 놓는다. **턴당 지형 1장 제한을 안 먹는다** |
| 수정구 | **소멸** | 스펠이 만들어 내는 임시 지형. 다음 내 턴에 한 번 쓰고 사라진다 |

- **`putLand(p,name,opt)`** — 지형을 판에 올리는 단 하나의 통로. 손패에서 내는 것도,
  스펠이 만들어 내는 것도 여기를 지난다(`n`·`temp` 같은 지형 속성이 한 곳에서 실린다).
- **추가 지형(`extra`)** — `playLand` 가 `landPlayed` 대신 **자원**을 본다. `payMana(p,n)` 은
  `pay()` 와 같은 '남은 몫에 딱 들어가는 것 중 가장 큰 것' 규칙을 쓴다.
  ⚠ 손패 흐리기(`whyNotPlayable`)와 드롭 판정이 같은 함수를 보게 고쳤다 — 예전에는 드롭 쪽이
  `landPlayed` 만 따로 보고 있어서 추가 지형이 손에서 흐려진 채로 안 나갔다.
- **소멸 지형(`temp`)** — `startTurn` 에서 수명이 하나씩 준다. ⚠ used 를 푸는 코드보다 **뒤**에
  둔다(사라질 지형을 굳이 되돌릴 필요가 없다).

### 불 덱 개편 1단계 — 지형 (2026-08)

| 지형 | 종류 | 하는 일 |
|---|---|---|
| 불지옥 · 용암 폭포 · 용암 동굴 | 기본 | 불 자원 1. 화산과 같은 규격(이름만 다르다) |
| 불지옥의 계곡 | 특수 · 레어 | 자원 0 · **내 턴이 끝날 때** 파이어버그 소환 |
| 지하 감옥 | **추가** · 레어 | 불 자원 1. 자원 2를 내고 놓고, 턴당 1장 제한을 안 먹는다 |

- **파이어버그 = 1코 3/1 폭발 · 매수 0.** 고블린 요새(턴 **시작**)의 짝이지만 계곡은 턴
  **종료**에 뽑는다 — 상대 턴을 넘겨 살아남으므로 먼저 벽으로 쓰인다. 그만큼 몸을 세웠고,
  대신 연소가 없어 스스로는 안 죽고 체력 1 이라 뭐든 한 대 맞으면 사라진다.
  ⚠ `landEnd` 는 `resolveAttacks` **뒤**에 돈다 — 그래서 나온 턴에는 공격하지 않는다.
- **⚠⚠ `LANDBORN` — 지형이 낳는 몸은 예산 검산에서 뺀다.** FOEONLY 와 이유가 다르다.
  균형을 안 맞춘 게 아니라 **값을 이미 지형이 냈는데 검산표가 그걸 못 본다**: 계곡은 자원을
  하나도 안 만드니 깔린 내내 실질 마나 상한이 1 낮고, 환산표로 1마나 = 예산 4 가 영구히 나간다.
  그런데 지형은 `lands.csv` 에 있어서 크리처 예산표에 안 잡힌다 → 몸만 보면 늘 '초과'.
  (파이어버그는 1코 예산 1 에 소모 7 로 찍혔다. 폭발 가감이 `-atk` 라 ATK 를 세울수록 깎인다.)
  검산 라벨은 '지형산물' 로 따로 찍는다. 빠지는 이름 전부 = `NOBUDGET = FOEONLY | LANDBORN`.
  ⚠ **스펠로도 뽑히는 몸은 넣지 말 것** — 고블린 폭탄병은 고블린의 열의(2코)가 `CRP` 로 값을
  매겨 부르므로 검산이 살아 있어야 한다.
- **⚠⚠ 보상 지형이 `RICHLAND={fire:'화염의 원천'}` 하드코딩이었다.** 속성당 딱 하나만 손으로
  적어 두는 구조라, 물 개편에서 넣은 호수 유적·바닷속 풍경·인어 기둥이 **영영 안 나오는 죽은
  카드**였다(만들어 놓고 아무도 못 얻는 상태). `landOffers(el)` 로 갈았다 — `lands.csv` 가 정본이고
  **희귀도가 붙은 지형(rich·extra·special)** 이 곧 보상 후보다. 기본 지형은 뽑아 봐야 화산과
  똑같으니 뺀다(함정 선택지가 된다). 소멸 지형도 뺀다.
  - 곁들여 **`rarOf` 가 `LANDS` 도 보게 했다.** POOL 만 보던 탓에 지형이 전부 common 으로 잡혀
    희귀도 가중 뽑기가 헛돌고 있었다(레전더리 화염의 원천이 커먼 취급).
  - 새 지형을 추가할 때 코드는 건드릴 필요가 없다. `lands.csv` 에 줄만 넣으면 보상에 뜬다.
- 시작 덱은 그대로 두기로 했다(불 = 화산×17 · 물 = 심해×17). 새 지형은 **원정 보상에만** 나온다.
- 검사: `test_lands.js` 의 뒤쪽 15건(기본 3종 자원 · 계곡 소환/만석 · 지하 감옥 추가 배치·자원
  지불·자원 부족 · 보상 후보 목록 · 기본/소멸 제외 · 지형 희귀도).
- **`landEnd(p)`** — `landStart`(턴 시작)의 짝. `endTurn` 에서 인챈트보다 **먼저** 돈다.
- **면역(`veil`)** — HP 가 **아예** 줄지 않는다. 가호(첫 피해 1회)와 달리 끝이 없다.
  ⚠⚠ `hurt`·`hurtRaw`·**`hurtAll`** 셋 다 막아야 한다. hurtAll 을 빼먹으면 광역·연소가 그대로 뚫는다.
  ⚠ **수호와 양립하지 않는다** — `grantVeil()` 이 면역을 줄 때 수호를 벗긴다(둘 다면 못 뚫는 벽).
- 검사: `tools/test_water.js`(9건).

### 물 덱 개편 2단계 — 크리처 27종 전면 교체 (2026-08)

옛 11종(여울정령·파도술사·산호방벽·조수술사·해무령·해류지기·심해수호·심연룡·만조의 수호자·
소용돌이 정령·해신)을 **통째로 지우고** 27종으로 갈았다. 덱에 실리는 11종만 매수를 주고
나머지는 매수 0(카드 풀)이다. 시 서펜트는 7코라 **공식 커브를 벗어나므로 덱에는 안 싣는다.**

덱 11종 — 전기 해파리×2 · 닉시×2 · 워터리퍼 · 세이렌 · 인어 전사 · 인어 마술사 ·
물의 정령 · 켈피 · **심해 거인×2 · 해룡 · 대왕 문어**.

**⚠ 시작 덱 크리처는 커먼만 싣는다.** 그런데 물은 5·6코에 커먼 크리처가 하나도 없었다
(스킬라 레어 · 폭풍의 정령 언커먼 · 크라켄 레어) — 공식 커브를 채울 몸이 없어서 셋을 새로 뒀다:
심해 거인(5코 수호 4/9) · 해룡(5코 비행 5/4) · 대왕 문어(6코 일반 7/7 환류).
셋 다 순수 골격용이라 이름·컨셉은 언제든 갈아도 된다. 밀려난 스킬라·폭풍의 정령·크라켄은
**매수 0**(카드 풀·원정 보상)으로 남았다. `test_water.js` 의 `시작 덱 크리처는 커먼만` 이
`data/decks.json` 을 읽어 크리처에 `r` 필드가 없는지 검사한다 — 다른 속성도 커먼화할 때
같은 검사를 붙일 것.

| 카드 | 하는 일 | 엔진 |
|---|---|---|
| 세이렌 | **양쪽 스펠 모두**의 피해를 안 받는다 | `spellimm`(수호병의 `spellproof` 는 '내 스펠' 만) |
| 리자드 전사 | **연합** — 동명 아군이 모두 한 번씩 따라 친다 | `ally` · `strike()` + `resolveAttacks` 의 랠리 루프 |
| 상어 인간 | 상대에 수호가 있으면 낸 값을 **그 자리에서** 환급 | `refundMana` |
| 리자드 마술사 | 주문마다 **뒤집힌 지형 1장** 되돌림 | `unflipLands`(왼쪽부터) |
| 인어 마술사 | 주문마다 +1/+1 | `spellup` (화염 아귀의 `spellhp` 와 짝) |
| 운디네 | 소환 시 **덱에서 지형 1장** | `onSummon` |
| 마인드 플레어 | 대상을 **인쇄된 몸**으로 되돌린다 | `mindWipe` |
| 스킬라 | 소환 시 여왕의 가신 둘을 **양옆에** | `onSummon` |
| 크라켄 | 제 값보다 싼 몸을 **양쪽 다** 손으로 | `toHand` |
| 시 서펜트 | 내 턴 종료마다 **제일 싼** 상대를 손으로 | `reaper` · `endStep` |

- **환류를 코스트 비례로 바꿨다** — 고정 -4 였는데 그러면 **1코 카드의 예산이 통째로 사라진다**
  (cbase(1)=4). 되돌아오는 값은 제 카드값에 비례하므로 `-round(cbase*0.35)` 로 갔다.
  전액이 아닌 것은 한 턴 늦고 마나를 다시 내기 때문이다.
- **⚠⚠ 토큰을 없애고 소환체를 진짜 카드로 바꿨다.** 크라켄·정신분열 같은 바운스가 토큰에는
  안 통했기 때문이다(POOL 에 없는 이름을 손에 넣으면 **영영 못 내는 유령 카드**가 된다).
  소환 스펠의 규칙문이 이제 **카드 이름**을 가리키고, `parseSummon` 이 그 이름을 POOL 에서 찾는다.
  신설한 소환체: 불꽃 병사(불) · 새싹(자연) · 조립 병기(강철) · 부름의 망령·망자(어둠) ·
  여왕의 가신·물거품(물). 전부 매수 0.
  ⚠ 이름이 문장 **중간**에 있을 수 있다("내 HP 8 지불 → 부름의 망령 2개체"). 앞머리까지 통째로
  잡히므로 **뒤에서부터 낱말을 붙여** POOL 에 있는 이름을 찾는다(최대 세 낱말).
  ⚠ 부여 조항(`· 연소 1 부여`)을 **진짜 카드 쪽에도** 심어야 한다 — 토큰만 받던 시절의 흔적을
  빠뜨려 정령의 불꽃의 용암 정령이 연소를 못 받고 안 죽었다(검사에서 잡혔다).
- **`toHand(side,idx)`** — 손으로 되돌리는 단 하나의 통로. POOL 에 없는 이름은 그냥 사라진다.
- **연합 = 랠리(따라 공격)** — "동명 크리처 수만큼 연타" 였는데 **동명 아군이 각자 한 번씩 따라
  친다** 로 바꿨다. 그래서 `resolveAttacks` 에서 **공격 한 번**을 `strike(p,u,gen,tag)` 로 떼어
  냈고, 연합 개체가 제 공격을 마치면 같은 이름의 아군을 훑어 `strike` 를 다시 부른다.
  ⚠ `strike` 는 **슬롯 번호가 아니라 개체(unit)** 를 받는다 — 따라 치는 사이에 누가 죽으면
  슬롯이 당겨져서 번호가 어긋난다. 따라 친 공격은 다시 연합을 부르지 않는다(무한 연쇄 방지).
  검사값: 리자드 전사 셋 = 27 · 둘 = 12 · 혼자 = 3 · 전사2+오아네스 = 16.
- **마인드 플레어의 대상 지정은 양쪽 판**이다 — `S.mind={p,list:[{side,idx}]}`.
  밀물(`S.tide`)보다 **먼저** 잡고, 고르기 전에는 턴을 못 넘긴다. 하이라이트도 양쪽을 비춘다.
- 검사: `tools/test_water.js`(23건).

### 어둠 개편 1단계 — 카드 49종 · 지형 6종 전면 교체 (2026-08)

옛 13종 크리처(망령·피의광신도·흑기사·흡혈귀·그림자 습격자·시체 수확자·피의 군주·어둠의 수호·
심연의 사제·파괴자·부름의 망령·망자)와 스펠 7종·인챈트 2종을 **통째로 지웠다.**
지운 카드를 검사가 붙들고 있어 `test_ench`(흡혈 의식·피의 성배·피의 못·금단의 지식·망령·심연)와
`test_water`(부름의 망령·망자)가 함께 깨졌다 — **카드를 지우면 검사부터 훑는다.**

- 크리처 26 + 토큰 3 · 스펠 15 + 토큰 4 · 인챈트 4. 시작 덱은 **20종 23장, 전부 커먼**,
  커브 1코 4 · 2코 7 · 3코 7 · 4코 5(`CURVE_OVERRIDE["dark"]`) — 불·물과 같은 꼴이다.
- **공/체는 성권이 안 줬다.** 예산 공식으로 채우고 `docs/dark_report.md` 로 표를 올려 승인받았다.
  카드마다 소모/예산이 표에 남아 있다 — 나중에 값을 만질 때 그 표부터 볼 것.

#### 새 낱말 둘 — **단말마**(소멸할 때) · **출진**(소환할 때)

- ⚠⚠ **`KW` 표에 안 넣었다.** 붙는 효과가 카드마다 달라 표에 고정값을 적을 수가 없다.
  값은 `CUSTOM`(`"-N|단말마 — …"`)으로 카드마다 매기고, 문구 앞머리에만 이름을 단다.
  엔진은 인쇄된 `'단말마 —'` / `'출진 —'` 을 읽는다(카드 이름을 박지 않는다).
- ⚠ **'출진' 은 기존 '개전' 과 다르다.** 개전 = 덱에 있기만 하면 게임이 시작될 때 스스로 발동
  (적 전용 장치). 성권이 '개전' 이라고 부른 것은 소환 시 발동이라 **새 이름으로 갈랐다** —
  같은 이름에 규칙이 둘이면 용어집부터 갈라진다.
- **부활 계열의 값은 제 예산의 38%.** 증식(동시에 두 몸, ×0.55)보다 싼 이유는 순차라서다 —
  한 몸씩만 서 있다. 부활 자리는 **죽은 그 자리**(성권 결정) — 수호 순서·왼쪽부터 공격 순서가
  안 흔들리고, 자기가 비운 자리라 판이 꽉 차 있어도 항상 성공한다.

#### 토큰은 `TOKENBORN` 으로 검산에서 뺀다

`FOEONLY`(적 전용) · `LANDBORN`(지형이 낳음) 에 이어 **세 번째 면제**다. 빠지는 이유가 서로
다르므로 라벨도 갈라 찍는다. TOKENBORN = **부모 카드가 이미 값을 냈는데 검산표가 그걸 못 본다** —
미이라가 출진에서 −12 를 치르고 관 3장을 덱에 넣는데, 관 하나하나에 또 값을 매기면 같은 값을
두 번 받는 셈이다. ⚠ 좀비랫은 묘지 지형이 뽑기도 하지만 **1코 덱 카드**라 여기 안 넣는다
(손에서 낼 수 있으면 "값은 부모가 냈다" 가 성립하지 않는다 — 파이어버그 때와 같은 판단).

#### 지형 6종 — `landEnd` 가 **이름이 아니라 문구**를 읽게 바꿨다

| 지형 | kind | 하는 일 |
|---|---|---|
| 죽음의 늪 · 죽음의 호수 · 죽음의 대지 | basic | 어둠 자원 1 · 17장에 6/6/5 로 나뉜다 |
| 망각의 숲 | `twin` | 자원 1 · **턴당 1장 제한을 안 먹는다**(지불 없음) |
| 묘지 | special | 자원 없음 · 내 크리처가 소멸할 때마다 좀비랫 |
| 저주의 땅 | `extra+special` | 자원 2를 내고 놓는다 · 자원 없음 · 내 턴 종료마다 카드 1장 |

- ⚠⚠ **`lands.csv` 의 `kind` 는 이제 `+` 로 여러 개를 붙일 수 있다.** 저주의 땅은 extra 이면서
  동시에 special 이다. 한 칸에 하나만 적을 수 있다는 전제로 두면 그런 지형이 나올 때마다
  새 kind 이름을 만들게 된다(`build_proto_data` 가 집합으로 읽는다).
- ⚠⚠ **`landEnd` 는 지형 이름으로 분기하지 않는다.** 저주의 땅이 바닷속 풍경과 글자 그대로
  같은 일("내 턴 종료마다 카드 1장")을 하는 순간 같은 코드를 두 번 쓰게 됐다. 지금은 `note` 를
  읽는다 — 카드에 적힌 것이 곧 규칙이라는 원칙 그대로다. 소환 지형도 `내 턴 종료마다 X 소환`
  한 줄로 잡는다(불지옥의 계곡). 죽을 때 도는 것은 `landDeath()` — 부르는 곳은 `onDeath` 하나뿐.

#### 인챈트 4종 — 셋은 됐고 **호박 머리는 미구현**

| 카드 | 트리거/fx | 상태 |
|---|---|---|
| 오닉스 | `summon`/`buffnew` | **새 fx 가 필요 없었다** — 대지의 축복과 같은 함수가 E3 에서 정확히 +1/+1 로 풀린다 |
| 만월석 | `end`/`moonwolf` | 충전이 다하는 그 턴에 늑대인간. ⚠ 충전 차감은 fx **뒤**라 `charge<=1` 로 봐야 마지막 발을 잡는다 |
| 흑마도서 | `facehurt`/`grantall` | 새 트리거. `faceDmg` 한 곳에서만 울린다. ⚠ **제물(HP 지불)은 안 울린다** — 그건 피해가 아니라 비용이다 |
| 호박 머리 | `draw`/`scry` | **아직 안 만들었다**(덱 맨 위 5장 고르는 창이 필요 → 3단계) |

- ⚠⚠ 미구현 fx 는 **`FXPEND` 에 선언한다.** `fireEnch` 가 그 카드를 건너뛰어 **충전을 안 쓴다** —
  반쯤 돌다 충전만 태우는 게 제일 나쁘다. 검사(`test_ench`)가 '엔진에 없음' 과 '아직 안 만듦' 을
  갈라 본다: 없으면 실패, 선언돼 있으면 통과. 선언 없이 비면 실패다.
- ⚠ 인챈트를 새로 만들면 **`gen_rogue.py` 를 다시 돌린다.** 강화판이 `tg`/`fx` 를 물려받는데,
  안 돌리면 강화 카드만 `fx: undefined` 로 남아 검사가 **빈 이름**으로 실패한다(실제로 그랬다).

#### 이번 단계에서 **아직 안 도는 카드**

단말마·출진·탈취·면역 부여·다크조커·해그의 주문 3종·죽은 수 카운트는 **2단계**,
덱 조작(듀라한의 머리·미이라의 관·호박 머리·불길한 예감·사신의 시간)은 **3단계**다.
그래서 **1단계의 어둠 덱은 아직 제대로 안 굴러간다** — 카드 뷰어로 보는 것이 이 단계의 목적이다.

### 어둠 개편 2단계 — 단말마 · 출진 · 탈취 · 면역 아우라 (2026-08)

**어둠의 정체성은 죽는 것**이다. 단말마·부활·무덤이 전부 `cleanup → onDeath` 한 줄기를
지난다 — 여기가 어긋나면 카드가 값은 다 치르고 **조용히 아무 일도 안 한다.**
검사: `tools/test_dark.js`(30건) — 그 줄기를 통째로 훑는다.

- **단말마**(소멸할 때) · **출진**(소환할 때) — 규칙문의 `'단말마 —'` / `'출진 —'` **뒤부터
  줄 끝까지**가 그 효과다. ⚠ 그래서 이 둘은 문구의 **맨 뒤**에 와야 한다("제물 6 · 출진 — …").
  ' · ' 로 자를 수가 없다 — 효과 안에 ' · ' 가 들어가는 카드가 있다(본드래곤·서큐버스).
  `drText()` / `brText()` 가 뽑고, `deathRattle()` / `battleCry()` 가 문구를 보고 갈린다.
  카드 이름은 어디에도 안 박는다.
- **부활은 죽은 그 자리**(성권 결정) — `reviveAt(p,u,idx,shrink)`. '(1회)' 면 되살아난 몸의
  `dr` 을 비워 능력을 지운다. 본드래곤은 `shrink` 로 공/체를 한 칸씩 깎으며 반복하고,
  깎을 게 없으면 부활에 실패한다(5/5 → 4/4 → 3/3 → 2/2 → 1/1 → 실패).
- **탈취** — `stealUnit(to,from,idx)`. `u.own` 에 **원래 주인**을 적어 둔다(처음 빼앗길 때만).
  성권 정정: **소멸하거나 손으로 돌아갈 때는 원래 주인에게** 간다 → `ownerOf(side,u)` 를
  `toHand` 와 `onDeath`(무덤)가 함께 본다. 통로가 둘뿐이라 여기만 지키면 샐 곳이 없다.
- **무덤** — `S[p].grave` 에 **원래 주인 쪽으로** 쌓인다. 혼령 부활이 여기서 꺼낸다.
  판 전체 소멸 수는 그대로 `S.dead`(해골쌓기의 HP). ⚠ 성권 요청으로 **무덤 수를 화면에**
  찍는다(`myCnt`/`foeCnt`, 0 이면 안 찍는다).
- **면역 아우라**(포그위저드) — `refreshVeilAura(p)` 가 `u.vaura` 를 켜고 끈다.
  ⚠ **되돌릴 수 있게** 건다 — 위저드가 죽으면 풀려야 하므로 `u.veil` 을 파괴적으로 안 켠다.
  면역과 수호는 양립하지 않으므로(용어집) 아우라 동안만 수호를 접고(`gWas`) 풀릴 때 되돌린다.
  피해 판정은 `veiled(u)` 하나로 본다(`hurt`·`hurtRaw`·`hurtAll`).
  갱신은 `cleanup` 과 `placeCreature` 두 곳 — 나중에 나온 몸도 아우라를 받아야 한다.
- **`pickOne(p,list,msg,act,score)`** — 판에서 **반드시 하나를 고르는** 상태(`S.pick`).
  마인드 플레어(`S.mind`)가 혼자 쓰던 장치를 서큐버스·리치도 쓰게 일반화했다.
  상대(ai)는 `score` 로 알아서 고른다. 배선은 `S.mind` 와 **같은 다섯 자리**
  (newGame · endTurn 차단 · clickSlot · 하이라이트).
- **`darkMass(p,name)`** — 피해가 아닌 광역(물구나무 해골·전염병·블랙홀·자폭 공격·몸통던지기).
  `aoeSilence` 와 **같은 자리에 사람 쪽·AI 쪽 둘 다** 끼운다 — 한쪽만 넣으면 AI 가 그 카드를
  내고 아무 일도 안 일어난다.
- **사신의 수확** — 지금 안 죽인다. `u.doom` 을 심고 `startTurn` 이 **표식을 건 쪽의 턴**에
  거둔다. 그동안 상대가 그 몸을 쓸 수 있는 것이 즉시 요격보다 싼 이유다.

#### ⚠⚠ `kwHas` 가 `includes` 였다 — 문구가 키워드를 삼켰다

포그위저드의 문구("자신을 제외한 내 크리처 전체가 **면역**을 얻는다")가 '면역' 을 품고 있어서
**위저드 자신이 면역이 돼 버렸다**(검사에서 잡혔다). 키워드는 ' · ' 로 갈린 **한 토막 그
자체**다 — 이제 토막을 보고 판정한다(숫자가 붙는 것은 앞머리만 맞으면 된다).
⚠ 앞으로 **규칙문에 키워드 이름을 쓰는 카드**를 만들 때 이 함정을 다시 만난다.

#### 분류가 곧 조작이다 — 노화를 '광역' 에서 '단일' 로

'광역'(aoe)은 `INSTANT` 라 **대상 없이 즉시 터진다.** 노화는 한 개체를 겨누는데 광역으로
적어 놨더니 대상 선택이 안 떴다. 값이 아니라 **조작 방식**이 분류에서 나온다.

#### 이름 없는 몸은 내가 붙였다 — **해골탑**

해골쌓기가 세우는 수호 크리처에 성권 명세상 이름이 없었다. `해골탑`(2코 수호 0/1, TOKENBORN)
으로 붙였고 **갈아도 된다.** HP 는 카드값이 아니라 '이번 판에서 소멸한 크리처 수' 라
소환할 때 덮어쓴다.

#### 아직 안 도는 것 — 3단계(덱 조작)

듀라한의 머리 · 미이라의 관과 미이라 군주 변신 · 호박 머리 · 불길한 예감 · 사신의 시간.
전부 **덱을 들여다보거나 고르는 창**이 필요해서 한 묶음으로 미뤘다.

### 어둠 — 성권 지정 희귀도 반영 (2026-08)

**희귀도는 이름표가 아니라 예산 배수다**(커먼 ×1.00 · 언커먼 ×1.10 · 레어 ×1.22 ·
레전더리 ×1.40). 등급만 바꾸고 두면 카드가 약해지거나 예산을 남긴 채 남는다 —
그래서 등급이 움직인 카드는 **공/체·수치를 다시 채웠다**(자세한 표는 `docs/dark_rarity.md`).

- 포그위저드 0/2→0/1 · 다크조커 2/1→2/2 · 서큐버스 1/2→2/2 · 듀라한 4/4→**6/4** ·
  리치 6/9→6/6 · 가고일 5/6→5/4 · 뱀파이어 10/8→8/8.
- 주문은 기준선이 움직인 만큼: 몸통던지기 17→24 · 사신의 시간 4→5 · 혼령 부활 8→9 ·
  해골 던지기 8→9.
- 망각의 숲을 레어로(`build_proto_data` 의 `twin` 이 `r` 을 안 달고 있었다).

#### ⚠⚠ 희귀도를 바꾸면 **시작 덱이 통째로 다시 짜인다**

시작 덱은 커먼만 싣는다. 듀라한·다크조커·해골 던지기·사신의 시간·전염병이 커먼에서
빠지면서 옛 구성이 성립하지 않았다. 그리고 **4코 커먼이 두 종뿐**(스컬 기마병·나이트메어)
이라 불·물이 쓰던 커브(4코 5장)를 채울 방법이 없다 — 동명 상한이 2 라서.
대신 5코에 커먼 몸이 둘 생겨(가고일·뱀파이어) **뒤가 열린 커브**를 쓴다:
`CURVE_OVERRIDE["dark"] = {1:4, 2:7, 3:7, 4:3, 5:2}`.
⚠ 다음 속성도 같은 일이 난다 — **희귀도 지정을 받으면 커브부터 다시 센다.**

#### ⚠ 검사에 카드 수치를 박지 말 것

`test_dark` 가 가고일 HP 6 을 전제로 '해골 던지기 → HP 0' · '생명 흡수 → HP 46' 을
박아 뒀는데, 가고일이 언커먼→커먼이 되며 5/6 → 5/4 가 되자 둘 다 깨졌다.
지금은 **몸의 HP 를 재 두고 그 배수/합을 본다.** 카드값은 언제든 바뀐다.

### 어둠 개편 3단계 — 덱 조작 (2026-08 · 어둠 완료)

덱을 들여다보거나 카드를 고르는 카드 여섯이 한 묶음이다. **고르는 창은 하나면 된다** —
무엇을 고르는지만 다르다. `cardPick({list,need,title,sub,btn,act})` → `S.cpick`.

- ⚠⚠ **`S.busy` 를 켜지 않는다.** 처리 가리개(z-140)가 이 창(z-80) 위를 덮어
  카드를 못 누르는 채로 멈춘다 — 승리 화면에서 똑같이 당했던 그 함정이다.
- ⚠ 고르기 전에는 턴을 못 넘긴다(`endTurn` 이 `S.cpick` 을 본다).
- ⚠ **상대(ai)는 이 창을 절대 안 연다.** 부르는 쪽이 `p==='ai'` 면 `bestIdx()` 로 스스로 고른다.
  한쪽만 만들면 AI 가 그 카드를 내고 판이 멈춘다.

#### 덱에 심고, 뽑는 순간 발동한다

- `deckIn(p,name,n)` — **무작위 자리**에 꽂는다. 맨 위에 두면 다음 드로우가 또 그 카드다.
- `drawTrigger(p,name)` — `draw()` 안에서 **손 검사보다 먼저** 본다.
  ⚠⚠ 뒤에 두면 손이 가득 찼을 때 `deck.pop()` 으로 **조용히 갈려 나간다** — 덱에 심어 둔
  장치가 보이지도 않게 사라지는 셈이다. 그래서 관·듀라한의 머리는 **손을 안 거친다.**
- 관 = "덱에서 뽑으면 즉시 판에 나온다" · 듀라한의 머리 = "판 위의 X 전부에 +a/+h ·
  이 카드를 덱으로 되돌린다". 이름이 아니라 **뽑힌 카드의 규칙문**으로 갈린다.
  듀라한이 없으면 머리는 덱으로 안 돌아가고 소멸한다(성권 결정).
- `mummyCheck(p)` — "관 3장이 모두 내 판에 서면 미이라 군주로 변한다".
  ⚠ **변신 조항은 출진보다 앞**에 둬야 한다. 출진은 `'— '` 뒤부터 줄 끝까지를 먹으므로
  뒤에 붙이면 소환 효과가 그 문장까지 통째로 삼킨다.
  부르는 곳은 `cleanup` 과 관이 나온 직후 두 곳.

#### 호박 머리 — `draw()` 를 건드리지 않고 `turnDraw()` 에서 갈아탄다

`draw()` 는 발동 도중에도 불리는 **동기** 함수다. 거기서 창을 띄우면 처리 한복판에 끼어든다.
그래서 **턴 시작 드로우 한 자리**(`startTurn`)만 `turnDraw(p)` 로 바꿨다.
`ENCHFX.scry` 는 자리를 지키되 일반 트리거로는 안 돈다(충전식 화염구의 `chargebomb` 과 같은 꼴) —
`fireEnch` 는 `'draw'` 트리거를 부르지 않는다.

#### 덱 맨 위를 다루는 두 함수

`topOfDeck(p,n)` 는 **맨 위부터** 배열로 보여 주고, `takeFromTop(p,n,i)` 는 고른 한 장만 빼고
**나머지를 순서 그대로** 되돌린다. 덱은 `push`/`pop` 으로 쓰는 스택이라 뒤집기를 한 곳에
가둬 두지 않으면 순서가 조용히 어긋난다.

#### 이제 미구현 fx 가 없다

`FXPEND` 는 비었다. **비워 두는 것도 선언**이라 `test_ench` 는 목록이 비었는지를 본다 —
이름이 늘면 그때 눈에 띈다.

### ⚠⚠⚠ 죽음은 **한 통로**로만 지나간다 — `cleanup → onDeath` (2026-08)

`strike()` 가 전투로 죽은 개체를 `d.insts.shift()` 로 **직접 지우고 있었다.** 그러면
cleanup 이 볼 시체가 안 남아 `onDeath` 가 **아예 안 불린다.** 결과:

- **전투로 죽은 몸은 폭발도 환류도 단말마도 안 돌았다.**
- 무덤(`S[p].grave`) · 소멸 수(`S.dead`) · 묘지 지형 · 소멸 트리거 인챈트
  (불사조의 깃털 · 악마의 석상 · 흡혈 의식) · 작열 좀비의 `rise` · 고블린 전차의 `ate` —
  **죽음에 매달린 것 전부**가 조용히 새고 있었다.
- 오래 안 들킨 이유: 공격은 **수호에만** 간다. 수호 없는 판에서는 크리처가 전투로 안 죽고,
  스펠·연소로 죽는 길(`hurt → cleanup`)은 멀쩡했다. 어둠에 수호 단말마(스컬 기마병)가
  들어오고 나서야 눈에 보였다(성권이 로그로 잡았다).

지금은 `cleanup(foe)` 를 부른다. **지름길을 내면 그 통로에 매달린 것들이 전부 새 나간다.**

#### ⚠ 이 검사를 짤 때 두 번 헛봤다

공격 대상 판정 때문이다. **공격은 수호에만 가고, 비행 공격은 지상 수호를 지나친다.**
그래서 회귀 검사는 반드시 **지상 공격자 vs 지상 수호**여야 한다 — 비행(가고일)으로 짰다가
얼굴만 때려서 "고쳤는데도 안 된다" 로 읽었다. 검사: `test_dark.js` 의 '전투 사망' 4건
(단말마 · 환류 · 폭발 · 묘지). 어둠 전용 검사에 있지만 **어둠만의 문제가 아니다.**

### ⚠⚠ 새 스펠 mode 를 만들면 **`modeOf` 에 등록**한다 (2026-08)

`modeOf()` 는 맨 끝이 `return 'target'` 이다. 즉 **모르는 모드는 전부 '상대를 겨눈다' 로
흘러간다.** 그래서 대상이 필요 없는 카드에 대상 선택 창이 떴다 — 산불·수정구의 힘(`ritual`)이
실제로 그랬다(`INSTANT` 에 안 넣었다).

- 대상을 안 고르는 모드는 `INSTANT` 배열에 넣는다(`['draw','heal','aoe','ritual']`).
- 안전망: `modeOf` 가 끝까지 왔는데 `NEEDS_FOE` 도 아니면 **콘솔에 경고를 남기고 `instant`
  로 돌린다.** 조용히 target 으로 떨어뜨리는 것보다 낫다.
- 검사: `test_targeting` 의 '모드가 전부 등록돼 있다' — 카드 하나가 아니라 **POOL 의 스펠
  전체**를 훑어 target 으로 새는 게 있는지 본다.
  ⚠ '상대를 겨눈다' 는 `NEEDS_FOE` 만이 아니다 — 상대에게 심는 부여(작열 감옥)도 정당한
  target 이므로 `isFoeGrant` 도 같이 본다.

### 튜토리얼 — 원정의 **첫 전투**가 곧 규칙 안내 (2026-08)

`rgStart()` 는 이제 지도가 아니라 `tutStart(el)` 로 간다. 건너뛰면 그때 지도로.

**대본은 속성마다 따로다** — `TUTS = {fire:{deck,hand,land,foe,steps}, water:{…}}`.
`tutHas(el)` 로 대본 유무를 묻고, `tutS()` 가 지금 속성의 대본을 준다.
`rgStart` 는 `if(tutHas(el))tutStart(el); else rgMap();` — **대본이 없는 속성은 튜토리얼을
아예 안 한다.** 엉뚱한 속성 카드로 가르치느니 건너뛰는 게 낫다(현재 불·물만 있다).

- 속성마다 가르칠 게 다르다. 불은 **연소·폭발·대상 주문·본체 조준**, 물은 **환류·되돌리기·밀물**.
  그래서 덱·손패·상대 행동·안내글을 통째로 나눠 뒀다.
- 상대 행동(`foe`)은 **문자열이면 크리처 소환**, `{sp,at}` 이면 **내 판 `at` 번째에 주문**이다.
  ⚠ 주문 항목이 필요한 이유: **막는 쪽은 반격하지 않는다.** 수호에 부딪히는 것만으로는 내 몸이
  안 죽어서, 물의 '환류' 를 보여 주려면 상대가 주문으로 직접 잡아야 한다.
- **손패도 상대 행동도 전부 대본이다**(`TUTS[el].hand` · `.foe`). 무작위가 하나라도 섞이면
  "이 카드를 내세요" 를 시킬 수가 없다 — 그 카드가 손에 없을 수 있다.
  `newGame()` 이 돌린 손패를 덮고 멀리건 창도 닫는다.
- ⚠⚠ **대본이 시키는 카드는 덱에 첫 손패 몫만(1장씩) 넣는다.** 단계 통과 조건이 "손에서
  없어졌는가" 라서, 덱에 여분이 남으면 **다음 턴 드로우로 같은 이름이 다시 손에 들어와**
  조건이 영영 안 채워진다. 실제로 그렇게 멈췄고, 드로우 운에 따라 **가끔만** 멈춰서
  더 나빴다. 나머지는 전부 지형으로 채운다 — 드로우가 무엇을 줘도 대본이 안 망가진다.
  (닉시처럼 판을 볼 수 있는 단계는 `!hand.includes` 대신 `board.some` 이 더 안전하다.)
- ⚠ **대본의 순서는 판 위에 대상이 남는지까지 계산해야 한다.** 물에서 밀물(닉시)을 먼저
  시켰더니 올렝이 손으로 돌아가 **판이 비었고**, 뒤이은 정신분열이 겨눌 게 없어 멈췄다.
  지금은 정신분열 → (상대가 올렝을 **다시 냄**) → 밀물 순서다. 밀물은 자기 코스트 이하만
  집으므로 3코 오아네스로는 대체가 안 된다 — 되돌린 그 장을 상대가 다시 내게 해서 푼다.
- ⚠ 뒤 단계가 마나를 쓴다면 앞 단계 `ok` 에 **지형 수를 같이 건다**(`S.turn>=3&&lands>=3`).
  글로만 "지형을 한 장 더" 라고 부탁하면 안 지키는 사람이 있고, 그러면 그 자리에서 멈춘다.
- 상대는 `aiTurn` 이 아니라 `tutFoeTurn()` 이 움직인다(`startTurn` 에서 갈린다).
- ⚠ `TUTFOE` 의 턴 번호는 **내 턴 기준**이다 — 상대 차례에도 `S.turn` 은 그대로다.
  그래서 `1` = 내 첫 턴이 끝난 직후의 상대 차례다.
- ⚠ **훈련 상대에게 지형 셋을 미리 깔아 준다.** 안 그러면 3코 수호(홉고블린)가 세 턴 뒤에나
  나와서 '수호' 안내가 한참 밀린다 — 대본이 가르치려는 순서가 무너진다.
- ⚠ **안내 순서는 화면에서 실제로 보이는 순서를 따른다.** 연소·수호는 '방금 본 것' 을
  설명하는 단계라 첫 턴 종료 뒤에 온다. 순서를 바꾸면 안 본 것을 설명하게 된다.
- ⚠ 단계 조건에 **턴 수를 넣지 않는다** — 몇 턴째에 쓸지는 플레이어 마음이다.
  '그 카드가 손에서 빠졌는가' 로 본다(시작 손패에 반드시 있으므로 안전하다).
- ⚠ `tutSync()` 는 이미 조건이 찬 단계를 건너뛴다. 그래서 **이미 참인 조건을 쓰면 그 단계가
  통째로 날아간다**(연소를 '내 몸이 탔는가' 로 뒀더니 헤레스가 이미 타 있어 건너뛰었다).
- ⚠ 안내판 높이는 글 길이에 따라 다르다 → 그린 뒤 실제 높이를 재어 `--tuth` 로 넣는다.
  고정값으로 밀었더니 긴 안내에서 **상대 손패가 가려졌다.**
- ⚠ 튜토리얼 전투는 원정 전투가 **아니다** — `checkEnd` 에서 보상·패배 화면으로 새지 않게 갈랐다.
- ⚠ 원정을 지도로 시작한다고 가정하던 검사 넷(rogue·rghp·flow·enemies)은
  `tutEnd()`(= 건너뛰기)를 타도록 고쳤다. 검사: `tools/test_tutorial.js`(33건, 불+물).
- ⚠ 검사에서 속성 이름을 박지 말 것 — `Object.keys(EL).find(e=>!tutHas(e))` 처럼 **물어본다.**
  `test_flow` 가 `nature` 를 박아 뒀다가, 대본이 불·물로 갈리는 순간 조용히 틀렸다.
- ⚠⚠ Playwright `waitForFunction(fn, arg, options)` — **arg 가 먼저**다. 순서를 바꿔
  `waitForFunction(fn,{timeout:8000},n)` 로 썼더니 `TUT.step >= {…}` 가 영영 거짓이 되어
  호출마다 기본 30초를 통째로 까먹었다. 검사가 '느린' 게 아니라 헛돌고 있었다.

### ⚠⚠⚠ 검사는 `tools/test_all.js` 로 돌린다 — **크래시를 통과로 세지 말 것** (2026-08)

```
node tools/test_all.js            # 전부
node tools/test_all.js fire water # 이름에 걸리는 것만
```

- ⚠ `node tools/test_x.js | grep -c "❌"` 로 확인하면 **크래시한 검사가 0건 = 통과로 읽힌다.**
  죽은 검사는 ❌ 를 한 줄도 안 찍기 때문이다. 실제로 이렇게 **여섯 개가 조용히 죽어 있었다**:
  test_keywords(대가→제물 개명) · test_water/nostack(불의 군단 삭제) · test_rarity(겁화룡 삭제) ·
  test_swipe(작열 좀비 삭제) · test_ench(인챈트 10종 삭제) · test_mobile(BASICLAND 가 배열이 되며 무한 루프).
  판을 여럿 올리는 동안 전부 초록으로 보였다.
- `test_all.js` 는 **종료 코드와 ❌ 를 둘 다** 본다. 스택 트레이스가 찍히면 크래시로 센다.
- ⚠ **검사에 카드 이름을 박지 말 것.** 카드를 지우면 그 줄이 빨간불이 아니라 **크래시**가 된다.
  가능하면 POOL/데이터에서 골라 쓰고, 꼭 박아야 하면 `if(!POOL[n])` 로 먼저 막는다.

### 카드 설명란은 **개체의 지금 상태**를 보여 준다 (2026-08)

#### ⚠ 써 버려 사라진 능력은 **글에서도 지운다** (2026-08 · 성권 지적)

되살아난 스켈톤에 여전히 "단말마 — 죽은 자리에 다시 소환된다 (1회)" 가 찍혀 있었다.
그러면 **아직 한 번 더 되살아난다** 로 읽혀서, 판을 보고 판단할 방법이 없어진다.

- 단말마 '(1회)' — 되살아난 몸은 `u.dr` 이 비어 있다. `liveKw` 가 그때 인쇄 글에서
  **'단말마 —' 뒤부터 줄 끝까지**를 통째로 지운다.
  ⚠ **조각으로 못 지운다.** ' · ' 로 잘라 내면 본드래곤처럼 효과 안에 ' · ' 가 든 카드에서
  뒤 문장("줄일 능력치가 없으면 발동하지 않는다")이 홀로 남는다. drText 와 같은 규칙이다.
  ⚠ 본드래곤은 '(1회)' 가 아니라 반복이므로 `u.dr` 이 안 비고, 글도 그대로 남는다.
- 환류는 `u.echo` 로 이미 그 일을 하고 있었다(KWRE 에 걸려 상태에서 다시 만든다).
- **포그위저드가 걸어 준 면역(`u.vaura`)도 이제 보인다** — 인쇄에 없으므로 금색(부여)으로.
  안 보이면 이 몸이 왜 안 깎이는지 알 수가 없다.
- 손패·목록은 개체가 아니므로 **인쇄 그대로**다.


`liveKw(name,u,opt)` — 개체가 있으면 인쇄된 글이 아니라 그 개체의 필드로 키워드 줄을 다시 만든다.

- 카드 효과로 붙은 것은 **금색**(`.teff .gr`)으로 찍힌다. 인쇄된 것과 구분이 돼야 한다.
- 잃은 것도 사라진다 — 환류를 이미 쓴 몸에는 '환류' 가 안 뜬다.
- 손패의 **돌아온 장**도 '환류' 를 뺀다(`echoSpent`). 안 빼면 '또 돌아온다' 로 읽혀 계산이 어긋난다.
- ⚠ **되만들 수 있는 것만 인쇄 글에서 떼어 낸다**(`KWRE`). 밀물·제물·축복·성장·개전은 개체
  필드가 없으므로 글 그대로 남긴다 — 떼어 내면 정보가 사라진다.

### 주문 피해 면역은 `spellHits()` 한 곳에서 본다 (2026-08)

- `spellimm`(세이렌) = 양쪽 **모든** 주문 · `spellproof`(수호병) = **주인이 쓴** 주문만.
- ⚠ 예전에는 `spellSelfDmg`(자해 조항) 한 곳에서만 봤다. 그래서 **광역 주문이 세이렌을 그냥
  때렸다** — 카드에 적힌 규칙이 안 돌고 있었다. 이제 `aoeSpread` 와 단일 대상 둘 다 여기를 지난다.

### 원정에는 장수·동명 제한이 없다 (2026-08)

'카드 23 + 지형 17 · 동명 2장' 은 **시작 덱 규칙**이고 원정은 그 밖이다.
보상 지형도 이미 가진 것이 또 나온다 — 화염의 원천 둘을 쌓는 것도 플레이어의 선택이다.

### 턴 종료는 **왼쪽부터 한 개체씩** 순차로 돈다 (2026-08)

`endStep` 은 `async` 다. 연소는 **한 몸이 타고·죽고·폭발까지 끝난 뒤에** 다음 칸으로 간다.

- 전에는 전부 동기로 처리하고 연출만 몰아 띄웠다. 그러면 **무엇이 무엇을 죽였는지 순서가
  안 읽힌다** — 연소로 죽은 몸의 폭발이 옆 몸의 연소와 뭉쳐 버렸다.
- ⚠ **슬롯 번호가 아니라 개체 목록을 미리 떠서** 돈다. 죽으면 판이 당겨져 번호가 어긋나고,
  앞선 폭발에 이미 죽은 개체는 건너뛴다(`board.includes(u)` 로 확인).
- ⚠ `S.gen` 이 바뀌면 즉시 빠진다 — 지난 판의 연출을 이어 그리면 안 된다.
- **번지는 범위가 없다.** `endStep` 의 호출부는 `endTurn` 하나뿐이고 그건 이미 async 다.
  `cleanup` 은 27곳에서 부르므로 **동기 그대로 두고**, 여기서 개체마다 불러 준다.
- 비용: 연소 크리처 0체 7ms · 3체 776ms · 10체 2.6초(SPEED=1). 아무 일 없으면 시간을 안 쓴다.

### 연출 3종 — 부활 오오라 · 환류 생성 · 주문 발사체 (2026-08)

⚠ 연출은 **틀려도 검사가 안 죽는다.** 오오라가 안 뜨고 발사체가 한쪽에서만 날아가도
게임은 멀쩡히 돌아가서 눈으로 볼 때까지 아무도 모른다 → `tools/test_castfx.js`(12건)가
'무엇이 화면에 붙었는가' 를 직접 센다.

- **단말마 부활 = 보라 오오라**(`.slot.revived`). 소환(`.spawned`)과 **색으로 갈린다.**
  ⚠ 슬롯 번호가 아니라 **개체**에 표시를 단다(`u.revfx`) — 부활 뒤에도 판이 당겨져
  번호가 밀린다. render 가 양쪽 판을 훑어 붙인다.
- **환류 = 손패에 일렁이며 생겨난다**(`.hcw.echoin`). 그리고 **상대 것도 앞면으로** 보인다
  (성권 결정) — 무엇이 돌아갔는지는 원래 양쪽이 다 아는 정보다(판에서 죽는 걸 보고 있었다).
  뒷면으로 덮으면 없는 안개를 만든다.
  ⚠ `S[side].shown` 은 **이름별 개수**다. 낼 때마다 지우는 자리를 따로 두면 `hand.splice`
  하는 곳(여럿이다) 중 하나를 언젠가 빠뜨린다 → `renderFoeHand` 가 **손에 남은 수보다 많으면
  스스로 깎는다.** 낸 순간 자동으로 표시가 사라지고, 나중에 같은 카드를 뽑아도 안 새어 나온다.
- **주문 발사체**(`castBolt(p,tEl,name)`) — 시전자 정보줄에서 대상까지 구슬이 날아가 터진다.
  `mode==='dmg'` 인 주문만. 대상이 본체면 `boltTarget(side,FACE)` 가 정보줄을 준다.
  ⚠ **부르는 곳을 늘리지 않는다** — 사람·AI·튜토리얼이 전부 이 함수 하나만 부른다.
  늘리면 한쪽에서만 날아가는 일이 또 난다.
  ⚠ 연출뿐이다. 피해는 **이 함수 뒤에** 준다 — 순서를 뒤집으면 맞고 나서 날아가는 것처럼 보인다.
  ⚠ z-index 145 — **처리 가리개(`.busy` z-140) 위**여야 한다. 아래면 쏘는 내내 딤에 가려 안 보인다.

### 피해 연출 큐 (`FXQ`) — 연소·폭발·광분이 스쳐 지나가지 않게 (2026-08)

폭발·광분은 여전히 **동기 코드**에서 터진다(`onDeath` · `hurt`) — 그쪽까지 async 로 만들려면
`cleanup` 27곳을 다 고쳐야 한다. 그래서 상태는 즉시 바꾸고 연출만 큐에 쌓는다.

- 상태는 그대로 **즉시** 바꾸고, **눈에 보이는 것만** `FXQ` 에 쌓아 뒀다가 나중에 한 발씩
  띄운다(`splashHit` 이 쓰던 수법). 로직을 async 로 바꾸면 소환·사망 경로를 전부 뜯어고쳐야
  하는데 그럴 값어치가 없다.
- 쌓는 곳은 둘: `hurtAll`(크리처) · `faceDmg`(얼굴). 흘려보내는 곳은 `await flushFx()` —
  `strike` 한 번마다 · `resolveAttacks` 뒤 · 인챈트 뒤 · `endStep` 뒤.
- ⚠ **자리 번호가 아니라 개체를 들고 있는다.** 죽으면 판에서 빠져 번호가 어긋나므로,
  띄울 때 현재 자리를 다시 찾고 없으면 쌓을 때의 자리로 띄운다.
- ⚠ 피해가 없으면 **시간을 안 쓴다**(`FXQ` 가 비면 즉시 반환). 아무 일 없는 턴이 느려지면 안 된다.
- ⚠ `newGame()` 에서 큐를 비운다 — 지난 판에 못 흘려보낸 연출이 새 판으로 새면 안 된다.
- 검사: `tools/test_burnfx.js`(6건).

### 대상 지정 — 겹쳐 내기 폐지 · 본체 조준 (2026-08)

- **떨어진 자리로 대상을 정하던 지름길을 없앴다.** 이제 대상이 필요한 주문은 어디에 떨궈도
  **화살표 타게팅으로만** 간다(빈 곳을 누르면 취소). 내려놓는 손짓과 겨누는 손짓이 한 동작에
  섞이면 판이 붐빌수록 엉뚱한 몸에 꽂힌다.
  ⚠ 탭 경로도 같은 길을 쓴다 — `playFromHand` 가 target·mine 모드에서 `startTargeting` 을 부른다.
- **본체 조준** — `canFace(n)` = 규칙문에 "또는 상대에게" 가 있는가(파이어 애로우 · 화염구).
  - 대상은 `FACE`(-99). `tgtValid` 가 `#foeBar` 를 **슬롯보다 먼저** 본다.
  - 상대 정보줄 전체가 대상이다(이름·HP·체력바 어디든). `#foeBar.pick` 으로 켜 준다.
  - ⚠ `#foeBar` 는 슬롯과 달리 **다시 그려지지 않는다** → `render()` 가 매번 `pick` 을 손으로
    지운다. 안 지우면 타게팅이 끝난 뒤에도 정보줄이 빛나는 채 남는다.
  - ⚠ 손패 흐리기는 `hasTargetFor()` 를 본다 — 본체를 때릴 수 있는 주문은 **판이 비어도**
    대상이 있다. 이걸 빠뜨리면 상대 판이 비었을 때 흐려진 채 못 나간다.
  - AI 도 쓴다(2.2 분기): **그 한 방으로 끝나거나, 때릴 크리처가 아예 없을 때** 얼굴로 간다.
- **양옆 소환**(스킬라 · 메두사) — 정규식이 스킬라의 `1/4 수호 여왕의 가신` 앞머리를 요구해서
  **앞머리가 없는 메두사가 통째로 안 걸렸다**(카드가 조용히 아무것도 안 했다).
  이제 앞머리는 있어도 없어도 되고, 이름은 `cardBefore` 와 같은 수로 뒤에서부터 POOL 을 뒤진다.
  ⚠ 규칙문에서 카드 이름을 꺼낼 땐 **항상** 이 방식을 쓴다 — 정규식 한 방으로 자르면
    문장 앞머리를 같이 집는다(산호 검·산불에서도 같은 사고가 났다).
- 검사: `test_targeting` 뒤쪽 7건.
  ⚠ 검사에서 손패를 끌기 전에 **열려 있는 `.mull` 창을 닫고 드래그 상태를 푼다** —
    창이 손패를 덮으면 pointerdown 이 카드가 아니라 창에 꽂혀 '드래그가 안 되는' 것처럼 보인다.

### ⚠⚠ 처리 가리개(`.busy`)는 **끄는 자리를 한 곳으로 모은다** (2026-08)

이겼는데 "전투 처리 중…" 에서 안 넘어가는 사고가 났다. 보상 창은 **떠 있었지만 눌리지 않았다.**

- 원인: `checkEnd()` 가 **busy 를 켠 채로** `render()` 를 했고, 부르는 쪽은 그 뒤에
  `S.busy=false` 만 하고 **다시 그리지 않았다.** 화면에는 가리개가 그대로 남는다.
- 가리개는 `z-index:140` 이라 보상 창(`.mull` 80)을 덮는다 → 창은 보이는데 클릭이 죽는다.
  **눈으로는 멀쩡해 보여서 더 나쁜 종류의 멈춤이다.**
- ⚠ 가리개 z-index 를 낮추는 것은 **답이 아니다.** 흐름 페이지(`.pg` 120)까지 덮어야
  조작이 실제로 막히기 때문이다(낮췄더니 `test_ui` 의 '입력 차단' 이 깨졌다).
- 고친 방식 둘:
  1. **`checkEnd()` 가 직접 `S.busy=false` 한다** — render 하기 전에. 부르는 쪽이 하나라도
     빠뜨리면 같은 일이 또 나므로 끄는 자리를 한 곳으로 모았다.
  2. `render()` 는 **판이 끝났으면(`S.over`) 가리개를 아예 안 띄운다.** 누가 안 껐더라도
     승패 화면·보상 창이 갇히지 않는다.
- 검사: `test_rogue` 의 '이기면 판이 끝난다' 5건. ⚠ HP 를 0 으로 **직접 넣지 않는다** —
  실제로 공격해서 이겨야 그 경로를 지난다. 보상 카드를 진짜로 눌러 덱이 느는지까지 본다.

### 예산 초과는 기록만 한다 (2026-08)

`docs/budget_over.md` — `python3 tools/dump_over.py` 가 만든다. **고치라는 목록이 아니다.**
성권이 속성별 개편을 전부 마친 뒤 한꺼번에 검수하기로 한 것들이다.

- ⚠ **임의로 깎지 말 것.** 명세대로 만든 값이고, 예산에 맞추려고 수치를 손대면 의도한 카드가
  아니게 된다. `validate_budget` 은 계속 '초과' 로 세고 빨간불을 낸다 — 그게 정직한 상태다.
- ⚠ 반대로 **`NOBUDGET` 에 몰래 넣어 조용히 통과시키는 것도 안 된다.** 면제는 '값을 다른 데서
  냈다' 는 근거가 있을 때만이다(FOEONLY = 균형을 안 맞춘 적 전용 · LANDBORN = 지형이 값을 냈다).

## 지형 시스템 (구현 완료)

- **덱 구성**: **40장** (지형 17 + 카드 23) · 동명 카드 **2장 상한** · 매 턴 지형 1장 배치.
- **확정 상수**: 시작 손패 **7장** · 마나 상한 **10** · 덱 40 · 동명 2 · 지형 17.
  (2026-08 변경: 상한 5 → **10**. 코드 상수 하나 `MANACAP`만 바꾸면 지형존 칸 수·홍수 완화 기준·정보 표기가 전부 따라간다.)
- **유색 요구 규칙**: `cost_color = min(3, max(1, ceil((cost−1)/2)))` → 1코~3코 유색1 · 4~5코 유색2 · **6코 유색3**.
  개별 카드는 상향 override 가능(데이터의 `cost_color` 컬럼). 6코가 다색 덱에서 가장 쓰기 어렵다.
- **지형 카드**: `data/lands.csv` — 기본 7종(즉시 사용) + 복합 7종(**놓은 턴 사용 불가**).
- **홍수 완화**: 지형이 상한(`MANACAP`=10)에 도달하면 손의 지형을 버리고 카드 1장 드로우.
  상한이 10이 되면서 이 완화는 **사실상 후반에만 발동**한다(시뮬레이션: 5장 6.9턴 · 7장 10.4턴 · 8장 12.4턴 · 10장 16.4턴).
  덱 40에 지형 17은 상한 5 기준으로 잡은 비율이라, 상한 10에서는 남는 지형이 늘어난다 —
  게임이 20턴을 넘게 갈 생각이 아니면 지형을 15 안팎으로 줄이는 쪽을 다음에 검토할 것.
- **카드 프레임 코스트 표기**: 속성색 채운 동그라미 = 유색, 빈 동그라미 = 무색. 갤러리·에디터·프로토타입 손패 전부 적용.
- **프로토타입**: 자동 마나 → 지형 기반으로 전면 교체 완료. 샘플 3색 덱(강철·대지·어둠) 40장 내장.
- **지형존 (매직 더 개더링 방식)**: 보드 아래(상대는 보드 위, 좌우 대칭)에 `MANACAP`(=10)칸을 둔다.
  **내려놓은 지형을 카드 그대로 보여준다** — 보드·손패와 같은 규격(5:7)이고, 그 위에 자원 토큰과 상태 뱃지만 얹는다.
  - 지형 카드는 `landCardHTML(name,size)` **한 함수**가 담당한다. 손패·지형존·확대가 전부 이걸 쓴다.
    지형은 POOL에 없으므로 `tcardHTML`과 별개다 — 지형 표시를 고치려면 여기만 고친다.
  - **지형판은 크리처 보드와 같은 마크업을 쓴다** — `<div class="board lz">` + `.slot` / `.slot.occ`.
    전용 `.lzwrap`/`.lslot` 클래스는 폐기했다. 지형존 CSS는 토큰·뱃지 오버레이(`.lz .tok`, `.ltag`, `.lzc`)만 남는다.
    규격이 어긋나면 `.board`/`.slot`을 고쳐라 — 두 판이 자동으로 같이 움직인다(실측 슬롯 67.2×94.1 · 비율 0.714로 동일).
  - 배치 수 표기는 판 밖 라벨의 `.lzc`(`#myLzInfo`/`#foeLzInfo`)에 `사용 가능 n / 배치 n / 상한 10` 형태로 찍는다.
  - 자원 토큰은 아트 패널 위에 겹친다. 사용 가능=속성 오브 · 사용함=빈 점선 + `소모` · 배치한 턴=회색 점선 + `대기` · 빈 칸=`+`.
    턴 시작 시 전부 재충전(`l.used=false`).
  - 렌더는 `lzHTML(p)`가 담당하고 `render()`에서 `#myLz`/`#foeLz`에 주입한다. HP 바의 지형 pip은 숫자 표기만 남겼다.
- **확률 계산 결론** (덱30·손패7·지형13): 2턴 98% · 3턴 93% · 4턴 83% · 7턴 5지형 88% · 9턴 기대 6.9장.

## 속성별 견본 덱 — 역디자인 140종 (완료)

메커니즘을 먼저 정하고 그 덱에 들어갈 카드를 만드는 **역디자인** 방식. 7속성 × 20종 = **140종**.

- **정본은 `tools/gen_decks.py`의 `DECKS` 딕셔너리**다. 카드를 고치려면 여기를 고친다.
  - `python3 tools/gen_decks.py --check` → 검산만 · `python3 tools/gen_decks.py` → `data/sample_decks.csv` 출력
  - `python3 tools/build_deck_doc.py` → `docs/sample_decks.html` 재생성 (DECKS를 import하므로 문서·검산이 항상 일치)
  - 현재 **140종 전원 예산 정확히 일치**(초과·미달 0), 커브 7덱 모두 목표와 동일.
- **공통 골격(7속성 동일)**: 20종 = 크리처 11 · 스펠 7 · 인챈트 2 / 23장 = 1코3·2코5·3코5·4코4·5코4·6코2 (+지형 17 = 40장).
  종수는 1코2·2코4·3코4·4코4·5코4·6코2, ×2 채용은 1·2·3코에서 한 종씩(덱의 엔진 카드).
- **스펠 7장 고정 배치**: 1코 저코 요격 · 2코 시그니처A · 3코 드로우 · 3코 시그니처B · 4코 메커니즘 페이오프 · 5코 광역/대형 · 6코 고코 요격.
- **덱 메커니즘**: 불=연소 러시 · 물=환류 템포 · 자연=증식 군세 · 강철=경화 방벽 · 대지=**육중 공성** · 어둠=대가 어그로 · 빛=가호 지속.
- **키워드 예산표는 `gen_decks.py`의 `KW`에 한 번만 정의**한다(배수 먼저·가감 나중). 1회성 효과는 `"-4|소환 시 …"` 형식으로 인라인 가감.
  환산 기준: 1마나 ≈ 예산 4 · HP 8 지불 = 1마나 · 드로우 1장 = 1.5마나. **대가 N만 예산을 더한다**(+N/2).
- ✅ 승격 완료. 140종이 `data/`의 CSV·`cards.json`·`decks.json`에 들어갔고 프로토타입은 속성별 단색 덱 7종으로 교체됐다.
  옛 20종 중 이름이 겹치던 카드는 견본 덱 표 값으로 통일됐고, 티어를 넘던 소멸·처형은 카드풀에서 빠졌다.
  생성 아트 20장은 주제가 맞는 카드 80장에 재배정했다(`promote_decks.py`의 `ART_IMAGE`).

## 아트 작업 — 이미지 생성 프롬프트

- `python3 tools/gen_art_prompts.py` → `docs/art_prompts.html`(복사 버튼) + `.md`
- 정본은 `tools/gen_art_prompts.py`의 **`SUBJECT`(카드 140종 그림 소재)** + `STYLE`(공통 스타일 계약) + `ELMOOD`(속성별 팔레트).
  카드가 늘면 SUBJECT에 항목을 추가한다. 빠지면 실행 시 경고가 뜬다.
- **프레임 28장** = 7속성 × **4타입(크리처 cr · 스펠 sp · 인챈트 en · 지형 ld)**. 한 장짜리 7×4 시트가 기본 경로.
  띠 위치는 프로토타입 실측값이다(`BANDS`): 테두리 3.1% · **이름판 2.2~14%** · **코스트 자리 14~23.6%** ·
  아트창 23.6~56.8% · 효과문 56.9~81% · 능력치 81~97.8%. 카드 CSS를 바꾸면 `BANDS`도 다시 재서 갱신할 것.
- **타입별로 하단 띠가 다르다**(`TYPESPEC`): 크리처=양끝 원형 소켓 2개(공/체) · 인챈트=오른쪽 소켓 1개(충전) ·
  스펠=민무늬 · 지형=민무늬 + **코스트 자리 자체가 없음**(지형은 코스트가 없다).
- **코스트는 프레임에 굽지 않는다.** 1~6개로 개수가 변하고 유색·무색 조합도 카드마다 달라서 구우면 절대 안 맞는다.
  `cost-orbs.png`(속성 7 + 무색 1)을 **별도 모듈로 받아 코드로 얹는다.** 마젠타(#FF00FF) 배경으로 받아 키잉해 투명화.
- ⚠ **속성 상징을 못 박지 않으면 생성기가 제멋대로 만든다.** 실제로 첫 시트에서 강철·대지가 neutral·arcane으로
  바뀌어 나왔다. `ELSYM`에 7종 상징을 고정해 뒀다(불=불꽃 · 물=물방울 · 자연=잎 · 강철=모루+망치 ·
  대지=산봉우리 · 어둠=초승달 · 빛=태양).
- **일러스트 140장** = 속성별 4×5 시트 7장이 기본 경로(지난번과 같은 방식). 개별 프롬프트는 재작업용.
- **파일 이름 규칙** (이대로 받아야 자동 연결):
  `frame-<속성>-<타입>.png` · `sheet-<속성>.png` · `art-<속성>-<번호 2자리>.png`
  번호는 문서 3번 목록 순서 = 크리처 11 → 스펠 7 → 인챈트 2.
- ⚠ DALL·E는 투명 배경·정확한 좌표를 못 맞춘다. 프레임은 **아트창을 단색 빈 사각형**으로 받아
  받은 뒤 잘라내 투명화하고 5:7로 보정하는 전제로 프롬프트를 짰다.
- ⚠⚠ **격자 시트는 카드를 눌러 버린다.** 7열×4행에 5:7 카드를 담으려면 캔버스 비율이 1.25여야 하는데
  DALL·E의 가로 캔버스는 1.75뿐이라, 카드를 칸에 맞춰 40% 납작하게 만든다(실제로 1:1로 나왔다).
  → 프롬프트에 `SHAPE` 문단을 넣어 **카드를 칸에 꽉 채우지 말고 검은 여백을 두고 앉히라**고 못 박았다.
  → 그래도 눌리면 `01-B` 속성별 2×2 시트를 쓴다(칸 비율 0.714 ≈ 세로 캔버스 0.667라 눌릴 압력이 없다).
- **눌려 나와도 버리지 않는다** — `python3 tools/fix_frames.py <시트.png> --cols 7 --rows 4`
  검은 거터로 격자를 찾아 자르고, **일러스트 창(가장 크고 어두운 가로 띠)만 늘려** 전체를 5:7로 맞춘다.
  테두리·이름판·능력치 띠의 장식 비율은 건드리지 않으므로 늘어나 보이지 않는다.
  `--el fire`를 주면 속성 하나짜리 2×2 시트로 처리한다. 출력은 `assets/frames/frame-<속성>-<타입>.png`.

## 생성 프레임 — 보류 (2026-07 되돌림)

**카드는 기존 CSS 벡터 카드로 그린다.** 생성 프레임을 붙였다가 문제가 너무 많아 되돌렸다.

- 스위치: `tools/build_proto_data.py`의 **`USE_FRAMES = False`**. True로 바꾸면 다시 프레임을 쓴다.
  `tcardHTML`이 `FRAMES`에 키가 없으면 CSS 카드로 자동 폴백하므로 스위치 하나로 왔다 갔다 한다.
- 되돌린 이유(전부 프레임 이미지 쪽 결함): 속성 뱃지가 이름판 한가운데를 덮어 카드 이름이 안 보임 ·
  스펠·인챈트에 코스트 자리가 아예 없음 · 효과문 판이 얕아 2줄만 들어감 · 일러스트 창이 카드의 44~62%를 먹음.
- 도구는 남겨 뒀다. 프레임을 다시 받으면 그대로 쓸 수 있다:
  `tools/fix_frames.py`(격자 분할 + 5:7 보정) · `tools/build_frames.py`(창 투명화 + 좌표 실측) ·
  프로토타입의 `frameCardHTML()`.
- **자원 오브 8종은 계속 쓴다** — 문제가 없었다. 지형존 자원 토큰이 생성 오브를 쓴다.
  `tools/build_orbs.py`로 마젠타 시트에서 뽑는다. 카드 코스트 동그라미는 CSS 벡터로 돌아갔다.
  - ⚠ 키잉은 **순수 마젠타와의 RGB 거리**로 한다(`near=95 / far=165`). "R·B 높고 G 낮음" 식으로 판정하면
    **빨강·보라 오브까지 배경으로 먹어 까맣게 뭉갠다**. 자르기는 격자 분할(기본 4열×2행)로 한다.
- 3차 프롬프트는 위 결함을 고쳐 뒀다(`gen_art_prompts.py`의 `BANDS`·`CLEARANCE`) — 다시 뽑을 때 쓴다.

### 기본 지형은 속성마다 **여러 종**일 수 있다 (2026-08)

- 정본은 `tools/promote_decks.py` 의 `BASIC_LAND` — **값이 문자열이 아니라 목록**이다.
  `split_lands(el)` 이 17장을 그 종류들에 고르게 나눈다(3종이면 6/6/5, 나머지는 앞쪽부터).
- 불 = 불지옥 · 용암 폭포 · 용암 동굴 / 물 = 파도 지대 · 폭포 · 설원 지대.
  **옛 화산·심해는 카드째 지웠다** — 성권이 준 지형 목록에 없었다.
- 프로토타입 쪽 짝은 `BASICLAND` + `splitBasics(el,n)`. 원정 지형(`runLands`)과
  생성 적 덱(`rgFight`)이 둘 다 이걸 쓴다.
- ⚠ 같은 표가 세 군데(promote_decks · 프로토타입 · validate_budget)에 걸린다. 한 곳만 고치면
  덱과 화면이 갈린다 — `validate_budget` 의 `basics` 는 목록을 **펼쳐서** 집합으로 만든다.
- ⚠ 검사에 종류 수를 박지 말 것. 뷰어 머리말의 'N종' 은 **지형 종류 + 카드 종류**라
  기본 지형이 3종이 되면서 21 → 23 이 됐다. `test_cards` 는 이제 `data/decks.json` 에서 센다.

## 게시 (GitHub Pages)

- **주소: https://sepi-toolbox.github.io/ten/** — 아이패드 사파리 북마크용. 랜딩에서 프로토타입·갤러리·에디터·설계문서 8종으로 이동한다.
- `gh-pages` 브랜치가 정적 사이트다. main은 건드리지 않는다.
- 갱신: `python3 tools/build_pages.py --push` (/tmp 별도 클론에서 gh-pages를 새로 쌓아 force push)
- ⚠⚠ **확인: `python3 tools/check_published.py`** — 작업본과 게시본(gh-pages)의 VERSION 을 맞대 본다.
  main 만 밀어 놓고 게시를 빠뜨리는 사고가 실제로 났다(0.23.0 에 넉 판 동안 멈춰 있었다).
  사이트가 바뀐 것을 **눈으로 확인하기 전에는** '올렸다' 고 말하지 않는다.
- **토큰**은 `--push` 뒤 인자 → `GITHUB_TOKEN` 환경변수 → `~/.config/ten/token` 순으로 찾는다.
  ⚠ **저장소는 공개다. 토큰을 저장소 안에 두면 그대로 노출된다.** `.gitignore`에 `*token*`·`*.pat`·`.env`를 넣어 두었지만
  근본적으로 저장 위치는 항상 저장소 **밖**(`~/.config/ten/token`, 권한 600)이어야 한다.
  클라우드 컨테이너는 세션이 끝나면 회수되므로 이 파일도 함께 사라진다 — 새 세션에서는 다시 받아야 한다.
- 랜딩 페이지 원본은 `site_index.html`. 링크를 추가하려면 여기와 `build_pages.py`의 FILES/DOCS/DATA 목록을 함께 고친다.
- ⚠ 클로드 앱 미리보기 창은 스크립트를 막아 프로토타입이 빈 화면으로 보인다. **프로토타입은 반드시 Pages 주소로 연다.**
  프로토타입은 외부 폰트를 비차단 로드하고(`media="print"` 스왑), 스크립트가 막히면 안내 배너를 띄운다.

## 크리처가 지는 효과 전부 (2026-08 **전면 개정**)

카드에 인쇄되는 효과는 두 갈래다. 뷰어의 효과 태그 필터도 이 구분을 그대로 쓴다.
**13종 중 8종의 규칙이 이때 바뀌었고 대지에 '육중'이 새로 들어왔다.**
정본은 예산 = `tools/gen_decks.py` 의 `KW`, 규칙문 = `tools/build_proto_data.py` 의 `GLOSSARY`,
구현 = 프로토타입의 키워드 엔진. 셋이 어긋나면 `test_keywords.js` 가 잡는다.

**① 배타 태그 — 스탯 가중치가 달라진다(하나만 고른다)**

| 태그 | 데이터 | 가중치 | 크리처 | 규칙 |
|---|---|---|---|---|
| (없음) | — | 2A+H | 4 | 순수 스탯 |
| 수호 | `g` | 2A+2H | 21 | 아군이 받을 공격을 왼쪽부터 대신 받는다. ATK 캡 = 코스트−1 |
| 비행 | `f` | 4A+H | 14 | 비행 없는 수호는 못 막는다. 공격도 지상 수호를 무시 |
| 관통 | `p` | 3A+H | 9 | 수호를 통째로 무시하고 플레이어를 직접 때린다 |

**② 속성 키워드 — 속성마다 하나씩. `kw` 문자열 한 줄에 ` · ` 로 이어 붙는다**

| 키워드 | 속성 | 예산 | 규칙 | 바뀐 점 |
|---|---|---|---|---|
| 연소 N | 불 | **수명 기준 배수** | **내 턴 종료마다 자신이 N 피해** | 옛 "N턴 뒤 소멸" 폐기 |
| 폭발 | 불 | **−ATK** | 소멸 시 **상대 플레이어에게 자기 ATK 만큼** | 옛 이름 '폭산' · 적 전체 → 얼굴 · 수치 표기 없어짐 |
| 환류 | 물 | −4 | 소멸 시 손으로. **단 한 번**, 쓰면 능력이 사라진다 | 무한 → 1회 (−6 → −4) |
| 밀물 | 물 | −(1+코스트) | 소환 시 **이 카드 코스트 이하** 상대 1개체를 **직접 골라** 손으로 | 자동 선택 → 대상 지정 · N 표기 없어짐 |
| 증식 | 자연 | ×0.55 | 턴 종료 시 왼쪽 빈 슬롯에 복제. **단 한 번** | 무한 → 1회 (×0.5 → ×0.55) |
| 성장 N | 자연 | −round(cbase×0.5/N) | **N턴 뒤 '성장한 X' 로 교체** | 스탯 누적 → 변신 |
| 경화 N | 강철 | −4/−8 | 받는 피해 −N | 그대로 |
| 연마 | 강철 | −5 | 턴 종료마다 **ATK +1, 상한 없음** | +1/+2 3회 → 무제한 (−4 → −5) |
| 진형 | 대지 | −3 | **내 크리처가 나 하나뿐이면 '각성한 X' 로 교체** | 슬롯 1~3 HP 보너스 → 변신 |
| 육중 | 대지 | ×1.25 | **소환한 턴에는 공격하지 않는다** | 신설 |
| 속공 | 불 | −round(cbase×0.35) | **소환 시 내 크리처 1개체를 골라 즉시 공격**시킨다 | 신설(고블린) |
| 대가 N | 어둠 | +2/+3/+4 | 소환 시 내 HP N 지불 (예산을 **더하는** 유일한 키워드) | 그대로 |
| 흡혈 | 어둠 | **−ceil(ATK/2)** | **실제로 입힌 피해의 절반**만큼 회복(반올림) | 고정값 → 가변 · N 표기 없어짐 |
| 가호 | 빛 | −5 | **처음 받는 피해**를 통째로 무효 | '첫 파괴 무효' → '첫 피해 무효' (−6 → −5) |
| 축복 N | 빛 | −2/−4 | 소환 시 내 HP N 회복 | 그대로 |

**③ 고유 효과 4종** — 불사조(소멸 시 잿불) · 조수술사 · 숲의 여왕 · 지진술사.
`"-4|소환 시 …"` 형식으로 인라인 가감하고 `ONSUMMON` 표 + `onDeath` 이름 분기가 처리한다.

### 꼭 기억할 것

- **예산 순서**: `(cbase + 태그 보정) × 키워드 배수 × 희귀도 배수 + 가감`. 배수는 연소·증식·육중 셋뿐.
- **얼굴 피해 1 ≈ 예산 1** — 인챈트 환산으로 실측한 값이다(조수의 인장 4코 = 얼굴 24 ↔ 예산 25 ·
  고대 제단 2코 = 10 ↔ 11). 폭발의 가감이 **−ATK** 인 근거가 이것이다.
  ⚠ 결과적으로 폭발 크리처는 **ATK 1 점이 2 가 아니라 3 을 먹는다**(때리는 값 2 + 터질 때 값 1).
  그래서 폭발은 자연히 '작고 아픈' 몸이 된다 — 그게 이 키워드의 정체성이다.
  ⚠ 수치를 카드에 안 적으므로 엔진은 **죽는 시점의 ATK** 를 쓴다. 연마·버프로 커진 값이 그대로 들어간다.
  ⚠ 처음엔 옛 광역 기준(폭발 N = −1.5N)을 그대로 뒀는데, 그러면 **1코 폭발 카드는 예산 2 만 남아
  몸을 못 만든다**(실제로 '고블린 폭탄병' 을 만들다 막혔다). −N 을 거쳐 −ATK 로 왔다.
- ⚠ **`KW` 표에 None 이 두 종류 있다.**
  · **가감이 None**(밀물·성장·흡혈·폭발) → 코스트 또는 ATK 를 봐야 한다 → `kw_dyn(key, cost, atk)`
  · **배수가 None**(연소) → 체력을 봐야 한다 → `burn_mult(n, hp)`
  그래서 `kw_resolve(keys, cost, hp, atk)` 는 **코스트·체력·ATK 를 함께 받는다** —
  호출부에서 빠뜨리면 3코 / 체력 1 / ATK 0 기준으로 조용히 어긋난다.
- ⚠⚠ **연소 배수는 N 이 아니라 수명(= ceil(체력 ÷ N))이 정한다.**
  `BURN_LIFE = {1:1.70, 2:1.45, 3:1.28, 4:1.16, 5:1.08}`, 6턴 이상은 1.03.
  8턴 안에 끝나는 게임이라 6턴을 사는 크리처에게는 사실상 디메리트가 없다.
  - 한때 N 만 보고 1.15/1.30/1.45 로 줬는데, 그러면 **체력이 두꺼울수록 연소가 안 아픈데
    보정은 더 받는 정반대** 상황이 된다(용암거인 14/14 연소3 = 수명 5턴에 +45%).
    수명 기준으로 바꾸면서 용암거인 14/14 → 11/12, 겁화룡 12/12 → 9/11 로 제자리를 찾았고,
    반대로 수명 1턴짜리(고블린 폭탄병)는 ×1.70 을 받아 1/1 → 2/1 이 됐다.
  - 체력은 카드에 적힌 **입력값**이라 검산에는 순환 참조가 없다.
    반대로 **스탯을 예산에 맞춰 채워 넣을 때는** 체력이 배수를 바꾸므로 후보를 훑어야 한다.
  - 그래도 체력이 곧 수명이라는 사실은 그대로다 — `연소 N` 은 체력이 3N 은 돼야 세 턴을 버틴다.
    개정 직후 1코 2/2 에 연소 3 을 그대로 뒀다가 **낸 턴에 바로 타 죽었다.**
- ⚠ **가호는 `hurt()` 안에 있다**(예전엔 `cleanup()` 의 파괴 무효였다). 순서가 규칙이다 —
  **가호(전부 무효) → 경화(수치 감소)**. 뒤집으면 경화가 깎아 0 이 된 피해가 가호를 벗겨 버린다.
- ⚠ **환류는 손패가 이름뿐이라 진영별 카운터로 소진을 센다**(`S[p].noecho[name]`).
  `placeCreature` 가 카운터를 하나 까면서 `mkUnit(..., {noecho:true})` 로 만든다.
  이걸 빼면 환류 카드가 죽고 살아나기를 무한 반복한다(예산은 1회 기준이다).
- ⚠ **밀물은 대상을 고를 때까지 턴을 못 넘긴다**(`S.tide`). `endTurn` 이 막고, 딤은 대상 지정과
  같은 장치(`body.tidemode` → `.tgtdim`)를 쓴다. 자동 테스트는 `tideBounce('me', S.tide.list[0])` 로 대신 고른다.
- ⚠ **`endStep` 의 순서가 규칙이다**: 연마·증식 → 변신(성장·진형) → **연소 자해가 맨 마지막**.
  연소를 먼저 태우면 그 턴에 성장할 개체가 타 죽어 변신을 못 본다.
  변신은 위 반복과 **따로 돈다** — 같은 반복 안에서 슬롯을 갈아 끼우면 새 몸에 연마가 또 걸린다.
- **`reform()`·`baseHp()`·`growSpec()` 는 삭제됐다.** 진형이 위치 보너스가 아니게 되면서 필요가 없어졌다.
  `mkInst` 도 `ward` 를 안 받는다(가호는 개체가 아니라 **유닛** 상태 `u.ward` 다).

### 인챈트 = 트리거형 (2026-08 전면 복구)

**인챈트는 상시 버프가 아니다.** 정해진 트리거가 올 때마다 **충전을 1 소모하고 한 번 발동**한다.
정본은 `gen_decks.ENCH_TRIG`(트리거·효과 키) → POOL 의 `tg`/`fx` → 프로토타입의 `ENCHFX`.

| 트리거 | 언제 | 쓰는 카드 |
|---|---|---|
| `end` | 내 턴 종료마다 | 불의 제단 · 조수의 인장 · 생명의 샘 · 병기고 · 고대 제단 · 성화 · 해무 · 고블린 화염포 |
| `death` | 내 크리처가 소멸할 때마다 | 연쇄 발화 · 흡혈 의식 |
| `summon` | 내 크리처가 소환될 때마다 | 대지의 축복 · 빛의 장막 |
| `foesummon` | 상대가 크리처를 소환할 때마다 | 전열 구축 |
| `hurt` | 내 크리처가 피해를 받을 때마다 | 강철 의지 |
| `paid` | 내가 HP를 지불할 때마다 | 피의 성배 |

- ⚠⚠ **한때 엔진이 모든 인챈트를 얼굴 피해로만 처리했다.** 15종 중 12종이 인쇄된 규칙과 달랐고
  ('성화 = 내 HP 4 회복' 이 실제로는 상대에게 4 피해였다) 그 바람에 인챈트 예산표가 무의미했다.
- ⚠ **같은 효과를 가진 카드를 만들지 말 것.** 2코 인챈트 넷이 전부 "사용: 내 크리처 HP +4" 로
  똑같았다 — 속성이 다른 의미가 없어진다. `test_ench.js` 가 (트리거·효과·E·C) 조합의 중복을 잡는다.
- ⚠ **강화 카드(`gen_rogue`)도 `tg`/`fx` 를 물려받아야 한다.** 빠뜨리면 강화 인챈트가 전부
  기본값(얼굴 피해)으로 떨어진다(실제로 그랬다).
- ⚠ `fireEnch` 는 `ENCHD` 로 재진입 깊이를 막는다 — 인챈트가 준 피해가 또 death/hurt 를 부른다.
- `drainEnchants(p)` 는 이제 **`end` 트리거만** 태우고 연출을 붙인다. 나머지는 그 순간에 흐른다.
- 검사: `tools/test_ench.js`(17건). 데이터의 `fx` 키가 엔진 `ENCHFX` 에 다 있는지도 대조한다.

### 스펠 부가 조항(라이더)

규칙문의 ' · ' 뒤나 '→' 앞에 붙는 조항. 예전에는 **전부 글자만 찍히고 안 돌았다** —
특히 어둠 덱의 'HP 지불' 5종이 공짜였다(덱 정체성이 통째로 무료였다).

- ⚠ **카드 이름을 코드에 박지 않는다. 규칙문 자체가 규칙이다** — 새 카드는 문구만 맞추면 돈다.
- `spellPay` "내 HP N 지불" — **`pay()` 안**에 건다. 모든 사용 경로가 거기를 지나므로 한 곳이면 된다.
  지불은 `paid` 인챈트 트리거도 함께 울린다.
- `spellHeal` "내 HP N 회복" · "처치한 수만큼 내 HP N 회복"(흡혈 파도)
- `spellSelfDmg` "내 크리처도 N 피해"(겁화) · "내 크리처 전체 N 피해"(소이탄)
- `aoeVal` — 분신은 **제물의 ATK 이 곧 피해량**이고, 연쇄 폭발은 내 연소 크리처 수만큼 오른다
- `dmgVal` "내 크리처가 N개체 이상이면 M"(가시덩쿨) · `drawVal` "없으면 3장, 있으면 1장"(폐허 발굴)
- `hurtRaw` — "경화를 무시한다"(쐐기·관통 사격). **가호는 그대로 먹는다** — 무시하는 건 경화뿐이다.
- 요격 뒤 조항(심판 회복 · 소이탄 자해 · 포식 버프)과 소환 뒤 조항(밀물의 부름 바운스)도 규칙문에서 읽는다.
- ⚠ **죽은 규칙을 남기지 말 것.** 관통 사격의 "초과분은 같은 슬롯 다음 개체로" 는 동명 중첩을
  폐기하면서 대상이 사라진 문구였다 → "경화를 무시한다" 로 바꿨다.

### 고블린 묶음 (2026-08 신규 · 불 카드 풀 확장)

플레이어의 불 샘플 덱(연소 러시)은 **건드리지 않았다.** 전부 매수 0(덱 미수록)으로 카드 풀에만 넣고,
원정의 **'사나운 고블린' 이 고정 덱으로** 쓴다. 늘어난 풀을 보고 나중에 플레이어 덱을 손볼 예정.

⚠⚠ **컨셉 덱에 기존 카드를 섞지 말 것.** 처음에 고블린 11종으로 골격(20종·커브)을 못 채우자
용암거인·겁화룡·소이탄 같은 옛 불 카드 8장을 끼워 넣었는데, 그러면 '고블린 덱' 이 아니게 된다.
**적 덱은 플레이어 덱 구성 규칙을 따르지 않는다** — '이 적이 무엇을 하는가' 를 따른다.
사나운 고블린은 **11종 23장, 커브가 4코에서 끝나는** 순수 어그로 덱이다(속공 아키타입 그 자체).
23장을 채우려 고블린 폭탄병만 3장 쓴다(동명 2장 상한의 예외 — 기본 지형 17장과 같은 취급).
`test_goblin.js` 의 '고블린 카드만으로' 가 옛 카드가 한 장이라도 섞이면 잡는다.

| 카드 | 규격 | 비고 |
|---|---|---|
| 고블린 폭탄병 | 1코 2/1 · 연소 1 · 폭발 | 낸 턴에 스스로 터진다(체력 1 + 연소 1) |
| 용암 정령 | 2코 3/1 | 예산 1 남음 — 나중에 뭘 더 붙일 여지 |
| 도화선 | 2코 부여 | 내 크리처에 **+5/+0 · 연소 3 부여**. 연소는 **더한다** |
| 파이어 볼트 | 1코 단일 2 | 불똥과 같은 자리 |
| 불사조의 깃털 | 2코 드로우 | 내 크리처 1개체를 파괴하고 2장 |
| 고블린 지휘관 | 3코 3/3 · 속공 | 언커먼 |
| 고블린 전차 | 3코 3/6 · 연소 2 · 폭발 · 삼킴 | 레어 |
| 고블린 화염포 | 3코 인챈트 E3×C5 | 언커먼. 턴 종료마다 **남은 충전 수만큼 아군 포함 무작위**에 1 |
| 고블린 방패병 | 4코 수호 2/7 · 폭발 | |
| 와이번 | 4코 비행 4/6 · 연소 2 | |
| 고블린 요새 | 지형(특수) | 자원 0 · 내 턴 시작마다 고블린 폭탄병 소환 |

- **속공** — 소환 시 내 크리처 하나를 **직접 골라** 즉시 한 번 공격(밀물과 같은 지정 장치, `S.rush`).
  ⚠ 고른 개체는 `sick=true` 가 되어 **이번 턴 정규 공격에서 빠진다.** 안 그러면 두 번 때린다.
  ⚠ 값은 자기 ATK 가 아니라 **남의 ATK** 라 카드 수치로 못 잡는다 → 코스트에 비례해 잡았다.
- **고블린 전차의 삼킴** — 소환 시 아군 하나를 **정식으로 파괴**하고(`u.ate` 에 이름만 남긴다)
  죽을 때 그 자리에 되돌려 놓는다.
  ⚠⚠ **파괴 판정을 반드시 지난다** — 폭발·환류·불사조가 그대로 터지는 게 이 카드의 존재 이유다
  (희생 콤보). 한때 조용히 치웠다가 정체성이 통째로 사라졌다. 예산도 −2 → **−4** 로 올렸다.
  ⚠ 이름은 **cleanup 전에** 떠 둬야 한다. 파괴되면 슬롯이 사라져 못 읽는다.
  ⚠ 삼킬 대상은 '가장 좋은 몸' 이 아니라 **죽어서 이득인 몸**(폭발 > 불사조 > 환류 > 싼 것)을 고른다.
    한때 최고 스탯을 삼켜서 제 에이스를 도로 치우고 있었다.
  ⚠ 확대창에서 삼킨 크리처를 **뒤에 겹쳐** 보여 준다(성장 카드와 같은 `.zstack`, 스와이프로 교체).
    `zoomHTML` 은 삼킨 쪽을 성장 상위 몸보다 **먼저** 본다 — 지금 실제로 그 안에 있는 것이니까.
- **불사조의 깃털(희생 드로우)** — 판정은 규칙문 하나로 한다(`isSac` = "내 크리처 1개체를 파괴하고").
  카드 이름을 코드에 박지 않는다. mode 는 draw 지만 **대상이 필요하므로 `modeOf` 가 'mine' 으로 돌린다.**
  - AI 는 **손해 보지 않을 때만** 쓴다: ① 곧 죽을 개체(HP≤2 또는 이번 턴 연소로 타 죽는다)
    ② 2코 이하 값싼 개체 ③ 손에 낼 게 이것뿐. 조건을 빼면 AI 가 자기 에이스를 갈아 넣는다.
- **인챈트 중 얼굴 피해가 아닌 것은 `ENCHFX` 에 이름으로 분기**한다(고블린 화염포).
  규칙문이 제각각이라 E×C 한 벌로는 표현이 안 된다. 새 인챈트를 만들면 여기 한 줄 붙인다.
- **특수 지형** — `lands.csv` 의 `kind=special`. 자원을 안 만들고(`LANDS[n].sp`) 다른 일을 한다.
  문구와 동작은 `LANDFX` 한 곳에서 정의한다. 도는 자리는 둘: 턴 시작은 `landStart`(고블린 요새),
  턴 종료는 `landEnd`(호수 유적 · 바닷속 풍경 · 불지옥의 계곡).
  - ⚠ **`freeLands()` 에서 반드시 빼야 한다.** 빼먹으면 자원 없는 지형으로도 카드가 나간다.
  - ⚠ `els` 가 비어서 카드 색·pip 이 없어지므로 `home`(테마 속성)을 따로 실어 색을 잡는다.
  - 덱에 몇 장 넣느냐가 곧 밸런스다 — 깔릴수록 실질 마나 상한이 내려간다.
    고블린 덱은 17장 중 3장(화산 14 + 요새 3) = 10턴까지 기대 1.3장 → 실질 상한 8~9.

### 연소 묶음 (2026-08 신규 · 잿불 방화범 · 화염술사 카린)

고블린과 같은 방식이다 — **전부 매수 0**, 플레이어 불 덱은 그대로 두고 카드 풀만 늘렸다.
불 속성 일반 적 셋이 이제 서로 다른 것을 한다: **고블린 = 수로 민다 · 방화범 = 태워서 민다 ·
카린 = 주문으로 태운다.** (한때 방화범이 '터지는 놈' 이라 고블린과 구분이 안 됐고,
'보드를 비운다' 는 카린의 자리였다.)

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 화염 아귀 | 3코 3/1 · 언커먼 | **내가 스펠을 낼 때마다 HP +2**(최대치도 같이). 시작 몸은 종잇장 |
| 화염 방패 | 2코 강화 | 내 크리처에 **+0/+5 · 연소 2 부여**. 주로 화염 아귀와 짝 |
| 파이어볼 | 3코 광역 · 언커먼 | **적 전체 4 · 내 크리처도 4.** 자기 판까지 비운다 |
| 불씨 살리기 | 3코 강화 · 언커먼 | **내 연소 크리처 전체 HP +2**(최대치 무시). 대상을 안 고른다 |
| 정령의 불꽃 | 4코 소환 · 레어 | **패를 전부 버리고 그 수만큼 3/1 용암 정령**(연소 1). 그 턴에 다 탄다 = 개체당 얼굴 3 |
| 작열 좀비 | 2코 3/2 연소1 · 언커먼 | **소멸 시 잿더미 좀비(1코 0/3 수호)가 그 자리에 남는다.** 앞은 공격, 뒤는 벽 |
| 잿더미 좀비 | 1코 수호 0/3 | 작열 좀비가 남기는 몸. 손에 잡히지 않지만 **카드로 존재한다**(확대창 스와이프) |

엔진에 처음 생긴 장치 넷 — 새 카드가 같은 것을 하려면 여기를 재사용한다.

- **개수를 손패에서 세는 소환** — `parseSummon` 이 `버린 수만큼` 을 보면 `n:null` 을 돌려주고,
  `resolveSummon` 이 그 자리에서 손패를 센 뒤 통째로 버린다.
  ⚠⚠ **부르는 쪽이 이 카드를 손에서 먼저 빼야 한다.** 안 빼면 자기 자신을 하나 더 세고,
  뒤늦게 `hand.splice(S.sel,1)` 하면 이미 빈 손패라 **카드가 손에 남는다**(무한 반복).
  `clickSlot` 의 summon 갈래와 AI 2.7 갈래 둘 다 먼저 빼도록 고쳤다 — 실패하면 원래 자리에 되돌린다.
  ⚠ 자리가 없어 0개체가 나와도 **무르지 않는다**(버린 것 자체가 발동) → `made>0||t.n===null`.
- **토큰에 키워드 부여** — 규칙문의 `· 연소 1 부여` 를 `grantOf` 로 읽어 `mkToken` 결과에 심는다.
  토큰 이름도 이제 규칙문에서 뽑는다(`3/1 용암 정령을 소환` → "용암 정령 토큰").
  예전에는 카드 이름 앞머리를 썼다 — 불의 군단이 "불의 토큰" 이 되는 식이었다.
- **대상을 고르지 않는 강화** — `MASSBUFF` 정규식(`내 (…) 크리처 전체 HP +N`)이 곧 판정이다.
  `modeOf` 가 이걸 보면 `'instant'` 로 돌려 대상 지정을 건너뛰고, `resolveInstant` 가 처리한다.
  ⚠ **최대치를 무시**하므로 현재 HP 와 최대 HP 를 **함께** 올린다(= 영구 +0/+N, 회복이 아니다).
- **스펠에 반응하는 크리처** — `pay()` 가 모든 사용 경로를 지나므로 거기 한 곳에 `onCast(p,name)` 을
  걸었다(`c.k==='sp'` 일 때만). `mkUnit` 이 kw 에서 `spellhp` 를 읽는다.
  ⚠ 지형은 `pay()` 를 부르지 않는다 — 지형으로는 안 자란다.
  ⚠ **강화 스펠 자신도 스펠이다.** 화염 방패를 아귀에 쓰면 +5 에 더해 반응 +1 이 붙어 4/1 → 4/7 이 된다.

**AI 가 못 내던 부류를 열었다** — `aiTurn` 에 2.65(강화·부여) · 2.66(전체 강화) · 4)(마지막 순위)를 넣었다.
그전까지 AI 는 `NEEDS_MINE` 스펠 경로가 아예 없어서 **고블린 덱의 도화선 2장이 끝까지 손에서 썩었다.**
- 2.65 대상은 붙이고도 그 턴을 넘길 몸으로 고른다 — `(현재 HP + 버프 HP) > (기존 연소 + 부여 연소)`.
  안 그러면 3/1 용암 정령에 도화선(연소 3)을 붙여 그냥 버린다.
- 4) **가장 낮은 순위** — 더 낼 게 없으면 좋은 대상이 없어도 남은 하수인에게 붙인다.
  잿불 방화범처럼 판이 죄다 자폭병인 덱은 2.65 조건을 영영 못 채워 손에서 썩는다.
  어차피 태워 죽일 몸이라 얹은 ATK 만큼은 얼굴에 들어간다.
- 전체 강화는 **받을 개체가 둘 이상일 때만** 낸다(`massCount`).
- **자해 광역(파이어볼)은 내 몸이 살아남을 때 쏜다** — `selfDmgOf()` 로 자해 수치를 미리 재고,
  타 죽을 내 개체 수(`lose`)와 지울 상대 개체 수(`foeKill`)를 견준다.
  ⚠⚠ **살릴 강화가 지금 손에 있으면 이번 턴은 안 쏜다**(`rescuable`). 셈으로만 보면
  화염 아귀 하나와 상대 셋을 바꾸는 게 이득이지만, 화염술사가 하려는 건 그 교환이 아니라
  **방패를 두르고 혼자 살아남는** 것이다 — 방패를 먼저 두르고 같은 턴에 그대로 쏜다.
  그때 2.65 는 '이 광역에 타 죽을 몸'(`saveIdx`)을 최우선으로 고른다.
  ⚠ 광역 값을 재려고 `aoeVal()` 을 부르면 **안 된다** — 분신은 재는 순간 내 크리처를 태운다(부작용).
    판단에는 `POOL[n].v` 를 쓴다.
- 검사: `tools/test_burn.js`(20건) — 카드들의 동작 + AI 가 실제로 내는지까지 본다.

**죽은 자리에 다른 몸이 남는다(작열 좀비)** — `RISE` 정규식(`소멸 시 X로 되살아난다`)이 판정이다.
`mkUnit` 이 `rise` 를 읽고, `onDeath` 가 고블린 전차의 `ate` 와 **같은 장치**로 그 자리에 세운다.
확대창은 `grownPeer` 가 앞뒤 양방향으로 짝을 찾는다(`riseBase` 는 풀을 훑어 역방향을 되찾는다).
⚠ 안내 문구는 `peerLabel` 한 곳에서만 만든다 — 예전에는 '성장한 몸/원래 몸' 두 갈래가
zoomHTML 과 bindZoomSwipe 에 따로 박혀 있어서, 새 짝이 생기자 둘 다 거짓말을 했다.

### 용암 대장 그록 묶음 (2026-08)

**거물 램프.** 화염의 원천으로 한 턴 앞질러 큰 몸을 세운다. 6종 23장 — 파이어볼 · 고블린 방패병 ·
불의 군단 · 소이탄 · 겁화룡 · 용암거인. 단계는 아직 **매수만** 굵어진다(새 카드는 미정 · TODO).

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 화염의 원천 | 지형 · 레전더리 · 덱에 1장 | **자원을 둘 낸다.** 그록은 첫 패에 무조건 쥐고 시작한다 |
| 용암거인 (개정) | **8코** 11/12 연소3 · 레어 | 이번 판에 **소멸한 크리처 1개체마다 비용 -1**(최소 1) |
| 소이탄 (개정) | 4코 부여 · 레어 | 즉시 파괴 → **상대 크리처에 연소 5 부여**(지연 제거) |
| 불의 군단 (개정) | 4코 소환 | 3/4 둘 → **2/1 넷**. 총량은 같지만 개체 수가 값인 판에서 다르게 쓰인다 |
| 파이어볼 | 3코 광역 · 언커먼 | **겁화(5코)를 밀어내고 플레이어 불 덱에 정식 채용** |

- **지형 한 장이 자원을 둘 낸다** — lands.csv 의 `kind=rich` → LANDS 의 `n:2`.
  ⚠⚠ 마나는 이제 **지형 장수가 아니라 자원 합**이다. `.length` 로 세던 곳이 셋
  (`manaLeft`·`colorLeft`·`pay`) 있었다 — 한 곳만 놓쳐도 '낼 수 있다고 해 놓고 지불이 모자라'
  조용히 어긋난다.
  ⚠ `pay` 는 **남은 몫에 딱 들어가는 것 중 가장 큰 것**부터 쓴다. 그냥 작은 것부터 쓰면
  3코를 화산·화산·원천(=4)으로 내 자원 하나를 버린다(실제로 그렇게 샜다).
- **인쇄 비용 ≠ 실제 비용** — `costOf(p,name)` 하나가 정본이다(`canPay`·`pay`·손패 뱃지가 전부 이걸 본다).
  ⚠ 카드에 찍히는 숫자는 늘 **기본값**이다(스탯과 같은 규칙). 지금 값은 손패 위 `.costnow`
  오버레이 뱃지로만 보여 준다.
  ⚠ 소멸 카운터 `S.dead` 는 **양쪽 다** 센다(cleanup 한 곳에서 올린다 — 토큰 포함).
  ⚠ 커브 검산에 `CURVE_OVERRIDE` 를 뒀다. 불 덱만 8코 한 칸이 있는데, 그건 인쇄 비용이지
  실제로 내는 비용이 아니다.
- **'부여' 인데 상대를 겨누는 것** — `isFoeGrant` (규칙문에 '상대 크리처'). `modeOf` 가 대상을
  반대편으로 돌리고 `resolveOnFoe` 가 처리한다. AI 는 **두 턴 안에 탈 몸**(`hp <= 연소×2`)에만 쓴다 —
  체력 12짜리에 연소 5를 심으면 아무 일도 안 일어난다.
- **적별 첫 패 보정(`opener`)** — 카드 효과가 아니다. '이 적은 늘 이걸 쥐고 시작한다' 는 규칙이고
  `dealHand` 끝에서 `forceOpener` 가 제일 비싼 손패 한 장과 바꾼다(지형은 fixLand 가 이미 챙겼다).
  없으면 그록의 8코 거물이 영영 안 나온다.
- **원정 보상 지형** — `RICHLAND` 속성별 한 종, **런당 한 장**. RG.deck(카드)이 아니라
  `RG.lands` 로 들어가고 기본 지형 몫에서 그만큼 뺀다. 정예 35% · 일반 12% 로 뜬다.
- 검사: `tools/test_grok.js`(20건).

#### 그록 2·3단계 — 적 전용 카드

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 화산 폭발 | 5코 소환 · **적 전용** | **빈 슬롯을 전부 불씨정령(3/1 연소1)로 채운다** |
| 일대일 대련 | 3코 소환 · **개전** · 덱에 1장 · **적 전용** | 양쪽 모두 **자기 덱에서 가장 비싼 크리처**를 하나씩 소환 |

- **적 전용(`FOEONLY`)** — 플레이어가 절대 못 얻는 카드. **예산 검산에서 통째로 빠진다**(애초에
  균형을 맞출 생각이 없는 카드라 기준을 들이대면 의미 없는 '초과' 만 쌓인다).
  ⚠ 이름을 `gen_decks.FOEONLY` 에 **직접 적어야만** 빠진다 — 조용히 새지 않게.
  POOL 에 `foe:1` 로 실리고, 뷰어는 '적 전용' 으로 찍고, `poolOf()` 가 원정 보상·상점에서 뺀다.
- **개전** — **덱에 있기만 하면** 게임이 시작될 때, **첫 패를 뽑기 전에** 스스로 발동하고 덱에서 빠진다.
  크리처·인챈트는 판에 놓이고, 스펠은 즉시 발동한다(대상은 무작위).
  판정은 규칙문/키워드에 낱말 '개전' 이 있는가 하나뿐이다(`isOpening`).
  ⚠ `newGame` 에서 **로그를 먼저 비우고** 개전을 돌린다 — 예전 순서(손패 → 로그 비우기)로 두면
  개전 기록이 그대로 지워진다.
  ⚠ 덱에서 제일 비싼 몸은 **인쇄 비용(c.c)** 으로 고른다. 개전 시점엔 아직 아무것도 안 죽어서
  깎이는 비용(용암거인)이 의미가 없고, '제일 비싼 카드' 라는 문구 그대로여야 한다.
- AI 는 판을 채우는 소환(화산 폭발)을 **빈 칸이 4 이상일 때만** 쓴다 — 한두 칸 남기고 쓰면 헛돈다.

### 겁화룡 이그니스 (보스 · 2026-08)

**개전 넷을 깔고 시작하고, HP 20 부터가 2막이다.** 9종 23장 — 홍염×4 · 도화선 · 불사조의 깃털 ·
작열 좀비 · 용의 숨결 · 불의 군단 · 불사조 · 용암거인 · 이그니스×2. 지형 화산 16 + 화염의 원천 1.

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 홍염 | 2코 0/3 연소1 · **개전** · 적 전용 | 넷이 깔린 채 시작 · **소멸 시 적 전체에 3** (연소 1 + 체력 3 = 세 턴 시한폭탄) |
| 용의 숨결 | 3코 부여 · 적 전용 | **상대 크리처 전체에 연소 4** — 대상을 안 고른다 |
| 이그니스 | 8코 수호 20/20 · 적 전용 | **내 HP 20 이하에서만** · 내가 받는 피해 전부 무효 · **소멸하면 즉시 패배** |

- **얼굴 피해가 한 곳을 지난다(`faceDmg`)** — 이그니스가 서 있으면 전부 0 이 된다.
  ⚠⚠ 예전에는 `S[foe].hp-=v` 가 **일곱 군데**에 흩어져 있었다(공격·속공·폭발·인챈트 face·
  충전식 화염구…). 한 곳만 놓쳐도 무적이 샌다. 새 피해 경로를 만들면 반드시 여기를 지날 것.
  ⚠ 대가(HP 지불)는 피해가 아니므로 여기를 지나지 않는다.
- **`core` = 이 몸이 곧 목숨** — 소멸하면 `S[p].hp=0`. 그래서 **AI 는 절대 제물로 쓰지 않는다**:
  `sacOK(u)` 하나를 깃털(희생 드로우) · 도화선(연소 부여) · 분신(태워서 광역) · 전차(삼킴) ·
  `autoMine` 이 전부 본다. 한 곳만 빠뜨려도 AI 가 제 목숨을 스스로 갈아 넣는다.
  ⚠ 순수 버프(화염 방패 같은)는 얼마든지 얹을 수 있다 — 막는 건 **제물·자해·연소 부여**뿐이다(`isSacLike`).
- **카드 자신이 거는 사용 조건** — `hpGateOK` ("내 HP N 이하에서만 낼 수 있"). `canPay` 와
  손패 흐리기가 같이 본다.
- **마지막 보루(aiTurn 2.9)** — `core` 카드는 무엇보다 먼저 낸다. 판이 꽉 차 있으면
  **제일 값 안 나가는 제 몸을 정식으로 파괴해 자리를 만든다.** 안 그러면 2단계가 통째로 안 열린다
  (실제로 토큰이 열 칸을 채워 이그니스가 끝까지 안 나왔다).
- ⚠ 검사에서 손패 **장수**로 판정하지 말 것 — `aiTurn` 은 끝에 `endTurn()` 을 **기다리지 않고**
  부르므로, 그 뒤에 상대 턴이 시작되며 카드를 한 장 더 뽑는다. '그 카드가 아직 손에 있나' 로 본다.
- 검사: `tools/test_ignis.js`(13건).

### 방화범·화염술사 2·3단계 카드 (2026-08)

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 용암 쥐 | 2코 3/2 연소1 · 언커먼 | **소멸 시 카드 1장.** 어차피 타 죽으니 사실상 확정 드로우다 |
| 불의 샘 | 2코 인챈트 E1×C5 | 턴 종료마다 **내 연소 크리처 전부 HP +1**(최대치도 같이 = 수명이 는다) |
| 불사조 | 5코 비행 4/5 연소2 · 레어 | **소멸 시 불사조의 알(0/1)** → 내 턴 시작에 멀쩡한 몸으로 부화 |
| 충전식 화염구 | 3코 인챈트 E1×C4 · 언커먼 | **주문마다 충전 +1**(최대 8) · 내 크리처가 없는 채로 턴이 끝나면 충전만큼 적 전체+얼굴에 |
| 화염 광신도 | 3코 3/2 · 언커먼 | 소환 시 **소각된 주문** 하나를 무작위로 손에 |
| 푸른 불꽃의 수호병 | 3코 수호 2/4 · 언커먼 | **내가 쓴 스펠에는 피해를 받지 않는다** |
| 불씨정령 (개정) | 1코 **3/1** 연소1 | 예산을 전부 공격력에. 낸 턴 종료에 스스로 꺼지는 3피해 카드가 됐다 |

- **부화(hatch)** — `rise` 의 짝이다. `rise` 는 죽을 때 다른 몸을 남기고, `hatch` 는 **내 턴이 시작될 때**
  원래 몸으로 되돌린다(`startTurn` 에서 `transformUnit`). 알을 못 치우면 계속 돌아온다 — 그게 노림수다.
  ⚠ 옛 불사조의 '1/1 잿불 소환' 특수 분기는 지웠다. 이제 이름을 코드에 박은 크리처 효과는 없다.
- **소각 더미(`S[p].burned`)** — TEN 에는 무덤이 없다. **써 버리거나 버린 주문만** 따로 쌓는다.
  `pay()` 한 곳에서 넣고(`burnSpell`), 정령의 불꽃이 버린 패의 주문도 같이 넣는다.
- **`spellproof`** — 자해 조항(`spellSelfDmg`)이 이 유닛만 건너뛴다. 겁화·파이어볼·소이탄 전부에 걸린다.
- **모았다가 터지는 인챈트** — 보통 인챈트는 트리거마다 충전을 1씩 **쓴다.** 이건 반대다.
  ⚠⚠ 그래서 트리거를 **`fuse`** 로 뒀다. `fireEnch` 는 이 트리거를 절대 부르지 않는다 —
  일반 경로에 걸리면 매 턴 종료마다 충전이 1씩 깎여 영영 안 모인다.
  실제 동작은 `fuseEnchants(p)` 가 하고, `ENCHFX.chargebomb` 은 **데이터의 fx 키가 짝을 잃지 않게**
  남겨 둔 표시용 자리다(`test_ench.js` 의 'fx 키가 엔진에 있다' 가 이걸 본다).
  ⚠ 뇌관은 `endStep` **뒤에** 재야 한다 — 연소로 내 판이 비는 것도 '비었다' 로 쳐야 한다.
- **인챈트 범위 배수에 `조건전체`(×2.0) 를 넣었다** — '내 연소 크리처 전부' 처럼 **부분집합**만 받는 것.
  `전체`(×3.5)로 재면 불의 샘이 2코에서 예산을 통째로 넘긴다.

**AI**
- 뇌관(충전식 화염구)이 4 이상 찼고 **판이 이미 비어 있으면 크리처를 내지 않는다**(`hold`) —
  한 마리만 놔도 조건이 깨져 이번 턴 폭발이 통째로 날아간다. 인챈트는 몸이 아니라 계속 낸다.
- 반대로 뇌관이 얹혀 있으면 **파이어볼로 제 판까지 태운다**(`fuseCharge(P)>0` 이면 광역 판단이 뒤집힌다).
  평소 규칙(내 몸이 죽으면 안 쏜다)의 유일한 예외다.
- `spellproof` 유닛은 광역 손익 계산에서 **잃는 몸으로 세지 않는다** → 수호병이 서 있으면 그냥 쏜다.
- 검사: `tools/test_arson.js`(22건) — 카드 동작 + 위 네 가지 AI 판단.

### 고블린 2·3단계 카드 (2026-08)

| 카드 | 규격 | 하는 일 |
|---|---|---|
| 고블린 미치광이 | 3코 3/1 · 언커먼 | **소환 시 자신을 제외한 모든 크리처에 12 을 1점씩 흩뿌린다.** 아군도 맞는다 |
| 고블린의 열의 | 2코 소환 | **고블린 폭탄병 2개체 소환** — 토큰이 아니라 카드 그대로(연소 1 · 폭발이 따라온다) |
| 고블린 지뢰 | 3코 인챈트 E3×C6 · 언커먼 | **폭발이 터질 때마다** 그 피해만큼 상대 크리처 1개체에 (무작위) |

- **`boom` 트리거 신설** — `onDeath` 의 폭발 처리 끝에서 `fireEnch(p,'boom',{dmg:v})` 를 부른다.
  ⚠ 피해량이 **카드에 안 적혀 있다** — `ENCHFX.mirror` 는 `ctx.dmg` 를 쓴다. 인챈트 표의 E 3 은
  예산 계산용 평균값일 뿐이다(E×C 로 값을 매기는 틀을 깨지 않으려고 남겨 둔 자리).
- **흩뿌리는 피해** — `splashHit()` 이 1점씩 무작위 칸에 꽂는다(개체가 아니라 **칸** 단위).
  ⚠ 상태는 동기로 한 번에 바꾸고, 눈에 보이는 연출(`popHits`)만 나중에 한 발씩 띄운다.
  연출을 기다리게 만들면 `onSummon` 이 전부 async 가 되어 소환 경로 다섯 군데를 다 고쳐야 한다.
  ⚠ 경화·가호가 있는 몸에는 1 점이 안 먹힌다 — 합계를 재는 검사는 방어 키워드 없는 판으로 할 것.
- **카드 그대로 소환** — 소환문에 스탯(`3/1`)이 안 적혀 있으면 `parseSummon` 이 앞의 이름을 POOL 에서
  찾아 `real` 로 돌려주고, `resolveSummon` 이 `placeCreature` + `onSummon` 으로 정식 소환한다.
- **소환물은 언제나 판의 오른쪽 끝**에 붙는다(모든 소환 카드 공통). 떨어뜨린 자리에 끼워 넣으면
  카드마다 결과가 달라져 예측이 안 된다.

### 적별 고정 덱 (gen_enemies 의 `FIXED`)

아키타입 점수로 자동 생성하는 대신 **덱을 통째로 지정**한다. 컨셉이 분명한 적에게 쓴다.
지금 셋 — `fire_goblin`(11종 23장, 커브 4코까지) · `fire_arson`(8종 23장) · `fire_karin`(7종 23장).

- 키는 적 id. **`cards` 는 플레이어 골격(20종·커브)을 따르지 않는다** — 컨셉이 곧 커브다
  (카린은 3코에서 끝난다). 23장만 맞으면 된다.
- **`cards` 는 난이도 단계별 딕셔너리다**(`{0:[...], 1:[...], 2:[...]}`).
  적어 두지 않은 단계는 **바로 앞 단계를 그대로** 쓴다(`fixed_cards`).
- ⚠⚠ **고정 덱에는 강화 카드 치환(`apply_over`)을 걸지 않는다.** 강화는 '같은 카드인데 수치만
  큰 것' 이라 컨셉 덱에서는 단계가 올라도 **하는 일이 안 변한다.** 대신 단계마다 새 카드를 넣어
  덱이 무엇을 하는지 자체를 바꾼다(고블린 2단계 = 미치광이·열의 · 3단계 = 지뢰).
  자동 생성 덱 32명은 지금까지처럼 강화 카드로 세진다.
  지금 세 덱 모두 3단계까지 채워져 있다 —
  고블린(미치광이·열의 → 지뢰) · 방화범(용암 쥐·불의 샘 → 불사조) · 카린(화염구·광신도 → 수호병).
  그록(화산 폭발 → 일대일 대련) — 이 둘은 **적 전용**이라 예산 검산 밖에 있다.
  이그니스(보스)는 세 단계가 같은 덱이다 — 홍염·용의 숨결·이그니스도 전부 적 전용(TODO 단계별 카드).
- `lands` 로 **지형 구성까지** 지정할 수 있다. 프로토타입 `rgFight` 가 `e.lands` 를 보고 그대로 깐다
  (없으면 예전대로 기본 지형 한 종류를 카드 수에 비례해 깐다).
- ⚠ `RG.fighting` 은 **`newGame()` 뒤에** 켠다. 미리 켜면 커튼이 떠 있는 동안의 render() 가
  지난 전투의 낡은 `S.me.hp` 를 `RG.hp` 로 흘려보내 **이벤트로 회복한 HP 가 도로 깎인다**(실제로 밟았다).
- 검사: `tools/test_goblin.js`(10건).

### 예산 기준 정리 (2026-08 · 여기가 정답지다)

수치를 정할 때 이 환산만 보면 된다. 새 효과를 만들 때도 여기에 얹어서 값을 잡는다.

| 자원 | 예산 |
|---|---|
| 1 마나 | **4** (`cbase(n) = round(3n + 1 + (n−1)²·0.5)`) |
| ATK 1점 | 2 (일반·수호) · 4 (비행) · 3 (관통) |
| HP 1점 | 1 (일반·비행·관통) · 2 (수호) |
| **내 HP 1점** (지불·회복) | **0.5** — HP 8 = 1마나 = 예산 4 |
| **상대 얼굴 피해 1** | **1** — 인챈트 환산 실측(조수의 인장 4코 = 얼굴 24 ↔ 예산 25) |
| 카드 1장 드로우 | 6 (1.5마나) |
| 크리처 희생 | 6 (1.5마나) |

- **ATK 에 값이 달린 키워드 둘** — 폭발 `−ATK`(터질 때 얼굴에 ATK) · 흡혈 `−ceil(ATK/2)`
  (한 번 때릴 때 ATK/2 회복 × 평균 2회 ≈ ATK HP ≈ 예산 0.5·ATK).
  결과적으로 **폭발 크리처는 ATK 1점이 3, 흡혈 크리처는 2.5** 를 먹는다 — 둘 다 자연히 작고 아파진다.
- ⚠ **스펠 기준값은 정수로 반올림한다.** 스펠은 수치가 하나뿐이라 7.7 같은 눈금을 맞출 방법이 없다.
  소수로 두면 언커먼·레어 스펠 12종이 상시 '여유 −0.7' 로 잡히는데 그건 여유가 아니라
  **맞출 수 없는 눈금**이다. 정수로 반올림해 두면 '올릴 수 있는데 안 올렸다' 가 분명히 드러난다.
  (이 정리로 광역 5코 7→8, 소환 4코 18→20, 성광 16→18 등 12장이 제 몫을 찾았다.)
- **남는 예산은 공격력에 붙인다.** 지금 남은 것은 전부 **눈금 때문에 못 쓰는 1** 이거나 구조적 상한이다:
  · 여유 1 — ATK 1점이 2~4를 먹어서 1로는 못 산다(용암 정령·와이번·묘목·고목·세계수·공성귀·대천사 등)
  · 불의 제단 4 · 고블린 화염포 3 — 인챈트는 `E×C×배율` 이라 눈금이 성기다(화염포는 아군도 맞아 의도된 여유)
  · 수호는 **ATK 캡(코스트−1)** 이 있어 남는 값을 ATK 로 못 돌린다 → HP 로 돌린다(철벽 5/13 → 5/15)

### 성장·진형이 갈아입는 상위 몸

- `tools/gen_grown.py` 가 만든다 → `data/grown.json` → `build_proto_data` 가 `GROWN` 으로 주입 →
  게임·뷰어에서 `Object.assign(POOL, GROWN)`.
  - 성장 → **"성장한 X"** · 진형 → **"각성한 X"** (트리거가 다르니 이름도 다르게 읽히는 게 낫다)
  - 예산 = `(cbase + 태그 보정) × 희귀도 × GROW_MULT(1.5)`. **원본의 키워드 배수·가감은 뺀다** —
    상위 몸은 성장/진형 키워드를 잃기 때문이다. 스탯은 원본 ATK:HP 비율을 지키며 꽉 채운다.
  - 수호의 ATK 캡은 상위 몸에서 **한 단계만** 올린다(코스트−1 → 코스트). 안 올리면 예산이 전부 HP로 가서
    벽만 두꺼워진다.
- ⚠ **상위 몸은 덱에도 카드 목록 개수에도 들어가지 않는다.** 필드와 확대창에서만 나온다
  (원정 강화 카드 `ROGUE.over` 와 같은 취급). 뷰어의 `BASE` 는 **합치기 전에** 떠 둔다.
- **확대창에서 성장 전/후를 겹쳐 보여 준다**(`.zstack`). 가로로 24px 이상 끌거나 그냥 탭하면 앞뒤가 바뀐다.
  - ⚠ 뒷장은 `position:absolute` 로 뺀다. 둘 다 흐름에 두면 확대창 폭이 두 배가 되어 좁은 폰에서 카드가 반토막 난다.
  - ⚠ 스와이프 핸들러에서 이벤트를 **삼켜야** 한다. 확대창은 '아무 데나 누르면 닫힘' 이라
    안 막으면 넘기려던 손짓이 그대로 창을 닫는다.
  - ⚠⚠ **`stopPropagation` 만으로는 못 막는다.** 닫기는 document 의 **캡처** 단계에 걸려 있어
    바깥에서 안으로 내려오므로 `.zstack` 핸들러보다 **먼저** 돈다 → `zoomCloser` 가
    `closest('.zstack')` 을 보고 카드 안에서 시작한 손짓이면 **닫지 않고 다시 무장**한다(`armZoomCloser`).
  - ⚠⚠ **`.zhint` 에 `pointer-events:none` 이 꼭 있어야 한다.** 이 안내줄은 `position:fixed` 에
    `left/right:0` 인데 좁은 화면 미디어 규칙이 `top` 까지 잡으면 top~bottom 이 통째로 늘어나
    **화면을 덮는 투명 판**이 된다. 그러면 뒤집은 뒤(`.zfront` 가 z-index 0 으로 내려간 뒤)
    히트 테스트가 카드가 아니라 안내줄에 걸려 한 번 넘기면 다시 못 넘겼다. 실제로 그렇게 막혔다.
  - **뷰어(cards/index.html)도 같은 장치를 쓴다.** `build_cards_page.py` 가 `grownPeer`·`riseBase`·
    `peerLabel`·`bindZoomSwipe` 를 프로토타입에서 **그대로 가져간다** — 여기서 다시 적으면 두 벌이 된다.
  - 검사: `tools/test_swipe.js`(10건) — 게임·뷰어 양쪽에서 넘겨지는가·안 닫히는가·밖을 누르면 닫히는가.

### 보드 표시

- 아직 안 쓴 능력은 **카드 테두리 바깥 고리**로 알린다: 환류 파랑 · 증식 초록 · 가호 노랑.
  - ⚠ 광채는 `.slot` 이 아니라 그 안의 `.tcard` 에 건다 — `.slot.occ` 가 border/box-shadow 를
    `!important` 로 지우고 있어서 슬롯에 걸면 안 보인다.
  - ⚠ **`border-color` 는 건드리지 않는다.** 그건 속성(=카드 정체)이다. 한 번 덮었더니
    불 카드가 파랗게 보여 어느 속성인지 알 수 없었다. 고리는 `box-shadow` spread 로만 준다.
- 성장까지 남은 턴은 **일러스트 왼쪽 위**(`.gtim`). 카드 맨 위에 두면 이름 첫 글자를 덮는다(실제로 덮었다).
  오른쪽은 키워드 뱃지 줄(`.kbs`)이 쓰므로 왼쪽만 비어 있다.
- 스펠 분류는 전부 구현: 단일 · 광역 · 요격 · 바운스 · 드로우 · 회복 · 강화 · 소환 · 파쇄 · 부여.

## 희귀도 (커먼 · 언커먼 · 레어 · 레전더리)

- **정본은 `tools/gen_decks.py` 의 `RARITY` 표.** 이름 → 희귀도를 **직접** 적는다(자동 배정 아님).
  거기 없는 카드는 전부 커먼. 지금은 커먼 70 · 언커먼 42 · 레어 21 · 레전더리 7이며,
  '덱에서의 역할'로 잡은 **1차 초안**이라 마음대로 고쳐도 된다.
- **예산 배수** — 커먼 ×1.00 · 언커먼 ×1.10 · 레어 ×1.22 · 레전더리 ×1.40 (5코 기준 24 → 24/26/29/34).
  크리처는 `(cbase+태그보정) × 키워드배수 × 희귀도배수 + 가감` 순서로 곱한다.
  ⚠ **가감(`대가 6` 같은 절대량)은 배수 밖**에 둔다 — 안 그러면 이중 할인이 된다.
  ⚠ **희귀도를 올려도 카드가 저절로 강해지지 않는다.** 늘어난 예산만큼 스탯·수치를 직접 올려야 한다.
  얼마나 남았는지는 `validate_budget.py` 의 `[1-b]` 가 카드별로 알려 준다(실패가 아니라 안내).
- **파이프라인**: `gen_decks.RARITY` → `promote_decks` (POOL 의 `r` 필드 · CSV 의 `rarity` 열) →
  `build_proto_data` → 게임/뷰어. 커먼은 `r` 을 **안 적는다**(기본값이라 데이터를 가볍게 둔다).
  강화 카드(`gen_rogue`)는 원본의 희귀도를 그대로 물려받는다.
- **표시**: 일러스트 아래 **경계선 한가운데에 걸터앉는** 마름모 보석(`.rgem`). 색은 속성과 겹치지 않는
  별도 팔레트(회색·초록·파랑·주황). 크기는 `--cw` 비례(**7.5%**)라 어느 규격에서나 같다.
  ⚠ 자리를 세 번 옮겼다. 폭 10%로 **오른쪽** 경계에 걸쳤더니 아래 꼭짓점이 효과문 첫 줄을 덮었고,
  일러스트 **안쪽**으로 집어넣었더니 그림을 가려 답답했고, 오른쪽 끝에 작게 두었더니 눈에 안 띄었다 →
  **폭을 줄여 경계선 한가운데**가 정답이었다. 좌우 어느 글자와도 안 겹치고 그림도 덜 가린다.
  아래 꼭짓점 = `-bottom + (√2−1)/2 × 폭` = .030 + .0155 = **.0455 < 글자 시작 .053**.
  ⚠ `bottom` 은 `.tart` 의 **padding 상자** 기준이라 아래 테두리(.023)만큼 더 내려가야 경계선에 닿는다.
  ⚠ 가운데 정렬은 `left:50%` + 음수 `margin-left`. `transform:translateX(-50%)` 를 쓰면
  **45° 회전 선언을 덮어써 마름모가 그냥 사각형이 된다.**
  ⚠ 45° 회전이라 **크기**를 잴 땐 `offsetWidth`(`getBoundingClientRect` 는 √2 배로 부푼다).
  반대로 **꼭짓점 좌표**를 볼 땐 그 부푼 상자가 곧 정답이다 — `test_rarity.js` 가 140종 전부를 돌며
  '글자 침범 0 · 경계선 아래로 걸침 > 0 · 카드 중심과 일치' 를 잰다.
- **레전더리 프리즘(홀로그램)** — 실제 포일 카드처럼 **두 겹**이다. 반짝임은 **레전더리 전용**이고
  등급별로 광택을 다르게 주는 확장은 접었다(등급 구분은 보석 색이 맡는다).
  - `::before` 넓은 무지개, `mix-blend-mode:color`, opacity 0.28 — **색조만** 얹어 명도를 안 건드리므로
    글자 대비가 그대로 남는다. 9초에 한 바퀴.
  - `::after` 좁고 밝은 반사光 한 줄, `overlay` — 4.5초 주기로 카드를 가로지른다.
  - ⚠ 두 겹 모두 **z-index 3**. 숫자 뱃지·코스트 구슬(`.thead`/`.tstat`, z-index 5)보다 아래여야 한다.
    위로 올리면 ATK 붉은색·HP 청록색이 무지개에 물들어 무슨 수치인지 헷갈린다.
  - ⚠ `color-dodge`·`screen` 은 밝은 양피지 위에서 하얗게 날아가 글자가 안 보인다.
    `hue`나 높은 opacity 는 일러스트 색을 통째로 갈아엎는다(용이 초록색이 된다). 넷 다 실제로 비교해 봤다.
  - `prefers-reduced-motion` 에서는 멈추고 반사光은 끈다.
  - **더 요란한 2·3단계(반짝이 알갱이·액자 테두리)는 만들었다가 걷어냈다.** 강도 선택 UI
    (`prismGet/prismSet`·`ten.prism`·게임 설정과 뷰어의 프리즘 칩 줄)도 함께 지웠다 —
    **이 두 겹 하나로 확정**이니 다시 만들지 말 것.
- **규칙 영향은 둘뿐이다**(덱 동명 상한은 건드리지 않았다):
  - 로그라이크 보상·상점 등장률 — `rogue.json` 의 `config.rarityWeights`
    (일반 60/26/11/3 · 정예 34/30/25/11 · 상점 48/28/18/6). `pickByRarity(names, mode)` 가 쓴다.
    ⚠ '있는 카드 중에서' 고르는 것이라 그 속성에 레전더리가 없으면 가중치가 높아도 안 나온다.
  - 적 덱 구성 — `gen_enemies.RAR_BONUS[band]` 를 아키타입 점수에 더한다.
    1단계는 커먼 쪽, 3단계는 레전더리 쪽으로 기운다(일반 적 기준 레어+레전더리 2.2 → 4.9장).
    정예·보스는 원래 비싼 카드를 다 들어서 차이가 작다 — 그쪽 난이도는 강화 카드 수와 HP가 맡는다.
- 검사: `tools/test_rarity.js`(17건) + `validate_budget.py` `[1-b]`.

## 카드 뷰어 (cards/index.html)

- `python3 tools/build_cards_page.py` 로 만든다. **카드를 뷰어용으로 다시 그리지 않는다** —
  `prototype/index.html` 에서 CSS와 카드 함수를 **잘라다 붙인다**. 규격이 두 벌이 되면 반드시 어긋난다
  (옛 `tools/card_gallery.html` 이 실제로 그렇게 낡았다).
  - 떼어 오는 구간: `<style>` 전부 · `ELEM` · `ART` · `POOL` · `LAND` · `CARDJS` 마커 · `landCardHTML` · `glossaryFor`.
  - ⚠ `<style>` 은 **전부** 이어 붙여야 한다. 프로토타입에는 `<noscript><style>…</style></noscript>` 가
    먼저 나와서, 첫 블록만 집으면 카드가 통째로 민무늬로 나온다(실제로 그렇게 만들었다가 잡았다).
  - ⚠ 게임 CSS 는 `html/body` 를 잠근다(한 화면 게임 판). 목록은 스크롤해야 하므로 그 부분만 되돌린다.
  - `tools/build_pages.py` 가 배포 전에 자동으로 다시 뽑는다.
- 덱(속성)별로 묶어 보여 준다. 지형이 먼저, 그 뒤는 코스트 순. 속성·종류 필터와 이름/효과 검색,
  카드를 누르면 게임과 같은 `lg` 확대 + 용어 설명.
- **원정 적 덱도 같은 페이지에서 본다**(상단 탭). 적 35명(일반 21·정예 7·보스 7)을
  **난이도 3단계**(`ENEMY.bands=[0,3,7]` → 0~2 · 3~6 · 7~10층)별로 고를 수 있고,
  단계마다 덱 구성·체력·강화 카드 수가 달라진다. 등급·속성 필터와 이름 검색도 붙는다.
  - 강화 카드(`ROGUE.over`)는 **적 덱에만** 나온다 → `POOL` 에 합치기 **전에** 기본 목록(`BASE`)을
    떠 두고, 카드 목록의 개수는 그것으로 센다.
  - **주력 카드**(코스트 높은 순 4장)를 맨 앞에 세우고 금색 '주력' 표를 단다.
    ⚠ 정의는 게임의 적 조우 화면(`rgEnter` 의 `top`)과 **같아야** 한다 — 한쪽만 고치지 말 것.
    `test_cards.js` 가 두 곳을 대조한다.
- **효과 태그 필터**(`#tagBar`) — 크리처가 지는 효과로 걸러 본다. 두 갈래를 한 줄에 섞어 놓았다:
  **배타 태그**(수호·비행·관통, 데이터의 `g`/`f`/`p` 플래그)와 **속성 키워드**(연소·폭산·환류·밀물·
  증식·성장·경화·연마·진형·대가·흡혈·가호·축복, `kw` 문자열의 머리 낱말).
  거기에 `고유`(그 카드에만 있는 "소환 시 …"·"소멸 시 …")와 `없음`(순수 스탯)을 더해 **18칸**이다.
  - ⚠ **목록을 뷰어에 다시 적지 않는다.** 정본은 `build_proto_data.py` 의 `GLOSSARY` 이고
    칩 순서·설명문·색까지 거기서 끌어온다(순서 = 태그 → 속성별 → 기타). 키워드를 추가하면
    GLOSSARY 에만 넣으면 칩이 저절로 생긴다. `test_cards.js` 가 두 곳을 대조한다.
  - ⚠ `kw` 의 머리 낱말이 GLOSSARY 에 없으면 `고유`로 묶인다 — 새 키워드를 GLOSSARY 에 안 넣으면
    조용히 '고유' 칸으로 흘러 들어가므로, 칩 개수가 안 늘면 이걸 의심할 것.
  - 태그를 고르면 **크리처만** 남는다(스펠·인챈트·지형은 이 태그를 지지 않는다).
    고른 태그의 규칙 설명이 바로 아래 줄(`#tagDesc`)에 펼쳐진다 — 확대 카드의 용어집과 같은 문장이다.
- **뷰어도 앱(PWA)으로 깔린다.** `cards/manifest.webmanifest` · `cards/sw.js` · 아이콘 4종
  (`tools/build_cards_icons.py` 가 생성). scope 가 `/cards/` 라 게임과 **별개의 홈 화면 아이콘**이 생긴다.
  - 아이콘도 일부러 다르게 그렸다 — 게임은 마름모 하나, 뷰어는 부채꼴 카드 세 장 + `CARDS`.
    둘 다 깔았는데 똑같이 생기면 어느 쪽이 게임인지 알 수 없다.
  - ⚠ **서비스워커 scope 는 달라도 캐시 저장소는 출처 하나를 공유한다.** 게임 SW 가 옛 캐시를 지울 때
    `k !== CACHE` 로 전부 지워서, 게임을 열 때마다 뷰어의 오프라인 캐시가 날아갔다 →
    양쪽 다 **자기 접두사만**(`ten-v` / `ten-cards-`) 지우도록 고쳤다.
- **카드 뽑아보기** — 덱 헤더(속성 덱 · 적 덱 모두)의 `카드 뽑아보기` 버튼.
  그 덱을 실제로 40장 쌓아 **시작 손패 7장**을 돌려 보여 주고, 2000회 통계(지형 분포·평균 지형·
  평균 2코 이하·보정이 일한 비율)를 함께 낸다. 손패의 카드를 누르면 그대로 확대된다.
  - ⚠ **손패 매수와 뽑기 보정을 여기서 새로 만들지 말 것.** 게임의 `fixLand`(첫 손패에 지형이
    한 장도 없으면 덱의 지형과 **가장 비싼 카드**를 맞바꾼다)와 같아야 한다 — 갈리면 이 화면이 거짓말을 한다.
  - 적 덱은 난이도 단계(`F.band`)를 따라간다. 고정 덱이면 지형 구성(`e.lands`)도 그대로 쓴다.
- ⚠ **마커 밖의 상수도 빼먹으면 페이지가 통째로 죽는다.** `landCardHTML` 이 쓰는 `LANDFX` 를
  안 가져가서 적 덱 탭이 백지로 나왔다(`LANDFX is not defined`). `const(src, name)` 로 한 줄씩 가져온다.
- `tools/test_cards.js` 가 **게임과 마크업이 글자 단위로 같은지** 대조한다(44종). 이게 이 페이지의 존재 이유다.
  적 덱 6건 · 효과 태그 5건 · 앱(PWA) 4건도 함께 돈다(총 25건).
- 클래스 이름 충돌 주의: 원정 보상의 '강화' 표시를 `.rgc.over` 로 썼더니 **패배 화면 `.over{display:none}`** 에
  걸려 카드가 0×0 으로 사라져 있었다 → `.rgc.ovr` 로 바꿨다. 전역 클래스를 새로 지을 땐 먼저 검색할 것.
- ⚠ **강화 카드는 `CE` 에 없다.** `tcardHTML` 이 `CE[name]||'steel'` 로만 속성을 찾아서 원정의 강화 카드가
  전부 **회색**으로 나오고 있었다 → `CE[name]||c.el||'steel'`. 일러스트도 없으므로 `c.base` 의 것을 빌려 쓴다.

## 지형존 · 조작 규칙

- **카드는 손패 밖으로 끌어야만 나간다.** 탭으로 고르는 흐름(`selectCard`)은 삭제했다.
  손패 탭에는 아무 동작도 걸려 있지 않고, 길게 누르면 확대만 된다.
  `clickSlot`은 남아 있다 — **대상 지정이 유효 대상을 확정할 때** 이걸 부른다.
- **화면의 취소 버튼은 없앴다.** 대상 지정 중에는 **빈 곳을 누르면** 취소되고, 그 밖에는 취소할 상태가 없다.
  (`cancelTargeting()`은 코드에 그대로 있다 — 새 게임·드래그 시작에서 부른다.)
- **가운데 조작 줄(`#hint`)은 조작 안내를 보여 준다.** 한 번 '턴 표시만'으로 바꿨다가 되돌렸다.
  ⚠ 화면 한가운데 떠올랐다 사라지는 **턴 배너(`.turnban`)와 헷갈리지 말 것** — 요청이 어느 쪽인지 먼저 확인한다.
- **턴 배너는 상자가 아니라 옅은 그라데이션 위의 글자다.** 테두리·불투명 배경은 판을 가려 답답하고,
  배경을 아예 없애면 밝은 카드 위에서 글자가 안 보인다 → `.turnban::before` 로 **가장자리로 사라지는**
  방사형 그라데이션을 깔고, 글자에는 검은 그림자를 한 겹 더 준다. 아래 그라데이션 바는 지웠다.
- **조작 줄 위아래 여백은 대칭이어야 한다.** 남는 세로를 `#foeLz,#foeBoard,.ctl,#myBoard,#myLz` 다섯 곳이
  `margin-top:auto` 로 똑같이 나눠 가진다.
  ⚠ `.ctl` 에 `margin` **축약형**을 쓰면 그 `margin-top:auto` 가 지워져 위쪽만 여백이 사라진다.
- **낼 수 없는 카드는 손패에서 흐리게** — 판정은 `whyNotPlayable(n)` 하나가 한다(못 내면 이유 문자열을 돌려준다).
  마나뿐 아니라 **대상이 없거나 빈 자리가 없는 카드도** 포함한다. 손패 흐리기·드래그 팁·실패 토스트가 모두 이걸 본다.
- **빈 판 안내 문구**는 `.board.empty::after`. 크리처 판은 `크리처 & 인챈트`, 지형존은 `지형`.
- **쓴 지형은 뒤집힌다.** `pay()`가 `freeLands` 순서대로 소모하므로 **왼쪽부터** 쓰이고,
  `startTurn`이 `used`를 풀어 **다음 내 턴에 한꺼번에 앞면**으로 돌아온다.
  - 뒤집힌 칸은 상대 손패와 같은 `.cback`을 쓴다. 앞·뒷면은 `.lface` 껍데기 안에 있고
    뒤집기 애니메이션(`scaleX` 0→1)은 그 껍데기에 건다.
  - **뒤집기 연출은 양방향 모두 태운다.** `pay()`가 쓴 지형에 `justUsed`를, `startTurn()`이
    되돌린 지형에 `justBack`을 찍고 각각 잠시 뒤 지운다. `.flip` 클래스가 이 둘을 본다.
    ⚠ 돌아올 때 연출을 빼먹으면 앞면으로 '턱' 하고 갈리는 느낌이 난다(실제로 그랬다).
    ⚠ 플래그 없이 `.used`만 보고 애니메이션을 걸면 `render()`마다 다시 재생된다.
  - 카드 가운데 있던 **동그란 자원 마커(`.tok`)는 없앴다** — 뒤집힘과 알갱이 연출이 같은 정보를
    더 잘 보여 준다. '대기'(놓은 턴) 표시는 `.ltag`로 남는다.
- **지형 슬롯에는 `data-lz`(진영)·`data-i`(인덱스)를 붙인다.** 길게 눌러 확대할 때 쓴다.
  ⚠ 길게 누르기 핸들러에서 **지형존을 `.slot.occ` 보다 먼저** 검사할 것. 지형 슬롯도 `.slot.occ`라
  순서가 뒤바뀌면 `board[undefined]`를 읽고 조용히 아무 일도 안 한다(지형 확대가 안 되던 이유).
  뒤집힌 지형도 DOM이 아니라 **데이터에서 이름을 찾으므로** 그대로 확대된다.
- **자원 이동 연출**(`manaFlow`): 지형에서 알갱이가 튀어나와 방금 낸 카드로 빨려 들어간다.
  색은 **실제로 지불한 속성**(`EL[el].c`). 목적지는 `S.spawn` 슬롯, 없으면 그 편의 판 가운데.
  ⚠ 위치는 `render()`가 끝난 뒤에 재야 한다(지형이 방금 뒤집혀 자리가 달라졌을 수 있다) → `rAF` 두 번 미룬다.
  ⚠ `S.gen`을 물고 있다가 새 게임이 시작됐으면 그냥 버린다.
  검사는 `test_lands.js`(13건).

## 첫 턴 멀리건 · 턴 안내 · 공격 연출

- **멀리건 = 하스스톤식. 기회는 한 번, 손해는 없다.** 새 게임을 시작하면 시작 손패 화면이 먼저 뜬다(`showMull()`).
  7장을 가운데에 펼치고, **다시 뽑고 싶은 카드만 눌러 고른 뒤 확인 하나**를 누르면 그만큼 새로 받는다.
  런던 멀리건(뽑을 때마다 한 장씩 덱 아래로, 최대 3회)에서 바꿨다 — `MULLMAX`·`mullBtn`은 사라졌다.
  - `redrawPicked(p, idxs)`: **새로 뽑은 다음에** 되돌린 카드를 덱에 섞는다. 순서를 뒤집으면
    방금 버린 카드가 그 자리에서 다시 올 수 있다. 장수 손해는 없고 총량 40장은 보존된다.
  - `S.mull = {back:[], done:false}`. `done`이 두 번째 기회를 막는다.
  - 상대는 `aiMulligan()`이 한 번만 처리 — 5번째 이후 지형과 6코 이상만 골라 바꾼다.
- **뽑기 보정 — 첫 손패에 지형 최소 1장 확정**(`fixLand(p)`, `dealHand`가 호출).
  덱의 지형 한 장과 손패에서 **가장 비싼 카드**를 맞바꾼다. 7장을 통째로 다시 돌리지 않는다 —
  그러면 구성 전체가 흔들려 보정이 티가 난다.
  ⚠ 보정은 **최초 배분에만** 건다. 멀리건으로 지형을 스스로 버렸다면 그건 플레이어의 선택이다.
  - `test_mull.js`가 200판을 돌려 지형 0장이 한 번도 안 나오는지, 총량이 40장인지 검증한다.
  - ⚠ "되돌린 카드가 다시 안 온다"를 검사할 땐 **덱에 1장뿐인 카드**로 재야 한다.
    기본 지형은 17장이라 같은 이름이 다시 오는 게 정상이다.
  - ⚠ 자동화 테스트는 시작 시 `#keepBtn`을 눌러 이 화면을 넘겨야 한다. `setDeck()`으로 덱을 바꿔도 다시 뜬다.
- **턴 시작 안내**: `turnBanner(p)`가 양쪽 턴 모두 배너를 띄운다(`TURN n` + 내 턴/상대 턴).
  내 턴=에메랄드, 상대 턴=붉은색. `startTurn()`에서 호출한다.
- **공격선 네온화**: `.trace`가 다중 글로우 + 끝점 충격 원(`::after`)을 갖는다.
  기본=금색 · 비행=호박 · 수호 대상=에메랄드. 색은 `--nc` 하나로 바꾼다.

## 로그라이크 모드 (배틀 모드와 엔진 공유)

헤더의 **배틀 / 로그라이크** 버튼으로 전환. `RG.on`이 켜지면 전투 종료가 `rgAfterBattle()`로 빠진다.

- **속성 잠금**: 시작할 때 고른 속성 **하나의 카드만** 나온다(덱·보상·상점 전부). 섞이지 않는다.
- **지도**: 11층(마지막이 보스), 층당 최대 4칸. 아래에서 위로 올라가고 이웃 인덱스로만 이어져 경로 선택이 의미 있다.
  경로선은 `rgDrawEdges()`가 노드 위치를 실측해 SVG로 그린다.
  칸 종류: 일반 ⚔ · 정예 ☠ · 이벤트 ❓ · 상점 🛒 · 보스 👑. 초반 2층은 정예·상점이 안 나오고, 보스 직전 층은 상점으로 바뀐다.
- **보상**: 이기면 골드 + **카드 3장 중 1장**. 정예는 골드가 많고 보상에 **강화 카드**가 섞인다(확률 60%).
- **강화 카드** = `tools/gen_rogue.py`가 140종을 **예산 곡선 위로 +15%** 끌어올린 변형판(`강화 <이름>`).
  크리처는 ATK/HP 비율을 유지한 채 예산을 올리고, 요격·바운스처럼 수치가 없는 스펠은 **코스트를 1 깎는다.**
  ⚠ 일부러 밸런스를 깨는 카드라 `data/rogue.json`으로 분리했고 `validate_budget.py`의 검산 대상이 아니다.
- **적 덱 난이도**: 다른 속성의 견본 덱에서 카드 일부를 강화판으로 치환한다.
  일반 = `0.55 × 층수` · 정예 = +4 · 보스 = +7. 실측 0층 0장 · 4층 2장 · 6층 정예 7장 · 10층 보스 13장.
- **이벤트 10종**(`gen_rogue.py`의 `EVENTS`): 선택지형. **카드 제거에 비중**을 뒀다(10개 중 6개가 제거를 준다).
  효과 종류: `remove` `gold` `heal` `maxhp` `addcard` `upgrade`. 골드·HP를 대가로 받는 선택지가 섞여 있다.
- **상점**: 카드 5장(가격 = 26 + 12×코스트, 강화판 ×1.9) · **카드 제거**(60골드에서 시작해 쓸 때마다 +35).
- **덱과 지형**: 카드가 늘어나면 지형도 비례해 늘어난다(`landsFor()`, 견본 덱의 17/23 ≈ 0.74 유지).
  덱이 커질수록 원하는 카드가 덜 오는 것이 압박이고, **카드 제거가 그 해답**이라는 구조.
- **HP는 런 내내 이어진다. 깎인 체력은 전투를 어떻게 끝내든 그대로 남는다.**
  회복은 이벤트로만(정화의 샘 12 · 야영지 30 · 피의 제단/유물의 최대 HP 증가).
  - `RG.fighting` 이 켜져 있는 동안 `render()` 가 **매 프레임 `S.me.hp` → `RG.hp` 로 흘려보낸다.**
    ⚠ 예전에는 전투가 끝날 때(`checkEnd`) 한 번만 옮겼다. 그 바람에 **지고 있는 전투를
    설정 → '지도로 돌아가기' 로 빠져나오면 다음 전투에 만피로 들어가는 구멍**이 있었다
    (전투를 접으면 보상도 못 받지만 HP 는 공짜로 회복됐다).
  - ⚠ **`RG.fighting` 게이트를 빼지 말 것.** 지도·이벤트 화면에서까지 흘리면
    이벤트가 올려 놓은 HP 를 **지난 전투의 낡은 `S.me.hp` 가 도로 깎는다.**
    켜는 곳은 `rgFight`, 끄는 곳은 `checkEnd`(전투 종료) · `rgMap`(전투 포기) · `startBattle` · `rgStart`.
  - 전투 **중** 회복(축복·흡혈·회복 스펠·성화)은 그대로 둔다 — 그 전투에서 받은 피해를 되돌리는 것은
    카드의 정상 기능이다. 막는 것은 '전투를 안 끝내고 나가서 되감기' 하나뿐이다.
  - 검사: `tools/test_rghp.js`(8건).
- ⚠ 자동화 테스트는 모드 전환·전투 시작마다 `#keepBtn`(멀리건)을 눌러 넘겨야 한다.

## 미확정 사항 (결정되면 README·이 파일 함께 갱신)

1. 인챈트 발동과 크리처 공격의 선후 관계 — 현재 공격 후 인챈트(마지막)로 **잠정 유지**
2. 비행+수호 조합의 예산 가중치 — 잠정치 사용 중
3. 수호 파괴 보상 카드의 강도와 채용 수

## 새 세션에서 이어서 작업하는 법

1. 이 저장소를 기준으로 삼는다("sepi-toolbox/ten 이어서 작업하자").
2. 이 `CLAUDE.md`와 `README.md`를 먼저 읽어 맥락을 복원한다.
3. "현재 상태"와 "미확정 사항"을 확인하고 다음 작업을 정한다.
4. 데이터 변경 시 위 파이프라인(extract → validate)을 지키고, 통과 후 커밋한다.
